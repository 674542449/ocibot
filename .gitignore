# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
.build-venv/
venv/
env/

# Build artifacts
dist/
build/

# Node
node_modules/
web/frontend/dist/

# Local data & secrets (NEVER commit)
web_data/
data/
*.db
*.sqlite
*.sqlite3
*.secret
*.salt
.env
web/.env
*.pem

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/

# Stray agent/scratch dirs
.scratch_tmp/

# 每租户创建锁（SQLite 部署下 quota_guard.tenant_launch_lock 用的文件锁）。
# 放在数据库文件旁边——那是 API worker 和 worker 进程在 SQLite 下唯一保证
# 共享的目录。纯运行时产物，测试也会在仓库根目录留下几个。
.ocibot-launch-*.lock
