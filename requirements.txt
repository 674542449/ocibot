# OCIBot Web — runtime dependencies.
#
# This installs the FastAPI web application together with the shared OCI
# business layer under app/. It simply pulls in the backend's own pinned list,
# so `pip install -r requirements.txt` from the repo root is all you need.
-r web/backend/requirements.txt
