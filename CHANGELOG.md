# Changelog

## 0.4.91 — 2026-08-25

### 修复

- **容量雷达点「开始探测」没有结果 —— 而且它做了一件它承诺过绝不会做的事。**

  雷达为了拿一份可用域列表，调了 `fetch_launch_meta()`。那个函数的冷调用里有：

  ```python
  f_network = pool.submit(session.ensure_default_network,
                          compartment_id=default_comp, create_if_missing=True)
  ```

  `create_if_missing=True` —— 它会在租户里**创建 VCN、子网、网关和路由表**并等每一个
  变成可用状态，同时还给每个 OS family 列一遍镜像。它自己的 docstring 写着
  「A minute or more is normal」。

  后果有两层：
  1. 用户在雷达页（从没点过「加载配置」，所以缓存是冷的）点探测，请求要跑一分钟以上，
     浏览器/反代先超时 —— 表现就是「探测没结果」；
  2. 一个从页面副标题到 CHANGELOG 都写着「只读，绝不创建任何实例」的功能，
     **会创建网络资源**。这不是慢，是把承诺破坏了。

  现在改成：先 `peek_launch_meta()`（零 Oracle 请求，命中就白拿），没命中就单发一次
  `list_availability_domains()` —— 一次纯读、没有任何写入，正好是需要的那一样东西。
  顺带雷达不再依赖「必须先去创建页点过加载配置」，自己就能用。

- **这个 bug 是被测试桩掩盖的。** `tests/test_endpoint_smoke.py` 把
  `fetch_launch_meta` 整个换成了一个瞬间返回的假函数，于是测试全绿、线上超时 ——
  桩偷懒的地方恰好就是会出问题的地方。smoke 现在桩的是雷达**真正会走**的那个调用；
  另加两条源码断言，钉死雷达路由里不出现 `fetch_launch_meta(`（以及
  `tenant_launch_lock(`）。用源码断言而不是行为断言，是因为真正的危险是**以后有人
  为了图省事又把它加回来**，而那条路径在测试里是被桩掉的，行为断言看不见。

- **探测失败时页面看起来像「没结果」。** 失败信息渲染成一行小灰字，而此刻那张
  「还没有探测。上面的空白不代表没有容量。」的解释卡因为 `result` 已存在被隐藏、
  下面的网格又是空的 —— 整页就是一片空白加一行灰字。现在「没能进行探测」用
  `error-box` 显示并说明「这不代表没有容量」；「探测完成但零个可用域返回」
  单独用 `warn-box` 说明，不再和「没有容量」混成同一片空白。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.91
```

## 0.4.90 — 2026-08-25

### 修复

- **「测试连接」把一个它无法知道的原因当成结论说了出来。** 一个满权限的账号,
  点「测试连接」时**有时**通过、有时报「对 Compartment …k2wjnmitzo4po5wa 的读取
  权限不足 / 这是 IAM 策略范围的问题」,于是操作员被反复推去改一条本来没问题的策略。
  那句结论出自代码里一行注释:「Credentials are good past this point; anything below
  is a policy/scope problem」—— 它是错的。`get_user` 成功之后,探针仍然可能因为
  限流(429)、Oracle 5xx、网络超时、SDK 熔断器打开而失败,这些都不是策略问题。

- **探针表本身也是坏的,「2 通过 1 失败」根本推不出「compartment 读不了」。**
  旧表是 `{ListInstances, ListCompartments(ACCESSIBLE), ListAvailabilityDomains}`:
  * 后两个需要的是**同一个**权限 `COMPARTMENT_INSPECT` —— 同一件事测了两遍;
  * `ListCompartments` 传的 `access_level="ACCESSIBLE"` 是个**过滤器**(SDK 文档
    明说它只返回你有 INSPECT 权限的那些),零权限时返回空页 + 200,**结构上不可能
    因为缺权限而失败** —— 那一票是废票。
  所以真实的探针集合是 `{INSTANCE_READ, COMPARTMENT_INSPECT, 废票}`。
  而 **`ListInstances` 需要的是 `read` 不是 `inspect`** —— 一条只写了
  `inspect instance-family` 的策略会**永久**产生一模一样的「1 挂 2 通」。

- **新探针表把两条轴分开**,让形态本身能指认原因:
  | 探针 | 服务 | 需要的权限 |
  |---|---|---|
  | 列出实例 | Compute | `read instance-family` |
  | 列出规格 | Compute | `inspect instance-family` |
  | 读取 Compartment | Identity | `inspect compartments` |
  | 列出可用域 | Identity | `inspect compartments` |
  「列出规格通过、列出实例失败」→ 直指 verb 写错(inspect 少了 read);
  「两个 Compute 都挂、两个 Identity 都通」→ 差异在**服务端点**(Compute 走
  `iaas.*`、Identity 走 `identity.*`),常见于副区刚订阅不久,与 Compartment 无关。

- **对 404 自动重读一次,用来区分「瞬时」和「持续」。** 同一个调用 1.5 秒后就通了,
  那次 404 按定义就是瞬时的 —— 面板会直接说出来,并**不再**建议去改策略。
  只对 404 做(404 不是限流信号,SDK 本身也从不重试它);429/5xx 一律不重读,
  SDK 刚退避重试过,再补一次纯粹是抢抢机重试循环的请求预算。

- **记下诊断需要的事实,不再只留一个错误码。** 每个探针现在报出 HTTP 状态、
  错误码、**opc-request-id**、operation、端点主机、耗时。opc-request-id 是开工单时
  Oracle 唯一认的东西,而且只存在于那一次响应里 —— 以前被直接丢掉,故障过后再也
  找不回来。`_format_service_error` 也一并附上它。
  只取端点的**主机名**:`request_endpoint` 原文里带**完整 compartment OCID**,而
  SDK 的 `redact_sensitive_string_for_logs` 只脱敏凭据头、不脱敏 `compartmentId`
  (对 2.182.0 实测确认)。

- **探针改用收敛的重试策略(2 次 / 20 秒)**,不再用 client 默认那套(8 次 / 600 秒)。
  默认策略比前端超时和常见反代的 100 秒都长,所以一次真被限流的「测试连接」根本不会
  返回文案 —— 它会在浏览器里超时,而后端还在继续烧预算。收敛之后 `[429]` 才有机会
  被显示出来,而那正是本次要区分的东西之一。只改探针的单次调用策略,**不动
  client 级默认策略**(worker 的抢机循环用的是同一批 client)。

- **成败判定改为只看「列出实例」这一条。** 旧逻辑是「任一探针失败即报连接失败」,
  而旧表里有一个探针结构上不可能失败、另外两个测的是同一件事 —— 那种判定既不敏感
  也不特异。其余探针作为诊断上下文报出来。**这是一处有意的行为放宽**:原先会因
  Identity 探针失败而显示「连接失败」的租户,现在会显示「连接成功 + 提示」。

### 维护

- 新增 `tests/test_connection_diagnosis.py`(15 条):不再出现那三句断言、重读成功后
  不再提策略、inspect/read 与端点两种形态各自被指认、限流和熔断器不被讲成权限问题、
  排查清单按命中项动态编号(旧写法会渲染出「2) … 2)」)、文案是纯文本不含 markdown
  星号、以及**任何诊断文案和事实集合里都不出现完整 OCID**。
- 全量 1006 passed。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.90
```

## 0.4.89 — 2026-08-24

### 修复

- **容量雷达读不到时的提示把话说死了。** 原文是「该 API 密钥没有容量报告权限」——
  但 Oracle 的 `NotAuthorizedOrNotFound` 是**故意做成歧义**的:「没权限」和
  「这东西不存在」返回同一个 404。断言成权限问题会让人去改一条本来没问题的策略,
  而真正的原因可能是该区域暂不提供这个接口。改成给出最可能的原因和排查顺序,
  并写明:**用注册账号本人(租户管理员)的 API Key 时,`Administrators` 组默认的
  `manage all-resources in tenancy` 已经包含 `compute-capacity-reports`,不用加任何策略。**
  0.4.88 的升级说明同样改口(原来写的是「需要一条新的 IAM 策略」)。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.89
```

## 0.4.88 — 2026-08-24

新功能:**容量雷达**。创建实例之前先问 Oracle「这个可用域现在还开不开得出这台机器」,
用的是官方的 CreateComputeCapacityReport 接口。

### 功能

- **「工作台 → 容量雷达」新页面**(`/radar`)。选租户、填规格(或点 4C24G / 2C12G /
  1C6G 快捷档)、点「开始探测」,面板会对该租户的每一个可用域各发**一次只读请求**,
  按可用域出卡片,卡片里带每个故障域(FD)的明细芯片。探到有货的可用域上有「去创建 →」,
  一键跳到创建页并带上租户 / 可用域 / 规格。
- **创建实例的确认框里加了同一份探测**。点「确认创建配置」时会在免费额度预检之后
  再探一次容量,结论直接显示在确认框里;报告说无货时「确认并创建」按钮降级成普通
  按钮、文案变成「仍然创建」。
- 只支持 `VM.Standard.A1.Flex`。别的机型要么不是免费的(抢不到不是常态),
  要么是固定规格(没有 shape config 可探)。

### 这个功能**不**做什么(重要)

- **它不是硬门,报告说没货也拦不住你创建。** 三条理由,每一条都足以否掉硬拦:
  (1) 容量报告是瞬时快照,和随后那次 LaunchInstance 之间必然有竞态;
  (2) Oracle 自己的 CLI 仓库有一个**至今未关闭**的 issue(oracle/oci-cli #748)
  记录了 A1.Flex 上结论完全倒置的案例 —— 报告说有货的可用域开不出来、说无货的
  反而开得出来;(3) `CreateComputeCapacityReport` 需要一条和创建实例**完全不相交**
  的 IAM 授权(`manage compute-capacity-reports`),「能创建实例但调不了报告」
  是常见配置而不是边缘情况。
  硬拦就等于把 0.4.84/0.4.85 那个故事换一个权限重演一遍 —— 那次是预检比服务端严格,
  导致缺 `inspect compartments` 权限的租户从 UI 上**永久**无法创建实例。
- **它不是零消耗。** 这个调用确实不创建任何资源、不计费(响应模型连 `id` 和
  `lifecycle_state` 都没有,ComputeClient 上也只有 create、没有 get/list/delete),
  但它是一次普通的 Core API POST,和 LaunchInstance 走**同一个 per-tenancy 请求
  速率桶**。它省下的是一次注定失败的写入,不是省下一次请求。
- **它不进抢机任务的重试循环。** 每次尝试前加一次三可用域探测会把那个循环的请求量
  放大 7 倍(1440 次/天 → 10080 次/天),而放大的正是 CLAUDE.md 要求把预算留给它的
  那个循环。抢机的容量信号仍然只来自它自己的 LaunchInstance 结果。
- **它不进 `tenant_launch_lock`。** 那把锁保护的是「取额度快照 → LaunchInstance」这个
  check-then-act 窗口,而探测什么都不改;进去只会让探测把别人的创建堵住。

### 实现上的几处刻意选择

- **失败一律降级成「读不到」,绝不冒充「没有容量」。** 权限不足、429、Oracle 5xx
  都渲染成红色的「读不到」并附一句人话;红色在这一页只有这一个含义。「没货」用琥珀,
  和任务中心保持一致(那边容量错误也用中性徽章,红色只留给真正的失败)。
  混成一种颜色会让人放弃一台其实等一会儿就能开出来的机器。
- **权限提示不走通用的 404 诊断。** 通用文案会让人去检查 API Key / Fingerprint
  并点「测试连接」—— 而「测试连接」的三个探针在缺 `manage compute-capacity-reports`
  时会**全部通过**。雷达自己出文案,直接给出要加的那条 IAM 策略。
- **SDK 重试显式关掉**(`NoneRetryStrategy`,和 LaunchInstance 一样)。默认策略是
  8 次尝试 / 总时长上限 600 秒,不关的话一次被限流的探测最多变成 8 个 HTTP 请求并
  占住一个请求线程十分钟 —— 本项目所有路由都是同步的,卡住几十个就够让整个面板
  看起来像死了。
- **`available_count` 为 null 时绝不渲染成 0。** 普通公有云租户这个字段恒为 null
  (只有 DRCC / 白名单租户拿得到数字),折成 0 会显示「可开 0 台」,和「无货」
  长得一模一样,而它的真实含义是「有货,但 Oracle 不告诉你还剩几台」。
- **可用域的结论取主配置,不取所有配置里最好的那个。** 否则「4C24G 无货、1C6G 有货」
  会渲染成一个绿色的可用域,而用户要的是 4C24G。备用配置的结论照常返回。
- **一次调用带上全部规格。** `shape_availabilities` 是个列表,所以成本 = 可用域数,
  不是可用域数 × 规格数。
- **`compartment` 先用租户配置的那个,只在 404/403 上回退一次租户根,429 绝不回退**
  (回退会把一次限流变成两个请求)。SDK 文档说这里"should always be the root
  compartment",但本仓库在几乎相同的调用上踩过反向的坑:IAM 只覆盖子 compartment 的
  密钥问根会拿到 NotAuthorizedOrNotFound。
- **限流 + 缓存两道。** 每 (用户, 租户) 6 次 / 5 分钟;60 秒进程内缓存,缓存键包含
  租户 / 区域 / compartment / 可用域 / 规格 —— 这是本功能唯一一处真正的多租户隔离点,
  漏掉任何一项就会把 A 的结论渲染到 B 的面板上。限流键则**不含规格**,否则客户端
  把内存加 1 就能绕开。
- **进页面不自动探测**,和其余所有页面一样(0.4.20 试过自动拉取、0.4.21 回退)。
  未探测时不渲染空网格,而是一张明确写着「上面的空白不代表没有容量」的说明卡 ——
  这一页最危险的误读就是把空白读成结论。

### 维护

- 新增 `web/backend/capacity_radar.py`、`web/frontend/src/views/CapacityRadarView.vue`,
  `app/oci_client.py` 加 `get_capacity_report()`,新端点
  `POST /api/tenants/{id}/capacity-report`。
- 新增 `tests/test_capacity_radar.py`(31 条),覆盖:四种状态枚举(含 SDK 的
  `UNKNOWN_ENUM_VALUE` 和 `None`)、AD 级汇总行没有 FD 时不崩、`available_count`
  的 null 不变 0、可用域结论取主配置、单个可用域失败不作废其它、权限失败必须是
  「读不到」而不是「没货」、429 不回退 compartment、404 回退一次、缓存的多租户隔离、
  以及请求里确实没有 `fault_domain` / `baseline_ocpu_utilization` / `nvmes`。
- `tests/test_endpoint_smoke.py` 覆盖新端点的两条路径(指定可用域 / 探全部)。
- 全量 991 passed。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.88
```

**可能需要一条 IAM 策略。** 先直接点一次「开始探测」——

* 用注册账号本人(租户管理员)的 API Key:`Administrators` 组默认的
  `manage all-resources in tenancy` 已经包含 `compute-capacity-reports`,**不用加**。
* 专门给面板建了受限 IAM 用户:需要加下面这条,否则雷达显示「读不到」。

```
Allow group <你的用户组> to manage compute-capacity-reports in tenancy
```

不加不影响任何其它功能 —— 探测失败是刻意 fail-open 的,创建实例、抢机任务、
额度守卫都照常工作,只是雷达那一页用不了。

## 0.4.87 — 2026-08-24

把 0.4.86 里判定为「低危、可以先放着」的那一批全部修完。六条并行的修复线各自独占
一组文件,改完立刻交给一个专门找茬的复核 agent —— 复核抓出四条不该放过的问题
(其中一条会让全量测试变红),都在本版一并处理。修的过程中又暴露出两条更严重的。

### 修复

- **【高】抢机任务和浏览器端创建之间从来没有互斥过。** `web/AUDIT.md` 第十一轮把
  「`check_launch_quota` 是 check-then-act,没有互斥」列为 High,修复记录写的是
  「Wired around snapshot→launch in `routers/instances.py` **and the worker**」——
  后半句从来没有成立:全仓库 `with tenant_launch_lock` 只有 `routers/instances.py`
  一处。worker 自己的 `_busy_tenants` 和数据库租约只让**抢机任务之间**串行,对一个
  正在页面上点「创建实例」的人完全没有作用,两边各自读到同一份「已用 0」的快照,
  各开一台,Always Free 额度双花。抢机任务一挂一整夜,撞上并不需要多巧。
  worker 现在在自己的「取快照 → LaunchInstance」窗口上取同一把锁,拿不到就推迟本次
  尝试(且不消耗尝试次数);LaunchInstance 一返回立刻放锁,不把跨进程锁攥到后面
  几十秒的推送 I/O 上。`tests/test_worker_launch_lock.py` 钉住这几件事,AUDIT.md
  那句不实的记录也一并更正。
- **【高】免费出网额度的阈值比真实上限宽约 10%。** `egress_gb` 用 `bytes / 1024**3`
  算出的是 GiB,而 `FREE_EGRESS_GB = 10240` 也是 GiB —— 两边二进制自洽,所以内部
  比较不会立刻出错。但 Oracle 的「每月 10 TB 出网」是**十进制**计的
  (10^13 字节 ≈ 9313 GiB),于是守卫要到 10240 GiB 才报警,中间那 900 多 GiB
  是要计费的,而面板一直显示「仍在免费额度内」。标签写的也一直是「GB」。
  出网和对象存储统一改成十进制,和 Oracle 控制台/账单对得上。
- **密码到期倒计时少算一天,而且用的是 UTC 日历日。** `(expires_at - now).days`
  向下取整,而 `last_set` 总在过去若干小时,所以几乎每个真实密码都少一天:120 天
  策略下今天刚改的密码报 119 天;明天到期的报成「2026-08-25 到期(还有 0 天)」——
  同一句话里日期说明天、天数说 0。日期也取的是 UTC 日历日,一个 20:00Z 到期的密码
  对 UTC+8 的操作员会印成前一天。改成本地日历日之差,并按**到期那一刻**的时区偏移
  换算(不是拿今天的偏移去套一个几个月后的时刻 —— 那样在有夏令时的地区反而比原来
  的 UTC 版本更糟)。
- **管理员的「只看登录」审计视图,看不到针对真实账号的登录失败。** 失败事件在用户名
  存在时会被归属到**被匹配上的那个账号**,而查询按 owner 隔离,于是管理员只看得到
  瞎编用户名的乱撞,看不见有人正在撞一个真实账号;连「密码是对的、只被 2FA 挡住」
  这条信号最强的事件也一并滤掉。该文件顶部的注释却写着「Admins see them now」。
  现在只在 `auth_only=true` 且请求者是管理员时放宽到登录类事件,默认视图的 owner
  隔离原样保留(租户、实例、存储、备份、通知一概不跨账号可见),AUDIT.md 里那条
  不变量同步更正。事件白名单补上注册 / 改密码 / 开关 2FA,前端标签表同步。
- **推送失败在面板上不留任何痕迹。** 两处 `notify_user` 的返回值都被直接丢弃,而它
  从不抛异常 —— 于是「抢机成功,五个渠道全部推送失败」显示成一条绿色的成功任务,
  唯一线索是 worker 容器日志里的一行 warning。现在写成 `notify.failed` 审计事件
  (SAVEPOINT 隔离,记不上账也不会回滚抢机结果),审计页能看到「几个渠道没推送、
  分别为什么」。
- **Telegram 收到 429 后会立刻再发一次。** 「退回纯文本重发」本意只应对 Markdown
  解析错误,写成了无条件重试:一个 `retry_after: 30` 会产生 2 次 POST,第二次紧接着
  打向一个刚刚要求暂停 30 秒的端点。
- **主密钥轮换后,每个通知渠道报的都是「缺少字段」。** 解密失败被吞成 `{}`,于是
  Telegram 说 bot_token 非法、webhook 说 URL 为空、SMTP 说缺 host/username…
  五个渠道五种说法,每一种都指向一个其实没问题的字段,真正的原因
  (`OCIBOT_MASTER_KEY` 变了 / `.env` 没加载)一个字都没提。
- **SMTP 表单缺 `require_tls` 开关,而错误信息却让操作员去设它。** 服务器不支持
  STARTTLS 时后端会中止发送(继续发等于把邮箱密码明文送上网),提示让「在渠道配置中
  显式设置 require_tls=false」—— 面板上根本没有这一项,而且手发 PATCH 也不行:
  `update_channel` 是整包替换 config 并做必填校验,只提交 `require_tls` 会被拒成
  「缺少字段」。现在设置页有这个开关(默认开启,关闭时当场说明后果),错误信息也
  改成指向真实存在的操作路径。
- **端口输入框的 `465` 占位符暗示「不填就是 465」,留空却 400。** 后端必填清单里有
  `port`。改成留空即按 465 处理,和 `_smtp_port` 自己的兜底取同一个默认值。
- **通知扇出没有 `ORDER BY`,预算截断时丢掉哪些渠道由数据库决定。** 同一台机器同一个
  用户,这次推到了 Telegram、下次只推到了邮件,而操作员无从复现。
- **字节格式化用 1024 做除数却打 SI 标签**,`human_bytes(10**9)` 显示成 `953.7 MB`;
  而前端 `formatMetricValue` 用的是 1000 + 同样的标签,两者每 GB 差 7.4%。统一成
  十进制(理由:这些数字最终要和 Oracle 控制台对照)。顺带修掉:单位选定后才四舍
  五入导致进位线上打出 `1024.0 KB` 这种本档不可能出现的数;B 档用 `int()` 截断
  丢小数并打出带负号的零 `-0 B`;`axis_max` 在 `peak <= 0` 时原样返回调用方给的
  `minimum`,可能返回 0(除数)或一个负的「上界」。
- 通知渠道列表里,`events` 为空数组(谁也不订阅、永远收不到推送)渲染成一片空白,
  和「还没加载出来」长得一样。

### 维护

- **修掉一处会让全量测试变红的测试污染。** `test_feature_fixes.py` 和
  `test_sweep_fixes.py` 用裸赋值给 `notify.send_to_channel` 打桩、结束后不还原,
  那个恒返回 `(True, "sent")` 的桩会泄漏给按文件名排在后面的测试。新加的
  「主密钥轮换要报出真正原因」那条因此单独跑通过、全量跑失败。两处改成
  `monkeypatch.setattr`。
- 修掉三条「看着在测、其实没测到」的断言:SAVEPOINT 回滚测试原来让 `AuditLog`
  在**构造**时抛异常,SAVEPOINT 里一条 SQL 都没发出去,回滚的是个空 savepoint;
  `axis_max` 那条把分子写死成 `0.0`,在 `top > 0` 成立的前提下是恒真;
  密码到期的三条边界断言不钉时区,会在每天午夜前后各半小时必红一次。
- 更正两条与代码实际行为不符的注释(`require_tls` 开关为什么单独放一个 ref、
  通知渠道 `created_at` 是否可能为 NULL),以及 `web/AUDIT.md` 里两条已经不再成立
  的不变量记录。安全审计记录说假话,下一个审计员会照着它推理。
- 新增 `tests/test_worker_launch_lock.py`、`tests/test_formatting_units.py`、
  `tests/test_audit_auth_visibility.py`、`tests/test_notify_reliability.py`、
  `tests/test_password_expiry_days.py`、`tests/test_launch_lock_scope.py`。
  全量 960 passed。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.87
```

## 0.4.86 — 2026-08-24

全功能**业务逻辑**审计（九个方向并行）。前两轮查的是安全漏洞,这一轮查的是
「代码跑得通、但结论是错的」—— 类型检查、冒烟测试、构建全绿也照样发生的那一类。

### 修复

- **【严重】主区被添加成「副区」租户后,免费额度守卫在唯一有 Always Free 的
  区域里彻底失效。** `enforce_secondary_region` 先看 DB 的 `parent_tenant_id`
  再看真实区域,于是一条 region 恰好等于主区的子行被判成副区 —— 而副区是**整段
  跳过** `enforce_launch_quota` 的。结果是额度已经 100% 用完的账号,`POST /launch`
  照样返回 200,并附一条自相矛盾的提示「副区「ap-tokyo-1」…(主区 ap-tokyo-1)」。
  DB hint 的职责本来就是兜底一次**失败**的读取,而不是推翻一次读成功的结论,
  现在一次成功的读取说了算。判定逻辑收进 `quota_guard.resolve_secondary`,
  worker 里那份同样写反的副本一并改掉;`开通副区` 也不再允许为主区建子行。
- **抢机任务被一次网络抖动杀死。** 错误分类只有「容量」和「其他」两档,
  其他一律 `enabled=False` 永久停止 —— 一次 DNS 抖动、一次读超时、Oracle 一个
  503,都会让一个准备跑一整夜的任务在第一次抖动时死掉,早上看到的是
  「遇到非容量错误」,而那个错误早就过去了。新增 `is_transient_error`,
  传输层故障和 5xx 按容量错误同样的节奏退避重试;认不出来的仍按永久错误处理。
- **一个填错的备用规格拖垮整晚的抢机。** AD × 配置轮换里,某个备用配置被
  Oracle 拒绝时,整个任务被判死,主配置和其他 AD 一起陪葬。现在只把那一格从
  轮换里摘掉继续跑。主配置出错仍然停止 —— 那是任务本身填错了。
- **抢机成功的通知在未提交的写事务里发出。** `notify_user` 会做网络 I/O
  (SMTP 最坏情况几分钟),而此时 `job.status`、`success_instance_id` 和一条
  attempt INSERT 都还没提交:SQLite 部署下整库写锁被占住,期间所有登录、审计、
  租户编辑一律 `database is locked`;PostgreSQL 下则是一个几分钟的
  idle-in-transaction。更糟的是通知内部失败会让 PG 事务进入 aborted,
  `finally` 里的 commit 随之抛出,**这次抢机成功的结果连同 OCID 一起被回滚**,
  而机器已经开出来了。改成先落库再通知。
- **SMTP 没有总超时。** `_SMTP_TIMEOUT` 传给 smtplib 后只是 socket 超时,管的是
  单次 recv。一次投递十来个来回,实测一台每次响应前拖 0.8 秒的服务器就能把
  「20 秒超时」变成 6.5 倍。而 `notify_user` 只在每个渠道**之前**看一眼 60 秒
  预算,于是一个卡住的邮件服务器顶穿预算,并且因为它是在 worker 的抢机循环里
  调用的,全站其他用户的抢机尝试跟着一起等。新增 45 秒会话总期限
  (HTTP 路径早就有等价的 `_TOTAL_DEADLINE_SEC`)。
- **备份静默丢租户。** 主密钥轮换后,私钥解不开的租户会被跳过,而这份名单只
  出现在 `X-OCIBot-Backup-Notice` 响应头里 —— 前端一个读它的地方都没有,界面
  照旧显示「备份已下载」。现在前端会读并高亮告警,名单同时写进压缩包的
  `README-first.txt`:几个月后拿着这个文件去恢复的人,手边只有这个 zip。
- **批量/单台电源操作把失败印在绿色成功框里。** 这些接口只有传输层错误才抛
  502,OCI 自己拒绝返回的是 **200 + `ok:false`**。批量操作因此把「全部失败」
  报成「N/N 成功」,失败原因一条不显示;单台的开关机、重命名、终止同理。
- **Server酱把失败报成已发送。** 成败在响应体的 `code` 里,HTTP 状态只在 429
  时才是错的。SendKey 过期、当天推送次数用完都是 `200 + {"code":4000x}`,
  以前一律「已发送」,「测试」按钮跟着报绿 —— 操作员会对一条从来没通过的
  通知链路建立信心。
- **「测试」验的是链路通不通,不是这个渠道真会不会响。** 被停用的、或者事件
  订阅被取消勾选(`events: []`)的渠道,测试照样报绿,而实际通知一条都不发。
  现在测试成功时会把这两个状态说明白。前端新建渠道后不再继承上一次的空事件
  列表 —— 那会让**之后每一个**新渠道都订阅不到任何事件。
- **升级上来的老渠道显示成「一个事件都没订阅」。** `events` 为 NULL 在发送侧
  是「订阅全部」,读回来时却被塌成 `[]`,和真正订阅不到东西的渠道长得一样。
- **CPU / 内存曲线按峰值归一化。** 百分比的坐标轴天然就是 0–100,按本段数据的
  峰值缩放的后果是:一台常年 2% 的空闲机器和一台跑满 99% 的机器画出来一模一样
  的高位曲线,卡片上又没有轴和刻度,只有页脚那个数字是对的。网络吞吐没有天然
  上限,仍按峰值缩放。顺带修好只有一个采样时曲线全空、悬停圆点却钉在左上角,
  以及悬停取点忽略 4px 内边距导致的偏移(24 小时窗口下最多差十几分钟)。
- **每日费用柱状图的高度是仿射函数而不是线性的。** 高度算的是
  `svgH - 24 - barY(...)`,比 `barY` 的零点高 8 —— 柱子之间的比例全是错的
  (1.2 和 2.4 这样 2:1 的两天画出来是 2.4:1),低于峰值 5.9% 的一天被压成 1px、
  跟零成本的一天没区别,而零成本那根柱子的底边又比别人低 8,横轴线卡在中间。
- **超出免费额度的软额度桶徽章写着「接近上限」。** 3/2 个公网 IP、15000/10240 GB
  出网(超出部分要计费)都会显示成「接近上限」,而紧挨着的数字写着已经超了。
  「不参与硬性总判定」这件事本来就由汇总循环独立完成,不需要在状态上说谎。
- **SSH 公钥以裸 YAML 标量写进 cloud-init。** 公钥尾部的注释是自由文本,
  含 `: ` 会被解析成映射、含 ` #` 会被截断 —— 两种情况都会让整份 cloud-config
  装不上,结果是一台**没有任何公钥、连不上去**的机器,而创建流程一路显示成功。
- **IPv6 默认路由只比目的地址,不比下一跳。** 一条已存在但指向别处(NAT 网关、
  别的 IGW)的 `::/0` 会被当成「已经配好了」,实例拿到 IPv6 地址、路由表看着也
  有 `::/0`,但出网根本不通。
- **调整引导卷会静默把性能等级降到 10 VPUs/GB。** 表单初值是 10,而它不管有没有
  加载过引导卷信息都渲染 —— 在一块 120 VPUs/GB 的盘上只填个新容量就提交,
  性能被一起改掉,界面没有任何地方提示过。现在未加载时拒绝提交,降级要确认。
- **改 OCPU / 内存会重启机器,页面上却没说。** Oracle 走的是 reboot migration,
  跑着服务的机器会当场断线。其他会中断服务的操作一直都有确认框,这里漏了。
- **副区抢机任务无视「仅使用免费额度」的后续勾选。** 任务入队时租户是允许计费
  的,但用户之后可以把这个勾重新打上;任务长期挂着跑,这个翻转必须被看见,
  否则一个明确表示「只要免费」的用户会在副区被开出一台计费机器。
- **额度守卫拦下的任务,通知标题写的是「❌ 容量重试失败 / 遇到非容量错误」。**
  把面板自己做的免费额度拦截说成 Oracle 报的错,操作员会去查 API Key 而不是
  去看额度。
- 修掉 0.4.85 引入的一处 `NameError`:`instances.py` 里 `free_quota` 已经是一个
  路由函数的名字,模块名被它遮住,创建前额度预检会 500。冒烟测试抓到的。

### 维护

- 新增 `tests/test_functional_audit_0486.py`,20 条断言覆盖上述可单测的结论:
  副区判定的优先级(含 hint 仍需兜底失败读取)、瞬时/永久错误分类、cloud-init
  的 YAML 注入、软额度状态、Server酱返回码。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.86
```

## 0.4.85 — 2026-08-24

接着 0.4.84 修同一类问题:**面板把「读不到」说成了别的东西**。

### 修复

- **实例列表不再把权限拒绝渲染成「你没有实例」。** `list_instances_tree` 只在
  「只剩一个 compartment 且一台都没扫到」时才抛异常;有两个以上 compartment 而
  **全部被拒**时,它安静地返回空数组,页面于是显示
  「暂无实例。请先在「租户」添加 API,再「创建实例」」—— 把一次权限失败说成空账号,
  还让人去做一件他早就做过的事。**部分成功更隐蔽**:返回一个看着合理的子集,
  没有任何标记说明它不完整。

  现在路由通过 `X-Ocibot-Partial` 响应头把这件事传出去(不改返回结构,以免同时
  打破前端契约和 TS 类型),列表页顶部显示「这份列表可能不完整」,空态文案也改成
  指向权限而不是指向「去添加 API」。

- **存储页同理。** 后端一直在返回 `errors` 数组、并在 message 末尾附
  「(部分 compartment 读取失败 N 处)」—— **前端从来没读过这两样**。而引导卷页那张
  「未挂载合计 X GB,正在占用免费额度」的卡片正是基于这份可能被截断的列表算的。

- **账单读不出来时不再宣称「不会产生账单」。** 原文案是
  「未查询到账单。Always Free / 试用账号没有订阅,**因此不会产生账单**」——
  这是从一个失败的读取里推出了一个肯定的财务结论。如果真相是「缺少账单读取权限」,
  这句话会让一个正在计费的账号看起来是免费的。前端也终于开始读后端早就设好的
  `unavailable` 标记(此前只看 `ok`,而这条路径返回的是 `ok=true`)。

- **成本读取失败时不再显示「合计:0」。** 三个失败分支都返回 `total: 0`,而模板用的是
  `?? '—'`,`0` 不是 nullish 所以原样显示。讽刺的是这三处的上一行注释就写着
  「month_to_date 用 None 不用 0,对费用数字来说这是两个完全不同的意思」——
  写了原则,下一行违反了它。现在统一为 `None`。

- **扩容失败不再被诊断成 SSH 密码错。** 提示是拿
  `last.message + last.stderr` 做子串匹配的 —— 而 `stderr` 是**远端命令**的输出,
  能拿到它本身就证明 SSH 已经连上并认证成功了。`fs_grow` 的脚本执行
  growpart / resize2fs 时**不带 sudo**,默认登录名又是 `ubuntu`,所以这个功能最常见的
  真实失败会在 stderr 打出 `Permission denied` —— 然后面板告诉你「请检查用户名与私钥
  是否匹配」。现在连接层只看连接层的信息,客体侧的权限失败单独识别并明说「需要 root」。

- **创建实例的额度预检不再和服务端漂移。** 创建路径的 `enforce_launch_quota` 在用量
  读取不完整时会 503 拒绝,而预检用的 `check_launch_quota` 不做这一步。于是快照退化成
  「零用量」,预检回 `blocked=false`,「下一步」正常打开确认框,点「确认并创建」才被
  503 挡下。本路由的 docstring 写着「面板的预检结论不会和服务端漂移」——
  而这正是一处漂移。

### 维护

- 新增 `tests/test_refusal_not_emptiness.py`(7 个用例)。
- `ssh_bridge` 的提示逻辑抽成 `_enrich_hints()`:它内联在重试循环里时,
  「连接失败还是命令失败」这条判断没有任何办法单独验证 —— 而它当时是反的。
- CORS 增加 `expose_headers`:自定义响应头默认对跨源 JS 不可见
  (`allow_headers` 管的是**请求**头),不加的话跨源部署下前端读到的永远是 undefined。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.85
```

## 0.4.84 — 2026-08-24

修 `[404] NotAuthorizedOrNotFound` 时好时坏,以及面板对它的**错误诊断**。

### 修复

- **「加载配置」向租户根读取镜像 / Shape / 可用域,而不是租户配置的 Compartment。**
  这是那个 404 最直接的原因。`fetch_launch_meta` 第 456 行已经算好了
  `default_comp = compartment_ocid or tenancy_ocid`,自定义镜像和网络也用的它,
  唯独镜像、Shape 传了 `tenancy_ocid`;`list_availability_domains` 更是把租户根
  写死在函数里。**IAM 权限是按 Compartment 判的** —— 密钥只授权到子 Compartment
  时,问根就是 404,而可用域恰好是并行块里第一个硬抛的调用,于是整页报一个和
  可用域毫无关系的错。

  还有一层:`list_images` **自带**一个「子 Compartment 结果太少就退回根」的回退,
  它的条件是 `compartment != tenancy_ocid` —— 把根直接传进去,恰好让这条专为
  此场景设计的退路失效。后面那两个顺序回退(`if not images` / `if not shapes`)
  用的是正确的 Compartment,但并行块已经先抛了,永远走不到。

- **`NotAuthorizedOrNotFound` 不再被诊断成「请检查 API Key」。**
  这是 Oracle 故意做成模糊的 404,含义是「没权限 **或** 不存在」,谈的是 IAM 策略
  的作用范围。而判断顺序有 bug:它含 `notauthorized`,所以永远先命中凭据那条,
  下面本来就有的、正确得多的 404 分支**从来没被执行过**。

  现在按状态码分开:404 明说不是密钥问题并列出三个真实原因;401 才提示检查
  Key/Fingerprint/OCID(那才是签名没过);403 说明凭据有效但缺少 verb。

- **「测试连接」以前不测该测的东西。** 它算出 `compartment` 变量后**根本没用**,
  只调 `get_user` —— 而那个调用几乎任何有效密钥都能成功,和 IAM 策略无关。
  于是策略配错的租户一路绿灯,然后在每个真实页面上 404,面板还建议去检查刚刚
  被验证过没问题的密钥。现在会额外探测「列出实例 / 列出子 Compartment /
  列出可用域」,**逐项报出哪一项读不到**,并说明这是策略范围问题、重新生成密钥没用。

- **租户页新增「测试连接」按钮。** 此前 `POST /tenants/{id}/test` 全站只有一个
  调用点:粘贴导入时那个「保存后自动测试连接」复选框。手动添加、编辑,以及任何
  已存在的租户,都没有办法再跑一次 —— 而这恰恰是排查权限问题唯一能用的检查。

- **实例列表报错会说明扫描范围塌缩了。** 硬报错的条件是「只扫了 1 个 Compartment、
  没找到实例、且有错误」。枚举子 Compartment 成功时能扫到实例、一切正常;枚举失败时
  退化成只扫根,根里没实例又没权限,于是报错 —— 同一个租户、同一把密钥,
  **表现出来就是时好时坏**。以前只甩一句光秃秃的 Oracle 404,看不出扫描范围已经塌了。

- **错误提示的换行不再被压掉。** `.error-box` / `.success-box` 补上
  `white-space: pre-line`。权限诊断是分条写的(哪一项没权限、该加哪条策略),
  以前只有 `LaunchView` 自己加了内联样式,其余 12 处都会塌成一长串跑不断的句子。
  失败诊断也改走错误框而不是 toast —— `showToast` 会截断到 200 字,
  而被砍掉的恰好是「该加哪条策略」那半句。

### 维护

- 新增 `tests/test_error_diagnosis.py`(9 个)与
  `tests/test_launch_meta_compartment_scope.py`(3 个),后者钉死「所有元数据读取
  都必须用租户配置的 Compartment」。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.84
```

升级后请到**租户页点一次「测试连接」** —— 它会直接告诉你是哪一项没权限。

## 0.4.83 — 2026-08-24

实例侧四项:引导日志、终止保护、监控插件提示、以此为模板创建。

### 功能

- **串口控制台输出(引导日志)。** 详情页新增「引导日志」页签,一键抓取从开机到
  现在的串口输出。

  和已有的「控制台」不是一回事:那个是**交互式**串口,要配 SSH 公钥、要网络通;
  而最需要诊断的场景恰恰是**重启后机器再也没回来** —— 改坏 `/etc/fstab`、
  引导卷扩容失败,系统会停在 initramfs 提示符上,OCI 那边仍然显示 RUNNING、
  SSH 超时,在此之前面板没有任何办法看到原因。

  OCI 把它建模成资源而非调用(CaptureConsoleHistory 要从 REQUESTED 等到
  SUCCEEDED),所以是轮询式的、有超时上限。抓取前会先清理该实例上一次的记录 ——
  它们会累积并占用每实例配额,只创建不清理的话迟早撞上一个操作员看不懂的报错。

- **终止保护。** 详情页可开关;开启后终止按钮变灰,**服务端也会直接 409 拒绝**。

  闸门放在服务端、而且读的是**实例自己的标签**,不是 UI 传什么就信什么 ——
  上一轮审计查出过「页面显示 A 实例、按钮却打 B 实例」那一类缺陷,它的共同点是
  确认框问的和实际执行的不是同一个对象,再谨慎的文案也救不了。

  标记存在 OCI 的 freeform tag(`ocibot_protected`)上而不是面板本地的列:
  面板重装或数据库恢复之后它还在,而且在 Oracle 控制台里也看得见,不会变成
  只有本面板知道的事实。只认字面量 `true`,`1` / `yes` 一律不算 —— 标签可以被
  人手改,一个面板自己从不写入却会当真的值会让这个开关两个方向都不可预测。

- **监控插件被禁用时给出解释。** 监控页以前在插件被禁用时就是一片空白,面板一个字
  都不说,用户只会当成功能坏了。现在直接指出原因和开启位置。这个字段
  (`Instance.agent_config`)就在**已经拿到的** GetInstance 响应里,不产生额外调用。
  实例没返回 agent_config 时(老实例/老镜像)什么都不显示,不把「不知道」说成
  「已启用」。

- **「以此实例为模板创建」。** 详情页一键跳到创建页,带上 shape / OCPU / 内存 /
  可用域 / 引导卷大小。抢 Always Free 的人重建很频繁,以前每次都要重填一遍。
  **不产生任何新的 Oracle 调用** —— 这些值详情页早就拿在手里了。

  预填逐项校验后才落地:模板来自另一台机器,它的可用域可能不在当前租户的列表里,
  shape 可能和选中镜像的架构不兼容。宁可少填一项,也不预填出一个提交不了的组合 ——
  那比让用户自己选更糟,因为他会以为已经填好了。也**不会**顺手去加载配置:
  本页刻意不在进入时请求 Oracle(0.4.20 试过、0.4.21 回退)。

### 修复

- **通知设置页删掉了一句假承诺。** 那里写着「抢机成功/失败、**定时任务失败**、
  **预算超额**时推送」,但定时开关机和预算告警都是 0.4.36 删掉的功能,后端
  `notify.EVENT_KEYS` 只有 `("capacity",)` —— 那两类通知永远不会来。承诺一个不存在
  的告警比没有告警更糟:操作员会以为没收到就是没出事。

### 维护

- 新增 `tests/test_instance_protect_and_console.py`(19 个用例)。
- **补上了 8 个端点的 smoke 覆盖** —— 本次 4 个,以及 0.4.82 引导卷删除/改名的
  4 个(上次漏加了)。CLAUDE.md 要求新端点必须进 `test_endpoint_smoke.py`,因为
  OCI 路由普遍用 `except Exception -> 502` 兜底,一个拼错的名字只会表现为一个
  笼统的 502,单元测试碰不到。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.83
```

## 0.4.82 — 2026-08-24

存储页的引导卷从只读列表变成可操作:能删、能改名、能一眼看出哪些在白占额度。

### 功能

- **删除引导卷(仅未挂载)。** 之前引导卷页一个操作按钮都没有,而块卷早就有删除。
  这个缺口是有代价的:在 Oracle 控制台终止实例时,**「保留引导卷」是默认勾选的**,
  留下的卷会一直占着租户 200GB 的 Always Free 块存储额度。面板其实一直在数它们
  (`free_quota.summarize_storage` 的 `orphan_boot_count`,显示在免费额度面板里)——
  也就是说它会告诉你「有 3 个孤儿卷」,然后不给你任何清理的办法。

  是否仍被挂载在服务端判断,并且**读不到就拒绝**:删错的代价是抹掉一台在跑的机器
  的系统盘,所以宁可挡住也不赌。挂载中的卷前端直接不给删除按钮 —— Oracle 那边也会
  拒绝,但它的报错既不带卷名也不带实例名,面对几行几乎同名的孤儿卷根本分不清点中了哪个。

- **引导卷改名。** 孤儿卷全叫 `<已终止实例名> (Boot Volume)`,重建几次之后列表里
  就是几行几乎一样的名字,没法判断哪个能删。

- **孤儿卷汇总与筛选。** 顶部横幅直接给出「未挂载 N 个,合计 X GB,正在占用免费额度」,
  外加一个「只看未挂载」的筛选。把免费额度面板里那个抽象的数字变成一份可以逐行处理
  的清单。

### 维护

- 新增 `tests/test_boot_volume_ops.py`(14 个用例)。重点压的是删除前的挂载判断:
  包含「读不到挂载列表必须拒绝」以及一个针对 `TypeError` 的回归 —— 块卷那条路径
  上就是因为可用域参数传成了位置参数、抛出的 `TypeError` 被守卫外层的
  `except Exception` 吞掉,导致「还挂着吗」被静默答成「没有」。

### 未做(以及原因)

- **从备份恢复**:OCI 支持把备份还原成一个新引导卷,但本面板的创建实例只支持
  `InstanceSourceViaImageDetails`,没有「从引导卷启动」。单独做恢复,还原出来的卷
  启动不了,只会变成又一个孤儿卷 —— 正好是上面那个功能要清理的东西。要做就得和
  「从引导卷启动」一起做,那会动创建实例的表单、payload 白名单、配额守卫和抢机载荷。
- **挂载 / 卸载引导卷**:故障恢复场景(把坏盘挂到另一台机器上修),很少用,且容易
  把好机器一起搞坏。
- **自动备份策略**:有价值,但引入调度语义,并且会产生持续的后台 Oracle 调用,
  和本项目「不主动调 Oracle」的原则需要单独权衡。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.82
```

## 0.4.81 — 2026-08-23

12 个并行 agent 的成果合并。**安全与可靠性大批量加固**：容器不再以 root 运行、
面板内更新改为显式开启、密钥不再能用弱口令导出、WebSSH 不再能被单用户打爆内存。

### 安全

- **容器降权 + 更新能力改为 opt-in。** `docker.sock` 从基础 compose 里彻底移除，
  新增 `docker-compose.update.yml` 把「socket + 可写仓库 + `OCIBOT_UPDATE_ENABLED=1`」
  绑成一个整体 —— 路由开关和它守护的东西现在住同一个文件，不可能一开一关。
  基础文件写死 `"0"`（不是 `${...:-0}`），**结构上无法**被环境变量打开。
  `api` / `worker` 改跑 uid 10001、`cap_drop: ALL`、`read_only`、`/tmp` 用 tmpfs。

  之前的实际信任边界是「**任何**能在 API 进程里执行代码的东西 == 宿主机 root」，
  而不是文档说的「管理员失陷 == 宿主机失陷」：仓库（含 `web/.env`，里面有主密钥）
  和 socket 都是无条件挂载的，`OCIBOT_UPDATE_ENABLED` 只挡路由不挡挂载。

- **`install.sh` 不再偷偷把面板内更新打开。** 原来 `export_build_env()` 每个子命令
  都跑 `export OCIBOT_UPDATE_ENABLED="${...:-1}"`，而 compose 解析 shell 环境变量
  **优先于** `.env` 文件 —— 操作员按 README 把它设成 0，下一次 `install.sh update`
  就给改回 1，且无任何提示。README 和安全清单教的那个开关一直是失效的。
  新增 `install.sh update-on` / `update-off` 显式切换。

- **不再有能跑起来的默认密钥。** `OCIBOT_REQUIRE_SECURE_SECRETS` 默认改为 **1**，
  `.env.example` 的 `POSTGRES_PASSWORD` / `OCIBOT_MASTER_KEY` / `OCIBOT_JWT_SECRET`
  全部留空。主密钥只过一次无盐 SHA-256 就成为 Fernet 密钥（这是刻意设计），
  所以跑在仓库里那个公开默认值上 == 任何能读到数据库的人都能解出全部租户私钥。

  **这是一次会影响存量部署的破坏性变更**，见下方「升级」。

- **备份导出不再只靠 zip 口令。** pyzipper 用的 WinZip AES 为了互通，KDF 被规范
  钉死在 PBKDF2-HMAC-**SHA1、1000 轮**，无法调高；而归档里装的是每个租户的
  **明文 OCI 私钥**。现在 zip 里面再套一层信封：同一口令过 PBKDF2-HMAC-SHA256
  390 000 轮（与本地主密码同参数）后用 Fernet 封住整个 `tenants.json`。
  口令下限提到 12 位并说明这份文件等同于账号本身。**旧归档仍可导入**（信封带版本号）。

- **私钥现在会被真正解析。** 以前只 grep `BEGIN` + `PRIVATE KEY`，于是一份
  带口令保护的密钥（甲骨文控制台就会给你这种）或截断的粘贴会以 201 存下、
  `has_private_key: true`，而「保存后自动测试连接」默认关闭，操作员看到「已添加」，
  之后每个页面都 502 报一句看不懂的 SDK 反序列化错误。

- **WebSSH**：stdin 加 `drain()`（原先单个用户可让 asyncssh 的发送缓冲无限增长，
  把共享的 API 进程 OOM 掉）；会话改为字节透传（原先 `errors='strict'`，
  `cat /bin/ls` 就会**整条 SSH 连接**断掉，而且浏览器那边看起来还连着）；
  终端数据走二进制帧、控制消息走文本帧，于是**客体输出再也无法伪装成面板自己的
  控制消息**去诱导用户点「重置主机密钥」；主机密钥按密钥类型 pin，
  避免一次依赖升级把所有实例同时报成「可能是中间人攻击」。

- **通知**：SMTP 不再在服务器不宣告 STARTTLS 时静默降级明文发送凭据；
  SMTP 端口套用与 HTTP 相同的黑名单（原先可当端口扫描器，且远端 banner 会回显）；
  出站响应加大小上限与总超时（原先 15s 是**每块**的，慢速滴流可无限占住线程）。

- **自更新**：互斥锁原先在真正执行之前就写 `state=success`，第二次点更新的第一个
  动作是 `docker rm -f` 掉正在 `compose build` 的特权容器 —— 可能让 API 容器被销毁
  且不再重建。现在保持 `running` 直到 helper 容器退出，并按容器名二次拦截。
  `last_error` / `message` 也接入脱敏（原先只有 `log_tail` 走了，私库 PAT 会明文落库）。

- CSP `connect-src` 收紧为 `'self'`（原先裸 `ws:` / `wss:` 允许连任意主机，
  是这份策略里唯一的外泄通道）；`/openapi.json`、`/docs`、`/redoc` 默认关闭；
  请求体大小限制移到中间件最外层。

### 修复

- **配额守卫的读取失败不再比正常路径更宽松。** 判断「免费上限是硬阻断还是只警告」
  的规则被抄成了两份，`_blocked_by_incomplete_read` 只看 `free_only_mode`。于是
  一个 `account_tier="free"` 但关掉了 free_only 的租户，用量**读得到**时被硬挡、
  读**失败**（429）时反而放行。现在两边共用 `free_quota.hard_free_caps()`。

- **创建实例加了每租户锁。** 配额校验是 check-then-act，不预留任何东西：两个标签页
  同时提交 4 OCPU/24GB 的 A1，各自读到「已用 0」，双双放行，真的开出 8 OCPU/48GB。
  窗口还特别宽 —— `prepare_launch_network` 夹在判定和创建之间，可能新建 NSG 甚至 VCN。
  Postgres 用 `pg_try_advisory_xact_lock`（独立连接，因为请求 Session 中途会 commit），
  SQLite 用数据库文件旁边的 `flock`。

- **`list_compartments` 失败不再被静默当成「只有根 compartment」。** 缺
  `inspect compartments` 权限时（甲骨文回 404），子 compartment 里的用量永远算 0
  且 `read_incomplete=False`，配额守卫把这个欠计当成权威值。现在有了错误通道。

- **抢机任务的时间下限。** `resume` 直接把 `next_run_at` 设为 now 且无下限，
  而 worker 也没有独立下限 —— 反复点「停止/继续」可把 LaunchInstance 打到约 12 次/分，
  绕过 60 秒的合规下限。两侧都补了。

- **审计日志时间戳补上时区偏移。** SQLite 对 `DateTime(timezone=True)` 返回的是
  naive 值，`.isoformat()` 就没有偏移量，而 JS 把无偏移的串按**本地时间**解析 ——
  UTC+8 的操作员看到的每条事件都早 8 小时，偏偏这是「什么时候发生的」专用页面。
  `schemas.py` 里的 `UtcDatetime` 早就修过同样的问题，三个手写 dict 的端点漏了。

- **`GET .../console` 读取失败不再报 `ok:true, connections:[]`**（被读成「没有连接」，
  而实例只允许一个活动控制台连接，导致后续创建失败且看不出原因）。

- **前端竞态一批**：`InstanceDetailView` 换实例时显示旧实例、按钮打新实例
  （`doRename` 同一函数内读旧写新，会把 B 改成 A 的名字；`power` 无确认框；
  终止 / 防火墙全开 / 换 IP 的确认框不带实例名）；`TenantsView` 副区列表与租户错配，
  可能在**没选中**的账号上创建 Oracle 侧**无法取消**的区域订阅，而确认框写的是
  「添加为面板租户」；`LaunchView` 切租户时新租户根本不发请求，拿旧租户的额度做决策；
  `AccountView` 切租户后「刷新用量」永久卡死；`StorageView` 可用域跨租户残留；
  `SettingsView` 切换渠道类型会把上一个渠道的 token 存进无关渠道；
  `JobsView` 同时显示「已删除」和「删除失败」。

- **数据库迁移失败不再静默启动。** `_ensure_schema` 把所有 `ALTER` 放在一个事务里，
  Postgres 上一条失败会中止整个事务，后续每条都 `InFailedSqlTransaction`，
  最终隐式 COMMIT 退化成 ROLLBACK —— **同一轮里先加成功的列被一起丢掉**，
  而 `init_db()` 照常返回成功。

- 长度上限：`display_name` / `CapacityJobCreate.name` / `availability_domains`
  以前无上限，在 Postgres 上是裸 500，而此前 `prepare_launch_network` 已经在
  甲骨文侧建好了 NSG（孤儿资源，且每次重试再建一个）。

### 维护

- **供应链完整锁定。** 新增 `web/backend/requirements.lock`：49 个包的全量闭包
  （含 starlette / pydantic-core / cffi / certifi / uvloop 等间接依赖），
  每个都带 sha256，交叉解析到 linux/cp312，Dockerfile 改用 `--require-hashes` 安装。
  `compose build --pull` 从此不再是「向 PyPI 重新解析一遍」，而是「装回同一批字节」。
  `requirements.txt` 保留为人类维护的直接依赖清单，改完必须重新生成锁。
- **新增 `.dockerignore`。** 构建上下文原先会把 `.venv/`、`.git/`、`web_data/` 和
  **`web/.env`（主密钥、JWT 密钥、数据库口令）**整个发给 Docker daemon；在远程或
  rootless daemon 上这就是一次实打实的密钥外泄。还有一处更隐蔽的：frontend 阶段
  先 `npm ci` 再 `COPY web/frontend ./`，宿主机上若存在 `node_modules` 会**盖掉**
  刚装好的那份，lockfile 的意义随之消失。
- `web/AUDIT.md` 新增第 11 次审计记录，并订正了第 10 次的一处错误结论
  （当时写「JWT 算法硬编码、不可被环境变量覆盖」，实际是可以的）。
- 基础镜像改用 `ARG` + 可从 `web/.env` 注入 digest；未校验的 `docker-27.5.1.tgz`
  下载改成从官方 `docker:27-cli` 镜像 `COPY --from=`（内容寻址）。
- `npm ci` 去掉了 `|| npm install` 回退（lockfile 一旦漂移就会静默改用无校验的解析）。
- `app_version` / `jwt_algorithm` 改为 `ClassVar`，任何环境变量都碰不到它们。
  原先 `APP_VERSION=9.9.9` 能让 `/api/health` 谎报版本 —— 正是版本号规则要防的事。
- 测试夹具改用**真实生成**的 RSA 密钥（`tests/_keys.py`，import 时生成一次），
  而不是放宽私钥校验来迁就假 PEM。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.81
```

**本次有三处需要留意：**

1. **必须先有合格的密钥，否则面板会拒绝启动**（返回 503 并在正文里写明改哪个变量、
   用什么命令生成、以及逃生开关）。已经在跑的部署，`web/.env` 里本来就有强密钥的不受影响。
   若确实想暂时维持原样：`OCIBOT_REQUIRE_SECURE_SECRETS=0`。
   注意**轮换 `OCIBOT_MASTER_KEY` 会让已存的租户私钥无法解密**，需重新导入或恢复备份。
2. **面板内更新会在本次升级后关闭**，需要显式 `bash scripts/install.sh update-on` 再开。
3. **`POSTGRES_PASSWORD` 变成必填**，不再静默回落到 `ocibot_dev_pass`。

升级后建议手工验一次 WebSSH（CSP 收紧 + 会话改字节透传都影响这条路径）。

## 0.4.80 — 2026-08-23

12 个 agent 并行审计的第一批修复。**两个可造成真实损失的严重问题**：
任何登录用户可让面板连接内网任意 host:port；批量创建 E2.1.Micro 完全绕过免费额度。

### 修复

- **[严重] `region` 未做校验，可把 OCI SDK 的请求引向任意主机（SSRF）。**

  OCI SDK 拼 endpoint 时有一条向后兼容分支：region 里只要带 `.`，就当成完整域名、
  不再追加 `oraclecloud.com`。而 `TenantConfig.validate()` 以前只检查「非空」，
  `oci.config.validate_config` 的 PATTERNS 也只覆盖 fingerprint / tenancy / user。
  实测（`tests/test_region_ssrf.py` 用真实 SDK 断言）：

      region='@127.0.0.1:6379'  -> 实际连接 127.0.0.1:6379
      region='@169.254.169.254' -> 实际连接云元数据端点

  `iaas.` 被当作 URL userinfo 吃掉，真正的 authority 是 `@` 后面那截。这条路径
  **完全不经过 `url_safety.py`**，所有地址/端口/IDNA 控制都不生效；再配合
  `POST /tenants/{id}/test` 同步回显连接结果，就是一个内网端口探测器。

  改法：`TenantConfig.validate()` 增加字符集校验（只允许字母、数字、连字符），
  Pydantic 的 `TenantCreate` / `TenantUpdate` 也加上同样的 pattern 提前拦成 422。
  禁掉 `.` `@` `:` `/` 之后，上述每一种构造都不成立。`subscribe_tenant_region`
  是唯一不过这两道闸就能写 `Tenant.region` 的路径，也补了同样的校验。

- **[严重] 批量创建 E2.1.Micro 时 `count` 被忽略，免费额度形同虚设。**

  `validate_launch_against_quota` 里 `units` 已按 `count` 算好，A1 和块存储两条
  分支都用的批量值，唯独 E2 分支写的是 `if e2_rem < 1` —— 只问「还有没有空位」，
  不问「够不够 n 台」。于是免费额度只剩 1 个位置时，`count=3` 照样全过并真的发出
  3 次 LaunchInstance。讽刺的是 `projected` 里算出的 `e2_micro_count_after` 是对的
  （4 > 上限 2），只是没人拿它做判断。默认 47GB 的块存储上限只在 `count>4` 时
  才顺带挡一下，`count=2..4` 一路畅通。现在比较整批台数。

- **[严重] 一个坏租户会让全站所有用户的抢机任务永久停摆。**

  `worker.tick_capacity` 每个任务的 `try/finally` **没有 `except`**。建 OCI 会话
  那两行不在任何 try 里，于是一个配置被 SDK 拒绝的租户抛出的异常会一路逃出
  `for` 循环。抛点在 `attempts += 1` 和任何 `next_run_at` 写入之前，而候选是按
  `next_run_at.nullsfirst()` 排序的 —— 它会在一个周期内变成永久队头，**其他用户的
  任务再也不会被执行**；`attempts` 不涨所以 `max_attempts` 永远够不着；
  `last_error` 没写、`status` 卡在 `running`，面板显示「运行中」且毫无错误。
  删除任务时 worker 恰好在跑同一个任务（`StaleDataError`）也走同一条逃逸路径。

- **[严重] 副区管理：区域列表与租户错配，可能在**没选中**的账号上创建不可逆的区域订阅。**

  `openRegions` 没有序号守卫。先点 A 再点 B、A 的响应后到，就会「标题是 B、
  表格是 A 的区域列表」，而开通请求是打给 `regionsFor`（B）的。服务端
  `subscribe_region` 拿 **B 的真实已开通列表**重新判断，A 那边「已开通」的状态
  不作数 —— 于是那句「把已开通区域…添加为面板租户」（承诺不动 Oracle）的确认框，
  实际会在 B 上创建一个 **Oracle 侧无法取消**的区域订阅。「副区管理」按钮的
  `:disabled` 用的是 `busy`，而 `openRegions` 设的是 `regionsLoading`，
  所以加载期间按钮一直可点。

- **[高] 粘贴 OCI 配置存在二次方级 ReDoS，十几个并发即可挂死面板。**

  INI 探测用的是 `re.search(r"^\s*\[.+\]\s*$", raw, re.M)`。`\s` 含 `
`，
  加上 `re.M` 后引擎在每个行首重新起跳并回溯到串尾，对空行数是 O(n²)。
  `api_text` 上限 64 000 字符恰好是最坏情况：实测 **13.3 秒**纯 CPU 且不放 GIL。
  `/tenants/parse` 与 `/tenants/import` 都没有限流。改为逐行判断，天然线性。

- **[高] 抢机重试是唯一会重发 LaunchInstance 的路径，却唯独不带 `opc-retry-token`。**

  lease 在调用 OCI 前 commit，而 `attempts+=1` 和结果要到 `finally` 才 commit。
  容器在两者之间重启（面板自带的一键更新、OOM、宿主机重启）会回滚这两者，
  租约过期后以**完全相同**的 payload 再发一次 —— 多出一台没人记账的机器。
  现在用 `derive_retry_token(launch_token, attempts)`：同一次尝试的重放被 Oracle
  折叠，下一次尝试拿到不同 token，不会撞 IdempotentParameterMismatch。

- **[高] 账号用量页：加载途中切换租户会让「刷新用量」永久卡在「加载中…」。**

  `beginLoad().stale()` 里含租户判断，而 `finally` 拿它来决定要不要关 spinner；
  切换租户后这一次不满足条件、不关，又没有新的 loadAll 来接管所有权。
  `InstancesView` 早就修过同一个坑，注释写着「spinner 只认 SEQUENCE」。
  现在把「能不能写 state」和「能不能关 spinner」拆成两个判断。

- **[中] `PATCH /tenants/{id}` 与粘贴导入的字符串没有长度上限。**

  `TenantUpdate` 全是裸 `Optional[str]`，而 `TenantCreate` 每个字段都有 bound。
  SQLite 不检查 varchar 宽度所以测试全绿，PostgreSQL 会抛
  `StringDataRightTruncation`，且 `update_tenant` 的 commit 没有包 try ——
  PATCH 一个 200 字符的 name 就是一个不带任何信息的 500。已对齐列宽。

### 测试

新增 4 个回归测试文件、38 个用例（`609 passed`）：
`test_region_ssrf.py`（含用真实 OCI SDK 断言「攻击在修复前确实成立」）、
`test_worker_job_crash_isolation.py`、`test_free_quota_batch_e2.py`、
`test_parse_config_redos.py`（断言复杂度而非墙钟时间，避免 CI 抖动）。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.80
```

## 0.4.79 — 2026-08-23

实例列表瘦身：去掉「租户」列和「换IP」按钮，并且**一台机器一行**——
不再有某几行因为自己的参数比别人高一截。

### 功能
- **去掉「租户」列。** 这张表一次只列一个租户（就是顶部下拉里选的那个），
  每行重复同一个名字既占宽度又没有信息量。全租户聚合的 `GET /api/instances`
  是故意返回 400 的，所以不存在「列表里混着几个租户」的情况。
  租户名仍然参与搜索、仍然写进导出的 CSV。

- **去掉行内的「换IP」按钮。** 换公网 IP 会释放旧地址，是不可撤销的，
  夹在一排小按钮中间太容易点成隔壁的「重启」。详情页的「换公网IP」保留，
  那里同时能看到当前地址、IPv6 和实例状态，是做这个决定该有的上下文。

- **一行一台机器。** 表格是用来「扫」的：行高一致时，眼睛沿着一列往下走
  就能比出状态、IP、创建时间；只要有一行因为自己的参数变高，节奏就断了，
  而且高出来的那几行看着像是更重要。现在宽度不够一律横向滚动，绝不折行：

  - **规格（OCPU / 内存）从 shape 下面挪到同一行。** 之前它是个 `<div>`，
    于是弹性 shape 的行比固定 shape 的行高一截 —— 同一张表里两种行高。
  - **IPv6 从 IPv4 下面挪到同一行**，并缩写成「首段:…:末段」。完整地址最长
    39 个字符，铺开会把「操作」列顶出屏幕；前缀认子网、后缀认机器，这两段
    足够在列表里区分同一台机器的多个地址。**悬停看完整地址，单击复制的也是
    完整地址**，一个字符都不丢。
  - **长名字截断成省略号**（完整值在 title 里）。名称是唯一长度完全由用户
    决定的字段，一台机器起个长名字就能把整行撑高。
  - **按钮组不再折行。** flex 的换行不受 `white-space` 管，得单独关掉，
    否则窄屏下按钮折成两排，正是要避免的那种「某几行特别高」。

  实测（真实样式 + 五行极端数据：三个 IPv6、60 字符长名、固定/弹性 shape
  混排）：改之前在 1130px 可用宽度下每行 262px，改之后**每行 52px，
  从 420px 到 1592px 各种宽度下行高差都只有 0.5px 的亚像素**。

- **「操作」列吸附在表格右边缘。** 不折行的代价是表格变宽（真实机器数据约
  1420px，极端数据约 1760px）。1440 的笔记本可用宽度只有 1130 左右，
  最右边这一列会被推出可视区 —— 偏偏它就是要点的那一列。吸附之后横向滚动
  只影响中间那几个只读字段，按钮始终停在原地；表格宽度够时它就是个空操作。

  两个坑都踩过了：背景必须**不透明**（否则滚到下面的单元格会透上来），
  分隔线用 `inset box-shadow` 而不是 `border`（表格是 `border-collapse:
  collapse`，合并后的边框由 `<table>` 画，不会跟着吸附的格子走）。
  用 `.action-cell` 而不是 `:last-child`，因为空列表那行的 `colspan`
  单元格也是它那行的最后一格，不该被吸附。

### 修复
- **「没有公网 IP」的那个「—」占位符会把整行撑到两倍高。**

  `styles.css` 里给「暂无数据」整格提示用的是 `.card > .empty, td.empty,
  .empty`，**第三个是裸类选择器**，于是它也命中了 `.copyable.empty` ——
  也就是 IP 为空时显示的那个破折号，给它套上了 `padding: 2.25rem 1rem`。
  `.copyable.empty` 只声明了 `padding-left/right: 0`，上下就留给它了：
  一个破折号被撑成 **87px 高**，那一行直接变成别人的两倍。

  这个问题在改动之前就存在（实例详情页的 IP 字段同样中招），只是以前
  列表本来就在折行、行高本来就参差，没人看得出来是它。改成一行一台机器
  之后它立刻变成最显眼的破绽，所以一并修掉：`.copyable.empty` 的 padding
  写成四值 shorthand，靠 (0,2,0) 的 specificity 盖住 `.empty` 的 (0,1,0)。

### 维护
- **顺带发现（本次未改）：`:global(html[data-theme='dark']) .foo` 这种写法
  在 Vue 的 scoped CSS 里会被编译坏** —— 后代部分整个丢掉，只剩
  `html[data-theme=dark]{…}`，样式糊到 `<html>` 上而不是目标元素。
  本次新加的深色规则改成了直接写祖先选择器 `html[data-theme='dark'] .foo`
  （作用元素本来就在组件内，scope 属性会挂到最后一段），编译结果正确。

  `AccountView` / `TenantsView` / `ToastHost` 里还有 **8 处**老代码是坏的写法，
  也就是说那 8 条深色样式一直没生效（因为 `body` 自己的 color/background
  盖住了糊到 `<html>` 上的值，所以一直没人发现）。改掉它们会真的改变这三处
  在深色下的观感，需要单独看一遍再动，本次不顺手带。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.79
```

## 0.4.78 — 2026-08-20

实例列表新增「创建时间」列，精确到秒。

### 功能
- **实例列表多一列「创建时间」**，位置在「私网 IP」和「root 密码」之间，
  格式是本地时区的 `YYYY-MM-DD HH:mm:ss`。

  刻意不用 `toLocaleString()`：它的输出随浏览器语言变（`2026/8/20 14:03:07`
  还是 `8/20/2026, 2:03:07 PM`），而这一列的用途就是和 Oracle 控制台、
  和导出的 CSV 逐字对照，格式必须固定。等宽字体 + 不换行，让同一列的数字对齐，
  扫一眼就能比出哪台先建。

  鼠标悬停显示后端返回的原始时间戳（带时区偏移的 UTC 串），核对用。

- **导出 CSV 增加 `time_created` 字段**，值与列表里显示的一致（本地时区、
  精确到秒），而不是原始 UTC 串 —— 导出是这张表的副本，两者对不上更容易误判。

### 修复
- **`GET /tenants/{id}/instances` 返回的 `time_created` 改为标准 ISO 8601。**

  之前是 `str(datetime)`，得到 `2026-01-01 00:00:00+00:00` —— 空格分隔、
  带时区偏移，这种串**不在 `Date()` 必须支持的格式里**：Chrome 能解析，
  Safari 会得到 `Invalid Date`。字段本来一直存在但没人展示，所以从没暴露过。
  现在与块存储 / 对象存储那几处一样走 `_ts_iso()`（`isoformat()`），
  整个 API 的时间戳格式统一。前端另外兜了一层空格 → `T` 的兼容，
  以防旧版本 API 的缓存响应。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.78
```

## 0.4.77 — 2026-08-20

第一次点「加载配置」必报错的真正原因：**缓存的 key 超过了数据库列宽，
两个版本以来它一行都没写进去。**

### 修复
- **`app_meta.key` 是 `VARCHAR(64)`，而写进去的 key 有 139 / 146 个字符。**

  你反馈：账户第一次进创建实例页面，点「加载配置」就报错。0.4.75 把它改成
  「后台跑 + 轮询」本该解决这个问题，但没有 —— 因为轮询的状态**根本没存下来**。

  key 的组成是 `前缀 + 租户 uuid(36) + 区域 + 租户 OCID(约 79)`，合计 127 个字符
  再加前缀。**PostgreSQL（生产用的就是它）对超长的 varchar 是直接报错，不是截断**；
  而这两个写入点都刻意把异常吞掉（缓存不该弄坏一个请求），于是：

  - `POST /launch-meta/refresh` 写「running」→ **写失败，无声**；
  - 2 秒后前端来轮询 → 查不到状态行、也查不到结果 → 判定为 `idle`
    → 页面报 **加载未完成，请重试**；
  - 而此时后台那次抓取**正常地在跑**，一分钟后成功填进了进程内缓存 ——
    所以「再点一次就好」，让人以为是偶发。

  同一个原因还让 0.4.73 的**跨 worker 共享缓存自始至终是空的**：
  `OCIBOT_API_WORKERS=2`，另一个 worker 永远读不到别人抓好的结果，
  创建请求里仍可能现场跑一次冷抓取 —— 那个「偶发 520、重试就好」也是它。

  改法是让 key 一定装得下（`_row_key()`）：**租户 id 保持明文在前**（
  按租户清理缓存用的是 `LIKE '<前缀><tenant_id>|%'`，不能哈希掉），
  只把没有长度上限的部分（区域 + 分区/租户 OCID）哈希成 16 位，
  前缀也缩短。最长 60 字符，**结构上不可能再越过 64**。

  为什么以前测不出来：所有测试跑在 SQLite 上，**SQLite 不检查 varchar 长度**，
  而且桩数据的租户 OCID 只有十几个字符。

- **不再静默。** 这两处写入失败以前一个是 `pass`、一个只在外层记日志（内层
  commit 失败被 rollback 吃掉了），所以两个版本里日志干净、功能全废。
  现在两处都会打 `WARNING` 并写明后果。

- **轮询不再因为一次 `idle` 就判死。** 连续 3 次（约 6 秒）才报错。
  `idle` 本意是「这次运行已经死了」，但它同时也是「状态行丢了一次」的样子，
  用一次回复就把页面判错，代价太大而收益为零。

- **共享缓存的体积上限 512 KB → 4 MB。** 超限时是**静默降级成「只缓存在本进程」**，
  也就是又一次把跨 worker 缓存悄悄关掉；镜像很多的租户本来就可能接近 512 KB。
  现在超限会记日志，阈值也远离了任何现实文档的大小。

- 按租户清理缓存时**连状态行一起删**（以前只删文档），并顺手清掉 0.4.77 之前
  那种超长格式的遗留行（只有 SQLite 安装里才可能存在，且已无人读取）。

### 维护
`tests/test_launch_meta_key_width.py` 9 例。关键点是它**用 ORM 事件模拟了
PostgreSQL 的列宽检查**，否则在 SQLite 上无论 key 多长都会通过 ——
这正是同一个问题连发两次的原因。**已验证把 key 改回原样时其中 6 例会失败**：
包括「轮询在抓取进行中必须报 running 而不是 idle」这条端到端断言。

无需数据库迁移：`_ensure_schema()` 只能加列、不改列宽，所以修的是 key 的长度，
而不是列的宽度 —— 已部署的库不用动。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.77
```

## 0.4.76 — 2026-08-16

新增：取消实例上的公网 IPv6。

### 功能
- **实例详情页新增「取消 IPv6」**（`DELETE /tenants/{id}/instances/{iid}/ipv6`）。
  删除该实例主 VNIC 上的 IPv6 地址。

  按钮**只在该实例确实有 IPv6 时才出现** —— 一个永远回答「该实例没有 IPv6」的
  按钮，在其余每台机器上都只是噪音。

  **刻意不做对称的回滚。** 分配 IPv6 时可能顺带给 VCN 开了 GUA 前缀、给子网加了
  /64、并添加了 `::/0` 路由 —— 这些是**整个子网共享的**资源。因为一台实例取消
  IPv6 就把它们拆掉，会让同子网的其他实例一起失去 IPv6。撤销网络层的启用属于
  VCN 级别的决定，不是这个按钮该做的事。消息里也写明了这一点。

  其他行为：
  - **幂等**：没有 IPv6 时返回成功而不是报错，连点两次不会出现吓人的提示。
  - **部分失败如实报告**：删掉一个、另一个失败时返回 `ok=false` 并列出仍然存在的
    地址。报成功却还挂着地址，会让人以为已经关掉了。
  - 操作写入审计日志（记录结果，不只是意图）。
  - 前端二次确认，并说明**重新分配会得到不同的地址** ——
    指向旧地址的 DNS / 防火墙规则会失效。

### 维护
`tests/test_ipv6_remove.py` 6 例：全部删除、无 IPv6 时幂等成功、
不触碰子网/VCN 的共享资源、部分失败与全部失败的报告、找不到 VNIC 时报错。
新接口已纳入 smoke 测试。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.76
```

## 0.4.75 — 2026-08-16

「加载配置」改为后台执行 + 轮询。**不再依赖任何代理的超时上限。**

### 修复
- **不再试图「跑得够快」，而是让请求不再长。**

  你反馈：**挂 Cloudflare 就报错，不挂就正常**。这说明源站本身能跑完，
  只是耗时越过了 Cloudflare 免费版对单个请求固定的 100 秒上限。

  0.4.74 把六次 Oracle 读取改成并发，有帮助，但**仍然不够** ——
  因为耗时取决于你 Oracle 账号里有多少分区、多少镜像、要不要新建 VCN 并等它就绪，
  这些都不是本项目能约束的量。**在一个固定上限下承诺「一定跑得完」做不到。**

  所以改掉的是结构，不是速度：
  - `POST /tenants/{id}/launch-meta/refresh` —— 启动后台加载，**立即返回**
  - `GET  /tenants/{id}/launch-meta/status` —— 轮询：ready / running / error / idle

  前端点「加载配置」后每 2 秒问一次，直到就绪。**任何单个请求都是毫秒级的**，
  代理超时不超时都与它无关。这和面板里容量重试任务用的是同一套形态。

  几个刻意的设计：
  - **轮询绝不调用 Oracle**，只读缓存 —— 否则页面开着就会持续消耗速率额度。
  - **重复点击会汇入同一次运行**，不会并发跑两遍 Oracle 调用。
  - 状态存在数据库而非进程内存：`POST` 和轮询有大约一半概率落在不同的 worker 进程。
  - 15 分钟无进展的运行视为已死，不会让页面一直等一个已经重启掉的进程。

  同步的 `GET /launch-meta` 保留不动，非 UI 调用者不受影响。

### 维护
`tests/test_launch_meta_async.py` 7 例：启动不阻塞、轮询最终就绪且内容正确、
重复点击只跑一次、失败会报错而不是永远轮询、轮询不产生 Oracle 调用、
已缓存时非强制启动直接返回、未运行过时为 idle。两个新接口已纳入 smoke 测试。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.75
```

升级后 Cloudflare 可以保持开启，不需要再切「仅 DNS」。

## 0.4.74 — 2026-08-16

「加载配置」也报网关错误 —— 这条线索直接指出了超时的位置。

### 修复
- **「加载配置」的六次 Oracle 读取是串行的，总耗时是它们的和。**

  你反馈点「加载配置」同样报 Cloudflare 错误，这比之前的信息精确得多：
  超时发生在 `launch-meta`，不在创建请求里。

  该接口要做六件互不依赖的事 —— 列分区、列可用域、列 shape、列自定义镜像、
  按 OS 家族各列一次镜像、以及扫描默认网络（它自己还要逐个分区遍历 VCN 和子网）。
  每一项都是分页的网络往返，**串行执行时墙钟时间是它们相加**，
  在稍大的租户上就会越过代理对单个请求的 100 秒上限。

  这也解释了「再点一次就好」：第一次虽然在代理侧被切断，
  服务端仍在继续跑并把结果写进了缓存，第二次直接命中缓存。

  改为并发执行（有界线程池，6 个 worker）。**调用次数不变**，
  所以不会多消耗租户的速率额度，只是不再排队等待。
  同样的写法在 `list_instances_tree` 和 IP 解析里本来就在用。

  错误行为逐条保留：原先被单独 try 包住的（按家族列镜像、自定义镜像）仍然降级为空；
  原先会向上抛的（列分区、列可用域、列 shape、网络准备）仍然照抛 ——
  `Future.result()` 会重新抛出，这一点有专门的测试盯着，
  因为 future 在调用 `.result()` 之前是会静默吞掉异常的。

### 维护
`tests/test_launch_meta_parallel.py` 7 例：读取确实重叠、墙钟远低于串行总和、
返回内容逐字段不变、以及上面那几条错误行为。**已验证把并发改回 1 个 worker 时
前两条会失败**，不是摆设。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.74
```

升级后请再点一次「加载配置」。若仍然超时，`launch timing` 日志（0.4.72 起）
会指出是哪一段，这次是有数据可查的。

## 0.4.73 — 2026-08-16

创建实例相关的全量排查。两个真 bug。

### 修复
- **网关错误（520/524 等）只显示一句「Request failed with status code 520」。**
  Cloudflare 超时返回的是 **HTML** 而不是 JSON，前端拦截器取不到 `detail`，
  于是只剩 axios 的默认文案。而 `doLaunch` 里那条「可能已创建成功、不要盲目重试」
  的提示**只在没有状态码的客户端超时时才显示** —— 你实际遇到的这一类（带状态码的
  网关错误）恰好落到了通用分支。

  结果就是：最需要解释的那种失败，给出的信息最少，还正好诱导你再点一次。

  现在 408 / 502 / 503 / 504 / 520 / 521 / 522 / 523 / 524 一律按「响应丢了，
  不是请求被拒」处理，并明确告诉你：**只要没改配置，再点一次是安全的** ——
  面板会用同一个幂等标识提交，Oracle 不会重复创建；改了配置再提交则算全新创建，
  要先去实例页确认上一次的结果。

  （顺带：`.error-box` 不保留换行，这段多行说明会被压成一行，已加 `pre-line`。）

- **批量创建时只显示第一台的 root 密码。** 批量创建会给第一台之后的每台**单独生成**
  密码，服务端在 `instances` 里全部返回了 —— 但前端那块「请立即保存 root 密码」
  的面板是从标量 `root_password` 取值的，那只是第一台的。创建 3 台密码模式的机器，
  这个屏幕只交给你 1 个密码。

  不是永久丢失（每台的密码都写在自己的 OCI 标签里，实例列表能看到），
  但那块面板存在的唯一目的就是把密码交给你，它只交了三分之一。
  现在逐台列出，各自带复制按钮，并说明之后可在实例列表查看。

### 复查无发现
配额校验在网络准备**之前**执行，所以被拒时不会留下孤儿 NSG；
批量中途失败会保留已创建实例所用的 NSG（正确），容量失败时刻意保留以便重试；
副区 / 免费额度互斥逻辑与既有测试一致。

### 仍待数据
0.4.72 的分阶段耗时日志还没等到复现。创建流程的超时根因仍未定位，
本版**没有**改动创建流程的时序 —— 在看到 `launch timing` 之前不动它。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.73
```

## 0.4.72 — 2026-08-06

给创建实例加上分阶段耗时日志。**这一版不修 bug，是为了先拿到数据。**

### 说明
创建实例仍偶发失败、重试即成功，且报错来自 Cloudflare —— 说明 0.4.69 修的
「启动配置缓存按进程」是真实机制，但**不是全部原因**：请求仍会跑过 Cloudflare
免费版 100 秒的硬上限。

我已经两次靠推断定位、两次没修干净。这次不再猜：每次创建都会打印一行分阶段耗时，
成功失败都打。

```
launch timing outcome=created total=48.3s session=0.1s meta=0.2s build=0.0s
  pre=0.1s quota=31.7s network=14.9s launch=1.3s
```

| 阶段 | 做什么 |
|---|---|
| `session` | 建立 OCI 客户端 |
| `meta` | 启动配置（0.4.69 起跨进程缓存） |
| `quota` | **免费额度校验** —— 要列出所有分区的实例、引导卷、块卷，未缓存 |
| `network` | **准备网络 / 创建 NSG** —— 对 Oracle 的写操作 |
| `launch` | LaunchInstance 本身 |

总耗时超过 60 秒记为 WARNING —— 那已经贴近代理上限，下一次稍慢一点就会失败。

复现一次后执行：

```bash
cd ~/ocibot && docker compose logs api | grep "launch timing" | tail -5
```

有了这一行就能直接看出该优化哪一段。在拿到之前我不会再改创建流程 ——
`quota` 那段涉及免费额度守卫，为了赶时间给它加缓存会牺牲「不超额」这个保证，
不看数据就动它是不负责任的。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.72
```

## 0.4.71 — 2026-08-06

代码核对。一个真 bug：批量创建的幂等 token 会撞车。

### 修复
- **批量创建时，足够长的幂等 key 会让所有实例拿到同一个 token。**
  路由用 f-string 拼 `f"{key}-{index}"`，而 Oracle 把 token 截到 64 字符 ——
  客户端如果送来一个 64 字符的 key（**schema 明确允许**），那条 `-0`、`-1` 后缀
  正好被截掉，一批 8 台全部使用同一个 token。

  后果正是我自己在 0.4.69 注释里警告过的那一种：**Oracle 创建第一台，然后把它重复
  返回 7 次 —— 页面显示创建了 8 台，实际只有 1 台。**

  当时的测试用的是短 key，所以没测出来。现在改用 `derive_retry_token()`：
  先给后缀预留位置再截断，**让区分用的那部分永远不会被截掉**。
  测试改为在 key 长度 1 / 32 / 62 / 63 / 64 / 200 各跑一遍，每次都要求 8 个 token 互不相同。

- **密码备注的审计日志记的是意图不是结果。** Oracle 拒绝时也照样写「set」，
  等于日志断言了一次没发生的修改。现在按本项目其他路由的惯例记录 `ok` 与 `message`。

### 复查无发现
共享缓存的浅拷贝安全（确认没有任何地方写 `meta`）；抢机任务重复提交由「每租户
一个活动任务」的守卫拦住，不需要幂等 token；`month_to_date` 的窗口取并集在
`days=90` 与月初边界上均正确；`set_root_password_note` 不动 `defined_tags`。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.71
```

## 0.4.70 — 2026-08-06

复查上一版，改掉两处它自己引入的问题。

### 修复
- **改了配置再重试，会带着旧的幂等 key 发一个不同的请求。** 0.4.69 只在成功后才
  更换 key，失败一律沿用 —— 这对「原样重试」是对的，但如果你是因为失败而**改了规格
  或大小**再点创建，那已经是另一个请求了。用上一次的 token 提交，等于让 Oracle 把两
  个不同的创建当成同一个：要么把先前那台返回给你，要么直接拒绝这次提交。

  现在 key 绑定在它被创建时的那份请求体上：**内容没变就沿用（保护生效），
  内容变了就换新的**。

- **共享缓存写失败会静默关掉整个修复。** `_store_shared_meta` 原本吞掉所有异常。
  一旦 `json.dumps` 因为将来某个字段不可序列化而失败，跨进程缓存就悄悄失效，
  唯一的症状是 0.4.69 修掉的那个偶发超时**慢慢回来**，而日志里没有任何线索。
  现在这条路径会打一条明确的 warning，说明缓存已失效以及后果。

### 复查无发现
定时器与监听成对清理、`v-for` 均有 `:key`、无未标注纯图标按钮、除法分母均有下限、
表格均有横向滚动容器、全局遵循 `prefers-reduced-motion`；
100 个 `/api` 路由仍全部被测试覆盖；pyflakes 干净（顺带删掉两个失效 import）。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.70
```

## 0.4.69 — 2026-08-06

根治创建实例偶发 520：找到了机制，不再是猜测。

### 修复
- **偶发 520 的真正原因：启动配置缓存是「按进程」的，而 API 跑 2 个进程。**

  `fetch_launch_meta` 一次冷调用要列出每个操作系统家族的镜像，并且在没有网络的
  租户上**创建 VCN、子网、网关、路由表并逐个等待可用** —— 一分钟以上是正常的。
  它的缓存 `_META_CACHE` 是个模块级 dict，也就是**每个 uvicorn 进程各有一份**，
  而默认 `OCIBOT_API_WORKERS=2`。

  于是：你点「加载配置」，请求落在 worker A，A 做完那一分钟并缓存；
  你接着点「创建」，请求有**一半概率**落在 worker B —— B 的缓存是空的，
  于是**整个冷启动在创建实例这个请求里重做一遍**，超过 Cloudflare 100 秒边缘上限。

  这解释了为什么它「偶发」（两个 worker，五五开）以及为什么「重试一下就好了」
  （重试要么落到热的那个，要么此时 B 也热了）。

  修复：缓存加一层**跨进程共享**（存在已有的 `app_meta` 表，只有 id 和名称，
  没有任何凭据）。任一 worker 完成冷启动后，另一个直接读库，不再重复。
  命中后会提升到本进程内存，避免每次都走数据库。

- **创建实例改为幂等。** 之前不带 `opc-retry-token`，所以响应一旦丢失
  （代理超时、连接中断），你无法知道 Oracle 到底建没建 —— 去看会和重试赛跑，
  重试则可能建出第二台。现在浏览器为每次提交生成一个 key，**失败重试沿用同一个**，
  成功后才换新的。Oracle 24 小时内认得它，会返回原来那台而不是再建一台。

  批量创建按序号派生（`key-0`、`key-1`…）。这一步不能省：
  一批 5 台用同一个 key，Oracle 会创建第一台然后把它返回 5 次 ——
  页面显示创建了 5 台，实际只有 1 台。

### 维护
- 新增 `tests/test_launch_meta_shared_cache.py` 7 例（模拟第二个 worker 进程）、
  `tests/test_launch_idempotency.py` 9 例（含批量必须派生不同 token）。
- 修掉一处**既有的测试污染**：`test_endpoint_smoke.py` 在模块级把
  `launch_service.fetch_launch_meta` 整个替换掉了，泄漏到同一进程内的所有测试模块 ——
  新的缓存测试一开始测的其实是它的桩。改为只替换路由自己的绑定。
- 过程中我自己犯了一个错并修掉：共享缓存最初借用请求的数据库会话并在其上
  `commit()`/`rollback()`，会连带提交或丢弃该请求尚未完成的事务。
  表现为另一个无关测试丢了一行 User。现在缓存读写一律使用自己的独立会话。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.69
```

## 0.4.68 — 2026-08-06

实例列表的 root 密码备注可以改了。

### 功能
- **密码备注支持修改 / 清除**。这个值写在实例的 OCI 自由标签
  `ocibot_root_password` 里，**只在创建时写一次**，之后你在服务器上改了密码，
  面板还一直显示旧的，也没有任何地方提示它已经过期。现在密码列旁边多了个「改」，
  可以随时更新或留空清除。

  密钥模式创建的实例也能用：它们本来没有这个备注，现在可以补记一个。

  **只改面板显示，不动服务器上的密码** —— 弹窗里写明了这一点。
  「改密码」和「改密码备注」差一个字，后果差很远。

### 实现要点
- 写入前**先读一次现有标签再合并**。`UpdateInstanceDetails(freeform_tags=...)`
  是**整体替换**而不是合并，只发这一个键会把实例上的其他标签全部删掉 ——
  包括 `ocibot_managed` 这种别的功能依赖的标记。而且不会报错。
  `tests/test_root_password_note.py` 第一条测的就是这个。
- **不做复杂度校验**。这里记录的是一个**已经存在**的密码，不是设置密码。
  因为「太短」而拒绝，只会让面板继续显示上一个密码 —— 正是这个功能要解决的问题。
  但拒绝换行（会破坏标签）和超过 255 字符（OCI 上限，截断等于存了个错的密码）。
- 留空是删除该标签，而不是存一个空字符串 —— 后者在列表里是个空白单元格，看起来像渲染坏了。
- 更新成功后只改动内存里的那一行，不整表刷新：为了重画一个单元格而对每台机器
  再走一次 Oracle 往返不划算。

### 维护
- `tests/test_root_password_note.py` 9 例：其他标签不被破坏、新增/更新/清除、
  换行与超长被拒且**拒绝时不写入**、失败时旧值保留。
- 新接口纳入 `test_endpoint_smoke.py`（补桩时它先失败了一次，说明覆盖是有效的）。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.68
```

## 0.4.67 — 2026-08-06

账户用量页新增「本月费用」。

### 功能
- **费用查询新增本月费用**（自然月至今，UTC）。显示在「费用（最近 N 天）」卡片顶部，
  独立成块并标出统计起点日期。

  **为什么不在前端直接从日线里加总**：那份日线是「最近 N 天」的滚动窗口，
  和自然月只是偶尔重合。比如今天 20 号、你选了 7 天，把这 7 天加起来标成「本月」，
  数字会少掉三分之二 —— 而且看上去完全合理，没有任何地方会提示它是错的。
  所以本月费用由服务端算，查询窗口始终覆盖到 1 号。

  **不额外增加 Oracle 调用**：把查询窗口取「最近 N 天」与「本月」的并集，
  仍然是一次请求。多花一次调用会和抢机重试抢同一个速率限制额度。

  **原有数字含义不变**：「合计」「每日柱状图」「按服务」仍然只统计你选的那 N 天，
  不会因为查询窗口变宽而悄悄变大。这一点有专门的测试守着。

- 读取失败时显示「未能读取」而**不是 0.00**。对费用来说这两者含义相反，
  而「0.00」是最不会被质疑的错误答案。服务端在失败路径返回 `null` 而非 `0`，
  前端也刻意没有写 `?? 0`。

### 维护
- 新增 `tests/test_usage_month_to_date.py` 8 例：短窗口不截断本月、
  长窗口不把上月算进本月、窗口聚合不被拓宽的查询污染、无日期的条目不计入
  （`"unknown"` 在字符串比较里排在所有 ISO 日期之后，直接比会被算成本月）、
  失败路径返回 `null`、以及仍然只发一次 OCI 请求。
- 新 UI 的 4 组配色在亮暗两套下各校验一遍，8/8 通过（含非文本 3:1）。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.67
```

## 0.4.66 — 2026-08-06

上一版的 SDK 裁剪**一个字节都没省下来**。这一版才真正生效。

### 修复
- **裁剪放错了 Docker 层，镜像体积完全没变**。0.4.64/0.4.65 的 Dockerfile 写成：

  ```dockerfile
  RUN pip install ...                    # 这一层含完整的 404 MB SDK
  RUN python /tmp/trim_oci_sdk.py        # 这一层只记录「删除」
  ```

  Docker 的层是叠加的：删除操作只是在上层做个标记，**下层那 404 MB 原封不动地留在镜像里**。
  所以裁剪对镜像体积的贡献是 0。

  讽刺的是我在同一段注释里写了「放在与安装同一层，这样节省才是真的，而不是让删掉的文件
  藏在下层」—— 然后紧接着的代码就写成了两个 `RUN`。**注释说对了，代码做错了。**

  这也解释了为什么所有本地验证都是绿的：容器运行时看到的确实是裁剪后的树，
  导入校验、全量测试、静态闭包检查全部正常。唯一的症状是 `docker images` 的体积没降 ——
  而那个数字我看不到。是你贴出来的 **1.15 GB** 暴露的。

  修复：合并成一个 `RUN`。新增 `tests/test_oci_sdk_trim.py::test_trim_runs_in_the_same_layer_as_pip_install`
  钉住这一点（已双向验证：当前写法通过，拆成两个 `RUN` 的写法失败）。

### 说明
上一版记的「404 MB」里包含 pip 安装时一并生成的字节码：
208 MB 的 `.py` + 195 MB 的 `.pyc`。裁剪按目录删除，两者一起走，所以省下的仍是约 346 MB。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
docker images | grep ocibot        # 这次应能看到明显下降
docker image prune -f              # 回收旧镜像占的空间
```

旧镜像不会自动删除，`prune` 之后才能把磁盘真正还回来。

## 0.4.65 — 2026-08-06

修上一版裁剪 SDK 时埋下的一个坑。**建议直接从 0.4.63 升到本版**，
0.4.64 的镜像在某个环境变量下起不来。

### 修复
- **0.4.64 的 SDK 裁剪留了一颗雷**。上一版我只读了 `oci/__init__.py` 的前 14 行，
  看到它只导入基础设施模块，就断定服务包都是惰性加载、删掉安全。**这个结论只对了一半。**

  完整的 `__init__.py` 有**两条分支**：

  ```python
  if ... os.getenv("OCI_PYTHON_SDK_LAZY_IMPORTS_DISABLED", "False") != "true":
      def __getattr__(x): ...            # 惰性 —— 裁剪后没问题
  else:
      from . import <全部 175 个包>       # 预加载 —— 裁剪后直接 ImportError
  ```

  也就是说：只要环境里设了 `OCI_PYTHON_SDK_LAZY_IMPORTS_DISABLED=true`，
  `import oci` 就走预加载那条路，**面板根本起不来**，而报错指向 SDK 内部，
  没有任何线索指向这次裁剪。`web/.env` 里的变量是整个透传进容器的，
  这不是不可能触发的路径。

  修复：裁剪脚本现在同时把 `__init__.py` 里的 `__all__` 和那条预加载 import
  按实际留存的包重写一遍，两条分支于是都与磁盘上的内容一致，这个变量不再有影响。
  顺带让 `dir(oci)` 不再列出已经不存在的包。

  已实测三种模式（默认惰性 / `LAZY_IMPORTS_DISABLED=true` / `NO_SERVICE_IMPORTS=true`）
  在裁剪后的树上全部可用；并复现确认**修复前**第二种确实会 ImportError。

### 验证方法的改进
这个坑不是靠跑测试发现的 —— 0.4.64 的全量测试在裁剪后的 SDK 上是绿的（498 passed），
因为测试跑的是默认那条分支。真正找出来的是一次**静态导入闭包检查**：
把裁剪后仍然存在的 1932 个文件全部 AST 解析一遍，问「有没有谁 import 了已被删除的包」。
现在这一步的结论是干净的（17 个包 / 1932 个文件，无悬空引用）。

- `tests/test_oci_sdk_trim.py` 增至 5 例，新增两条：裁剪脚本必须同时处理
  `__all__` 与预加载 import；以及 SDK 若哪天去掉了这个分支，测试会提示去掉对应的死代码。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.65
```

## 0.4.64 — 2026-08-06

镜像瘦身 346 MB。

### 优化
- **裁掉用不到的 OCI SDK 服务包**。SDK 装下来 403 MB、175 个服务包，本面板只用到
  其中 17 个（约 57 MB）。剩下的 158 个是自治数据库、Data Safe、GoldenGate、
  日志分析这类**在这里永远不可能被调用**的代码，却是整个镜像里最大的一块。

  这件事值得做的原因是构建位置：镜像是在**操作者自己的服务器上**、每次
  `install.sh update` 都重建一次，而那台机器通常是引导卷不大的 Always-Free 实例。

  安全的前提是 SDK 不会预加载服务包 —— `oci/__init__.py` 只导入基础设施模块，
  服务包要等到引用客户端类时才加载。所以删掉从不引用的包是不可观测的。

  **但「所以」不足以作为依据**，因此 `scripts/trim_oci_sdk.py` 删完会把面板用到的
  12 个导入点全部 import 一遍，任何一个失败就以非零码退出 —— 错了是**构建失败**，
  而不是几周后某个功能报 502。

  实测：404 MB → 58 MB，删除 158 个包，全量测试套件在裁剪后的 SDK 上 **498 passed**。

### 维护
- 新增 `tests/test_oci_sdk_trim.py`。构建期校验只能防住 SDK 那一侧；如果**面板**新增了
  `from oci.某服务 import ...` 而忘了加进 KEEP，构建照样成功，故障会以运行期 502 出现。
  这个测试扫描源码里所有 `oci.<服务>` 引用，要求它们都能在裁剪后存活。
  （写完第一次运行就抓到 `oci.util` 漏网 —— 复查后确认它是 `util.py` 单文件、
  不在删除范围内，是测试的判定规则写错了，已修正为只判定目录。）
- 记录一个反直觉点：`oci.dns` 看着能删，实际不能 —— `oci.pagination` 在模块级
  `from .. import dns, object_storage`，而 `__init__.py` 导入 pagination，
  删掉它会让 `import oci` 本身失败，且报错指向 SDK 而非这次裁剪。

### 复查结论：前端不需要优化
首屏 181 KB JS + 39 KB CSS（gzip 后约 70 KB）；最大的 `WebSshTerminal`（290 KB）
已经是按需加载的独立分片，只在真的打开网页终端时才拉。这块没有值得动的地方。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.64
```

首次更新会重新构建镜像；之后 `docker image prune` 可回收旧镜像占用的空间。

## 0.4.63 — 2026-08-06

全量功能 + UI 排查。三个真实 bug，其中两个是「不会报错、只会给你看错数据」的那类。

### 修复
- **锁定的默认租户会被「打开某个页面」这个动作删掉**。`创建实例` 和 `存储` 两个页面
  只列出**已启用**的租户，而它们把这份过滤后的列表交给 `pickTenantId` 去判断
  「锁定的租户还在不在」。于是：锁定租户 A → 停用 A → 打开这两个页面中的任意一个
  → 锁定被清除。而打开 `账户` 或 `实例`（传的是完整列表）→ 锁定保留。

  **同一个停用租户，存不存活取决于你先点了哪个页面。** 0.4.58 把锁定改成账号级、
  存在服务端之后，这已经不是浏览器里的小麻烦 —— 是一个持久化设置被导航动作删掉。
  停用是可逆状态，只有真正不存在的租户才该解除锁定。

  修复：`pickTenantId` 增加第三个参数（完整租户列表），自愈判断改为「在完整列表里
  也找不到」才清除，而不是「非空就清除」。

- **账户用量页会把 A 租户的账单显示成 B 租户的**。`loadAll()` 并发拉取
  account / usage / quota / invoices 四份数据，**没有任何时序防护**。切换租户后再点刷新，
  两轮请求同时在飞，谁后到谁覆盖 —— 整页的消费、账单、剩余免费额度都可能是上一个租户的，
  而界面上没有任何提示。OCI 接口本来就慢（数秒），这个窗口很宽。

  修复：套用 `StorageView` / `InstancesView` 已有的 per-loader 时序防护
  （计数器 + 租户快照双重判断）。顺带修掉 `loading` 状态：先发起的那轮先结束时，
  会在当前这轮还在加载时就把加载指示关掉。

- **`DATABASE_URL` 用内存 SQLite 时，应用在 import 阶段直接崩溃**。
  `db.py` 对所有 SQLite 都传 `pool_size` / `max_overflow`，但 SQLAlchemy 给内存库分配的是
  `SingletonThreadPool`，两个参数都不接受，`create_engine` 抛 `TypeError` ——
  发生在模块导入期，所以进程还没来得及说明原因就死了。
  四种写法全中：`:memory:`、`sqlite://`、`sqlite+pysqlite://`、`?mode=memory`。
  其中 `sqlite://` 最容易漏 —— 字面上没有 "memory" 字样，却同样是内存库。
  现在用 SQLAlchemy 自己的 `make_url` 解析判断，不再靠字符串匹配。

### 维护
- **补齐接口测试覆盖**：全量枚举 OpenAPI 后发现 100 个 `/api` 路由里有 **6 个从未被任何
  测试碰过**，其中 4 个与认证相关（改密码、登出、全端登出、管理员重置密码）。
  已全部纳入 `test_endpoint_smoke.py`，现在 **100/100**。
  改密码那条不只断言「没有 5xx」，还会用新密码重新登录一次 —— 一个「返回 200 但其实
  什么都没改」的接口，光看状态码是分辨不出来的。
- 新增两个前端不变量的静态断言（本仓库没有 JS 测试运行器）：过滤过的租户列表必须传完整列表、
  自愈条件必须是「在完整列表里也不存在」。已验证把修复改回去这两条会失败。

### 复查无发现
定时器与事件监听全部成对清理；`v-for` 全部有 `:key`；无未标注的纯图标按钮；
所有除法分母都有下限保护（不会出现 `NaN%`）；表格全部有横向滚动容器；
全局已按 `prefers-reduced-motion` 关闭动效。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.63
```

## 0.4.62 — 2026-08-06

主工作区不再用纯白 / 近黑，两个主题都按感知亮度重排了表面阶梯。

### 修复
- **亮色主工作区是纯白**。`--panel: #ffffff` 感知亮度 L\*100，而它占满大半个视口，
  是屏幕上最亮的一块 —— 长时间盯着看会累。现在 L\*95.1（`#eef1f6`），
  仍然读得出是「纸」，但不再是最高亮度。这是电子书阅读模式不用纯白的同一个道理。

  降亮度**不是**靠降对比换来的：正文 11.52:1、次要文字 6.13:1，
  都远高于 AA 的 4.5:1。

- **暗色底色贴着纯黑**。`--bg: #141415` 是 L\*6.4，亮字压在近黑底上会产生光晕
  （halation），散光的人尤其明显；而且 bg 到 panel 跨了 11.6 个 L\*，
  一眼扫过明暗落差很大。现在把地板抬到 L\*10.8、落差压到 5.3 个 L\*，
  正文同时从 `#d5dbe5` 降到 `#cbd1dc`（暗色下过亮的字本身就是眩光源），
  降完仍有 9.63:1。

- 顺带修掉审计暴露出的三处对比度不足，都属于 WCAG 1.4.11 非文本对比（3:1）：
  - 亮色 `--border-strong` 落在页面底色上只有 **2.31:1** → 3.06:1。
    直接放在页面背景上的按钮/输入框，边框是看不清的。
  - 暗色 `--border-strong` 落在输入框底色上只有 **2.49:1** → 3.16:1。
  - 亮色 `--input-bg` 与 `--panel` **完全相同**（1.00），输入框只靠边框存在；
    现在 1.06，仍比面板亮一点以保住「这里可以输入」的可供性。

- `:root` 里 `--input-bg` 和 `--row-hover` **各声明了两遍**，后一份覆盖前一份 ——
  改前一份不会有任何效果。已合并为一处。
- `AppLayout.vue` 移动端抽屉里重复的 `box-shadow` 声明。

### 验证
23 组配色对（含非文本 3:1 与表面之间的可分辨度）在亮暗两套下各算一遍，
**46/46 通过**，且是对**构建产物** `dist/` 里的实际取值算的，不是对源码。
渲染后又经浏览器实测计算样式复核：亮色面板 L\*95.1、暗色底色 L\*10.8，与设计值一致。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.62
```

## 0.4.61 — 2026-08-06

切换到域名模式后的校验能报出真实结论，并认出 CDN 明文回源这一类故障。

### 修复
- **切换脚本会把坏掉的部署报成成功**。原来用 `curl -fsS https://域名/api/health`
  判断，而 `-f` 只在 **400 及以上**失败 —— 308 重定向被当成成功且返回空 body。
  于是「CDN 用明文回源 → 和本机的 HTTP→HTTPS 跳转打成死循环 → 浏览器
  ERR_TOO_MANY_REDIRECTS」这种坏法，脚本会打印「HTTPS 已就绪 ✓」。
  恰好是操作者最需要被告知的那一种。

  改为直接看状态码：只有 200 算就绪；30x 时取 `Location`，指回同一个 https 地址
  就点破成因。

### 功能
- 检测到域名前面有 CDN 时（按 `Server` 响应头认，不按 IP 段 —— IP 段会变），
  在切换前后都提示回源协议的要求。Cloudflare 单独给出确切操作：
  加密模式必须是 **完全（严格）/ Full (strict)**，且证书签发期间要先关掉
  **Always Use HTTPS**（它在边缘拦下 HTTP，而 ACME 的域名校验正是走 HTTP 到达本机）。
- 判定为死循环时额外说明：**308 是永久重定向，浏览器会缓存**。服务端修好之后
  旧标签页仍会自己跳，看起来像没修好 —— 需要用无痕窗口或清站点缓存验证。

### 维护
- `docs/ACCESS-MODES.md` 新增 ERR_TOO_MANY_REDIRECTS 专节，第一步先用
  `curl -L -w '%{http_code} redirects=%{num_redirects}'` 分清是服务端还在循环
  还是只有浏览器还在循环 —— 两者的处理完全不同。
- `tests/test_deploy_modes.py` 增至 22 例，其中三例专门锁住「不得用 `curl -f`
  判断 HTTPS 是否就绪」。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.61
```

## 0.4.60 — 2026-08-06

面板自带 HTTPS，不再需要外部反向代理。

### 功能
- **内置 HTTPS 终结**。新增一个 `caddy:2-alpine` 服务，挂在 compose 的 `tls`
  profile 下，配置见 `deploy/Caddyfile`。证书签发与续期全自动，
  **不需要再装 Nginx Proxy Manager**。

  ```bash
  bash scripts/install.sh domain panel.example.com   # 域名 + 自动 HTTPS
  bash scripts/install.sh ip                         # 改回 IP + 端口直连
  ```

  两种访问方式装完后随时可以互换，不影响数据库和已导入的租户。
  首次安装会交互询问；`curl … | bash` 这种没有终端的场景默认 IP 直连，
  与此前行为一致。

- **访问方式相关的配置成组写入**。`COMPOSE_PROFILES` / `OCIBOT_DOMAIN` /
  `CORS_ORIGINS` / `COOKIE_SECURE` / `TRUST_PROXY` / `FORWARDED_ALLOW_IPS` /
  `BIND` 七项由脚本一起改，不再逐项手工对照。

  这不是为了省事：它们配错**不会报错**。`COOKIE_SECURE=1` 配明文访问时，
  浏览器根本不回传登录 Cookie，表现是「登录成功后立刻跳回登录页」；
  `TRUST_PROXY=1` 而前面没有反代时，任何客户端都能自己声明限流身份。
  两种都不指向配置，很难联想。新增 `tests/test_deploy_modes.py` 18 例锁住这些配对。

- 切换到域名模式前会先检查 **80/443 是否被占用**（列出占用的容器名而不是让
  compose 抛一句 `port is already allocated`）、**DNS 是否指向本机**，
  并尽力在 ufw / firewalld 放行 80/443。云厂商安全组脚本改不到，会明确提示。

- `install.sh status` 与安装结束时打印**真正能打开的地址**，不再让人去猜。

### 修复
- `install.sh uninstall` 现在会带上 `--profile tls`。不带的话，
  `docker compose down` 会把 Caddy 留在后台继续占着 80/443。
- 面板内一键更新时向辅助容器透传 `COMPOSE_PROFILES` 等访问方式相关变量，
  否则更新流程眼里的「这套栈」不包含 HTTPS 前端。

### 维护
- 新增 `docs/ACCESS-MODES.md`（两种模式的差别、前提、自检与排查）。
  `docs/NPM-REVERSE-PROXY.md` 保留，作为仍想用 NPM 时的路径。
- 域名入参校验收紧：拒绝裸 IP、连续点、以 `-` 开头或结尾的标签。
  字符白名单不含换行——这个值会被原样写进 `web/.env` 的 `OCIBOT_DOMAIN=` 后面，
  带换行的输入本可以再插一行覆盖 `OCIBOT_MASTER_KEY`。

### 升级

```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.60
```

升级本身不改变现有访问方式。要启用 HTTPS 再执行
`bash scripts/install.sh domain <你的域名>`。

## 0.4.59 — 2026-08-03

全量 bug + 安全排查。三处修复，两处属于纵深防御，一处是真实的日常故障。

### 安全
- **自更新路径存在参数注入风险**。`git fetch --depth 50 origin <分支>` 里的分支名
  直接来自环境变量且**未校验**——只有「检查更新」那条路径做了校验，
  而它的正则 `[A-Za-z0-9._/-]+` **允许以 `-` 开头**。

  git 会把以 `-` 开头的参数当作**选项**解析，所以分支名写成
  `--upload-pack=<命令>` 就能让一次 fetch 执行任意命令。argv 是列表、没有 shell，
  但拦不住选项解析器；而这是整个面板权限最高的代码路径
  （驱动挂载的 docker.sock，并可能进入宿主机命名空间）。

  **不是 Web 面可达**——这两个值只来自环境变量，没有任何路由能写入
  （已逐一确认）。所以属于纵深防御，不是线上漏洞。

  修复：校验收敛到 `_checked_repo` / `_checked_branch` 两个函数，
  **拒绝前导 `-`**，并让读取环境变量的 `_repo()` / `_branch()` 本身就校验，
  这样执行路径和检查路径不可能再分叉。仓库名同样拒绝前导 `-`
  （它当前只进 URL 和 `-e KEY=value` 的值部分，但那是调用点的性质，不是值的性质）。
  新增 `tests/test_update_argv_safety.py` 24 例。

### 修复
- **更新后页面「点了没反应」**。发布新版本后分片文件名带新哈希，仍开着旧页面的
  浏览器一导航就会 `Failed to fetch dynamically imported module`，
  导航静默失败——这正是此前每篇升级说明都要写「按 Ctrl+F5」的原因。
  现在路由捕获这类错误并**自动重载一次**（`sessionStorage` 标记确保只重载一次，
  否则真正的错误会被无限刷新掩盖；导航成功后清除标记）。
- **地球组件的清理逻辑收拢**。`onBeforeUnmount` 原本注册在 `onMounted` 内部，
  依赖 Vue 当前实例仍然存在。它能工作，但没必要留这种存疑写法——
  现在移除监听、停止动画帧都在同一个钩子里。

### 复查无发现
- 无 `eval` / `exec` / `pickle` / `yaml.load` / `shell=True`；唯一的 `subprocess.run`
  用列表 argv 且日志脱敏
- 无 SQL 字符串拼接；前端无 `v-html` / `innerHTML`
- 新增的 `PUT /api/auth/locked-tenant`：需登录、入参有长度上限、
  锁别人的租户 404、**实测跨站 Origin 被 403 拦截**（同源 200）
- 10 个路由 × 深浅主题 × 1280/375：低对比文字 0、横向溢出 0
- 471 passed；`vue-tsc`、`pyflakes` 与构建均干净

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.59
```
**本次之后不再需要手动 Ctrl+F5**——路由会自己处理换版后的分片失效。
（这一次仍需刷新，因为自动重载的代码本身要先加载进来。）

---

## 0.4.58 — 2026-08-03

### 功能
- **「锁定为默认」改为账号级，存在服务端**。此前它只写在当前浏览器的
  localStorage 里——换一台设备、换个浏览器、开个无痕窗口，都要重新设一次；
  清一次浏览器数据也会丢。它是「这个人的偏好」，所以现在存在账号上。

  - 新增 `User.locked_tenant_id`，随 `/auth/me` 一起下发
  - 新增 `PUT /api/auth/locked-tenant`，空字符串表示取消锁定
  - **只能锁定自己的租户**；锁别人的返回 **404 而非 403**——不让攻击者据此
    区分「不是你的」和「不存在」
  - **删除租户时自动清除**对应的锁定，否则各页面会一直静默回落到第一个租户，
    界面上却没有任何解释
  - 退出登录时清空本地状态，避免同一浏览器换账号后继承上一个人的默认租户
  - 写入是乐观更新：点击立即生效，请求随后发出，不让一次点击等网络

### 修复
- 侧栏的锁定芯片在全新登录后只有图标、没有名称。会话只携带 id，而名称不该由
  服务端缓存（租户改名后就过期）。改为由持有租户列表的页面解析：
  「租户」页列出全部租户但从不调用 `pickTenantId`，因此显式同步一次；
  列表尚未加载时芯片显示「默认租户」而不是空白。

### 验证
- **迁移**：用裸 sqlite3 造出「升级前」的 users 表（无该列）再交给程序启动，
  实测列被自动加上、已有用户取到 `''`（非 NULL）、**新增用户的 INSERT 正常**
  —— 这正是 0.4.39 踩过的坑，所以专门验了
- **端到端**：浏览器里锁定一个租户后 **清空整个 localStorage 并重载**，
  锁定与名称仍然在 —— 证明它确实来自服务端
- `tests/test_locked_tenant.py` 9 例：跨客户端可见、账号间互不可见、
  锁别人的租户 404、删除租户后自动清除、未登录 401 等；
  新端点已纳入 `test_endpoint_smoke.py`
- 447 passed

### 说明
测试文件会在登录前重置登录限流器。限流器是进程内、按客户端 IP 计数的，
而全套测试共用同一个「IP」——不重置的话，本模块的登录会吃掉其他模块的额度，
导致后跑的模块随机失败（这次就先踩了一遍）。限流本身的行为由
`tests/test_login_audit.py` 覆盖。

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.58
```
升级后**原先在浏览器里设过的锁定不会自动迁移**，重新点一次「锁定为默认」即可，
之后所有设备通用。

---

## 0.4.57 — 2026-08-03

### 功能
- **登录后的页面沿用登录页那行模块名的排版**：等宽字体 + 字距展开。
  中文本身会回落到系统字体，效果主要体现在字距，以及拉丁字母与数字的等宽对齐上。

  落到两处：
  - **侧栏导航项**——它列的就是登录页那行里的同一批模块名（实例 / 存储 / 网络 /
    任务中心 / 备份恢复…），同样的词理应读起来是同一类东西
  - **各页标题下的说明行**——它在页面里承担的角色，与登录页那行相同

### 验证
字距会让文本变宽，所以逐项量了是否被挤断：
- 桌面 1280：侧栏 240px，**11 个导航项无一被截断**（含最长的「用户管理 / 更新」）；
  10 个路由的说明行均为单行，横向溢出全部为 0
- 移动 375：抽屉 300px，导航项同样无截断；说明行折为 2 行，无溢出

438 passed；`vue-tsc` 与构建通过。

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.57
```
更新后按 Ctrl+F5 强制刷新。

---

## 0.4.56 — 2026-08-03

界面文案改为陈述事实的技术表述，删掉没有信息量的话。

### 修复（重要）
- **登录页读数区的「区域 35 个节点」是编造的**。那是地球组件里坐标表的条目数，
  与你的账号毫无关系，却摆在版本号旁边像是系统状态。已删除。
  同一行的「服务 正常」原本是写死的文字，现在取 `/api/health` 返回的真实 `status`
  字段：`ok` 显示为「正常」，**其他任何值原样显示**——不把故障状态压成一个含糊的词。

### 文案
- **首屏标题**：`CONTROL / EVERY REGION` → `OCIBOT / 控制台`。
  前者是一句口号，没有任何可核实、可执行的内容。现在是产品名加它的定义，
  下面两行分别给出定位（多账号 · 多区域云资源管理）与**实际模块**
  （实例 · 存储 · 网络 · 容量重试 · WebSSH · 备份）。
- **删掉登录表单的副标题**「使用你的面板账号继续。」——字段本身已有「用户名」
  「密码」标签，这句话不承担任何职责。注册模式下的提示保留并改为
  「首个注册账号将获得管理员权限。」，因为它陈述了一个别处看不到的后果。
- **「请稍候…」全部替换为具体动作**：登录时显示「验证中…」、注册「创建中…」、
  创建实例「创建中…」。「请稍候」不说明正在发生什么。
- **「一键全开放」→「放行全部端口」**（含两处说明文字同步）。前者在推销点击动作，
  后者说明这次点击会做什么。
- 管理页的更新提示由「更新已开始，请稍候…」改为列出实际步骤：
  拉取代码、重建镜像、滚动重启。

### 验证
全站复查无营销措辞（轻松 / 快速 / 强大 / 智能 / 完美 / 极致 …）、无「请稍候」、
无编造数值。登录页实测：读数显示「版本 v0.4.56」「API 正常」均为真实数据，
地球正常绘制，横向溢出 0，新增文案对比度 4.74 / 5.56 / 14.1 全部达标。
438 passed。

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.56
```

---

## 0.4.55 — 2026-08-03

修复登录页地球「不动」。两个原因，都是我的问题。

### 1. 开启「减少动效」时被完全冻结
上一版遇到系统的 `prefers-reduced-motion: reduce` 会**只画一帧就停**。
这个设置的本意是让页面安静下来，不是把页面上唯一会动的东西变成一张静态图——
而一个不转的地球看起来就像坏了。现在改为**整体降速到 40%** 而不是冻结。

我当时的验证环境恰好也开着这个设置，所以测到的像素全部来自静止首帧，
没能发现问题。

### 2. 转速本来就太慢
自转是 **6.3°/秒，一圈要 57 秒**。即便动画在跑，也慢到看不出来。

| | 之前 | 现在 |
|---|---|---|
| 自转（正常） | 6.3°/秒 · 一圈 57 秒 | **20.1°/秒 · 一圈 18 秒** |
| 自转（减少动效） | 完全静止 | **8.0°/秒 · 一圈 45 秒** |
| 弧线脉冲 | 6.2 秒走完一条 | **2.5 秒** |
| 节点呼吸周期 | 5.2 秒 | **2.9 秒** |

### 验证
本环境的浏览器面板不合成帧，`requestAnimationFrame` **一次都不触发**，
所以循环本身在这里无法端到端验证。改用可验证的方式：间隔 2 秒各绘制一帧后比对，
像素从 23,753 变为 20,375、签名不同——**绘制函数确实随时间产生不同的帧**，
说明动画数学有效，在正常浏览器中会转动。

438 passed；构建通过。

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
```
更新后 **Ctrl+F5 强制刷新**（旧的 JS 分片会被缓存）。

如果仍觉得偏慢，多半是系统开了「减少动效」——在浏览器控制台执行这行可以确认：
```js
matchMedia('(prefers-reduced-motion: reduce)').matches
```
返回 `true` 就是开着；Windows 在「设置 → 辅助功能 → 视觉效果 → 动画效果」里关闭。

---

## 0.4.54 — 2026-08-03

登录页重做，主视觉是一颗会转的 3D 区域地球。

### 新登录页
- **全屏 3D 地球 + 粒子**：自转的经纬线框球体，上面 **35 个真实云区域节点**
  （东京 / 大阪 / 首尔 / 新加坡 / 法兰克福 / 阿什本 / 圣保罗 …），
  用的是各区域的真实经纬度，不是随机浮点——这个面板管的就是散布在这些区域的机器，
  所以地球是在**展示主体**，而不是装饰。
- 节点之间有**弧线链路与流动脉冲**，前后半球按深度做明暗与大小区分，
  配合星点背景形成纵深。
- 标题 `CONTROL / EVERY REGION`，第二行是全页唯一的渐变。
- 表单浮在地球之上，但**面板本身完全不透明**——这一页上唯一必须始终看得清的东西
  就是它，不能让背后转动的图形影响可读性。

### 为什么不用 Three.js
面板的 CSP 是 `script-src 'self'`，**不能从 CDN 引入**；而打包 Three.js 约 600 kB，
会压在整个站点第一个加载的页面上。所以整套是**手写 Canvas 2D + 自实现 3D 投影**：
透视投影、深度排序、DPR 适配、页面隐藏时暂停、组件卸载时清理。

**代价：登录页分片 8.29 kB（gzip 4.07 kB）**，含全部地球逻辑。

### 实测
- 桌面 1440×900：画布满屏，首帧绘制 26,745 像素（其中 487 高亮节点/脉冲）
- 移动 375×812：DPR 2 后备缓冲 750×1624，绘制 38,304 像素，单列布局，
  表单无需滚动即可见，触控目标 40 / 44px
- 两种尺寸横向溢出均为 0；控制台无报错
- 文字对比度：品牌 9.07 · 标题 18.65 · 副文案 8.14 · 表单标题 16.73 ·
  字段标签 8.63 · 主按钮 7.0 · 底部提示 5.49；渐变标题三个色标分别为
  6.77 / 12.18 / 5.74 —— 全部达 AA
- **`prefers-reduced-motion` 分支已实测**：首帧同步绘制，所以关闭动效时
  地球仍完整呈现，只是不再自转（本次验证环境正是这种设置）

登录、注册、两步验证、错误提示、重定向白名单逻辑均未改动。438 passed。

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.54
```
更新后按 Ctrl+F5 强制刷新。

---

## 0.4.53 — 2026-08-03

### 移除
- **用户管理 → 系统更新 里的「更新日志（中文）」整块删除**。同时删掉只为它服务的
  约 140 行日志翻译规则（把 `[ocibot-update] step: docker compose build` 这类行
  逐条译成中文的那张表）。

  更新卡片保留了真正有用的部分：**状态徽章**与**状态说明**，失败原因照旧显示。
  真要看逐行细节时，`docker logs ocibot-api-1` 比翻译过的摘要更完整。

  顺带：`AdminView` 分片从 14.83 kB 降到 **9.79 kB**（gzip 5.91 → 3.99 kB）。

### 维护
438 passed；`vue-tsc` 与构建通过；管理页实测渲染正常、控制台无报错。

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.53
```

---

## 0.4.52 — 2026-08-03

重新清点「有没有在后台偷偷请求 Oracle」。上次盘点是 0.4.37，之后新增了账单接口、
登录页版本读取和多轮前端改动，所以从头重查了一遍，**这次连前端的自动拉取一起查**。

### 结论：除抢机外没有自发请求
- **所有 OCI SDK 调用只存在于 `app/oci_client.py`**，进程内只有两个会话管理器：
  `oci_bridge`（给请求用）、`worker.sessions`（给抢机用）
- API 进程取会话的函数被调用 **58 次，全部位于 `routers/` 内**——即全部是 HTTP
  请求处理器，没有任何一处在模块加载、定时器或后台线程里
- API 启动钩子只做建表、提升首个管理员、检查弱密钥，不碰 OCI
- Worker 只有两个阶段：心跳（纯数据库）+ 抢机；没有启用任务时一次调用都不会发出

### 前端也查了（这次新增的角度）
- 各页面 `onMounted` 只调 `/tenants` 等**面板自身接口**，不触发 Oracle
- 实例详情页进入时只读该实例本身，其余标签页（监控 / 控制台 / 防火墙 / 网络 / 卷）
  按需加载
- 三处 `setInterval` 轮询打的都是**本地接口**，服务端只读面板数据库：
  `/system/status`（Worker 心跳 meta）、`/admin/update`（自更新状态）、
  `/jobs/capacity`（任务表）

### 修复
- **实例间切换会多打一次监控查询**：详情页的 watcher 无条件调用 `loadMetrics()`，
  与 `onMounted` 按当前标签页加载的逻辑不一致。停在「防火墙」标签页时从一台实例
  切到另一台，会白白发一次 Monitoring 查询。watcher 现在与 `onMounted` 完全一致。

### 已知且保留
`launch_service.schedule_post_launch_adjustments` 仍会在**你点了创建实例之后**
起一个后台线程等引导卷 hydration 完成再调性能（仅当引导卷性能不是默认 10 VPU）。
这是那次点击的延续，不是自发轮询；改成同步会让创建请求阻塞十分钟。

### 维护
438 passed；`vue-tsc` 与构建通过。

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.52
```

---

## 0.4.51 — 2026-08-03

PC 与移动端全量前端排查 + 安全复查。

### 修复
- **移动端抽屉里的文字看不清（你报的问题）**。0.4.47 改版时把 `.sidebar` 设成了
  `background: transparent`——桌面端它是页面上的一列，没问题；但**移动端它是浮在
  内容之上的抽屉**，透明之后文字直接叠在页面内容和深色遮罩上。
  浅色主题下实测对比度只有 **1.05**，等于隐形。抽屉现在有自己的不透明底色与投影。
- **移动端顶栏也是半透明**（`color-mix(... 78%, transparent)`）。它原本靠毛玻璃
  才成立，而毛玻璃在 0.4.47 已移除，剩下 78% 白叠在滚动的表格行上会发糊。改为不透明。
- **清掉最后残留的毛玻璃**：实例详情页的监控卡片与图表提示框。
  半透明卡片压在折线图上，正是数字最难读的地方。现在全项目 `backdrop-filter` 为零。
- **浅色 `--muted` 提到 `#62656a`**。之前只校验了它在面板上的对比度，
  没校验在页面底色上——那里是 4.24，不达标。现在三种表面分别为 5.87 / 4.51 / 4.86。
- 删除 `tests/test_invoices.py` 里一处重复定义的测试函数（后定义的覆盖前者，
  前者成为永不执行的死代码）。

### 全量检测结果
逐元素合成背景后计算真实对比度（含 `color(srgb ...)` 这类新语法），
**10 个路由 × 深/浅主题 × 375/1440 两种宽度**：

| 组合 | 低于 4.5:1 的文字 | 横向溢出 |
|---|---|---|
| 移动 375 · 深色 | 0 | 0 |
| 移动 375 · 浅色 | 0 | 0 |
| 桌面 1440 · 深色 | 0 | 0 |
| 桌面 1440 · 浅色 | 0 | 0 |
| 登录页 | 0 | 0 |

### 安全复查
- 新增的账单端点：需登录、按 owner 过滤租户、`limit` 有边界（1–100）；
  返回字段是**白名单**——`party_name` / `last_payment_detail` / `subscription_ids` /
  `is_credit_card_payable` 均未带出，付款人信息与支付方式不会回传浏览器
- 无 `eval` / `exec` / `pickle` / `yaml.load` / `shell=True`；前端无 `v-html` / `innerHTML`
- 唯一的原始 SQL 是硬编码常量的 PostgreSQL 咨询锁（首管理员竞态保护），无注入面
- 防 CSRF 的 Origin 校验、审计日志留存上限均仍在生效
- 438 passed；`vue-tsc` 与构建通过

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.51
```
更新后按 Ctrl+F5 强制刷新。

---

## 0.4.50 — 2026-08-03

修复 0.4.49 的账单查询——它对每个账号都失败。

### 修复
- **`sort_by` 传了服务不接受的值**，导致查询必然报
  `Invalid value for sort_by, must be one of [...]`。这个参数的取值是一个封闭集合，
  我上一版是**猜的**（`TIME_INVOICE_DUE`），没查 SDK。现在改为 `INVOICE_DATE`
  （按账期倒序，正是这张表要的顺序），并抽成模块常量。

- **失败时不再谎报"暂无账单"**。上一版你看到的是报错和「该账号暂无账单记录」
  同时出现——读都没读到，却断言这个账号没有账单。现在两种状态彻底分开：
  读取失败显示红色「读取失败」徽章与具体原因，只有**确实读到了空列表**
  才会说"暂无账单记录"。

### 维护
- `tests/test_invoices.py` 增加 3 例：把 `_INVOICE_SORT_BY` 与 **SDK 文档里声明的
  合法取值**逐一比对（这条能直接拦住"猜参数"这类错误）、确认请求真的带上了该值、
  以及"读取被拒绝 ≠ 账号无账单"。已验证有效：把错值放回去，测试立刻失败并打印
  `TIME_INVOICE_DUE not in [...]`。
- 顺带修掉这个测试文件自身的隔离问题：辅助函数把 SDK 类替换成 MagicMock 后没有还原，
  导致后续测试读到的是 mock 的文档。现在在打桩前就抓取真实取值。
- 438 passed；`vue-tsc` 与构建通过

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.50
```

---

## 0.4.49 — 2026-08-03

### 功能
- **账号用量页新增「每月账单」**：按账期列出账单号、金额、未付金额、到期日，
  以及**是否已支付**。顶部一眼给出结论——有未付清就标「N 张未付清」，
  否则标「已全部付清」。

  数据来自 OCI 的 **OSP Gateway 发票接口**，不是 Usage API。两者回答的问题不同：
  用量告诉你这个月**花了多少**，发票才知道 Oracle **开了多少账、收没收到钱**。
  支付状态只有发票接口有。

  状态区分四种：已支付 / 未支付 / 已逾期 / 支付处理中；
  **支付失败**单独标红——它不只是"没付"，是需要你去处理。

  与面板其他 Oracle 调用一致：**只在你点「刷新用量」时才请求**，不会后台拉取。

### 关于免费账号
Always Free / 试用账号**没有订阅，因此不会产生任何账单**，Oracle 对这个查询会返回
「未找到」。这在面板里显示为「该账号暂无账单记录」并附一句说明，
**不当作错误**——对这类账号来说"没有账单"就是正确答案。
若你的付费账号也显示为空，通常是当前用户缺少账单读取权限：
```
Allow group <你的组> to read invoices in tenancy
```

### 维护
- `TenantSession.list_invoices()`（`app/oci_client.py`）
- `GET /api/tenants/{id}/invoices`，已纳入 `test_endpoint_smoke.py`
- `tests/test_invoices.py` 新增 11 例：已付 / 未付 / 缺 `is_paid` 时按状态回退 /
  支付失败 / 免费账号不报错 / 真实故障仍报错 / 时间与币种序列化 / 条数上限 /
  端点不产生 5xx
- 435 passed；`vue-tsc` 与构建通过

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.49
```

---

## 0.4.48 — 2026-08-03

修复 0.4.47 的配色：看着累眼、细节之间没有反差。

### 原因
上一版我只测了**文字与背景**的对比度，没测**面与面之间**、**边框与面**的差异。
结果是文字达标、界面却糊成一片。实测当时的深色模板：

| 指标 | 0.4.47 | 应达到 |
|---|---|---|
| 面板 vs 页面底 | 1.05 | ≥1.30 |
| 分隔线 vs 面板 | 1.21 | ≥1.70 |
| 控件边框 vs 面板 | 1.51 | ≥3.00（WCAG 非文字） |
| 表头底 vs 面板 | 1.06 | ≥1.20 |

四项全不达标——面板贴在底色上看不出是一块面，边框近乎不可见，
表头和数据行混在一起。加上底色 `#0a0a0f` 接近纯黑，浅色文字会产生光晕，久看刺眼。

### 修复
整套配色按上述指标**解算**而非挑选，深浅两套都逐项验证：

- 页面底抬离纯黑（深色 `#141415`），正文改为 `#d5dbe5` 而非纯白，减少光晕
- 面板明显抬高（深色 `#2b2c2e`），与底色拉开到 1.32
- 分隔线与控件边框分级：分隔线 1.70 够看见但不抢戏，控件边框 3.01 满足 WCAG
- 表头恢复独立底色（上一版误设成与面板同色，浅色下分离度是 **1.00**，
  等于表头完全看不出来）
- 「次要文字」与「弱化文字」原本几乎同色，现在拉开到 1.34 倍，层级恢复
- 状态色（成功 / 危险 / 警告）也在新面板上重新校准

### 实测（深 / 浅）
面板分离 1.32 / 1.30 · 分隔线 1.70 / 1.70 · 控件边框 3.01 / 3.01 ·
表头底 1.26 / 1.21 · 正文 10.05 / 12.08 · 弱化 5.46 / 5.51 · 强调 5.29 / 7.48 ·
成功 8.02 / 5.15 · 危险 5.56 / 5.61。横向溢出 0。
424 passed；构建通过。

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.48
```
更新后按 Ctrl+F5 强制刷新。深浅主题都已重做，右下角可切换。

---

## 0.4.47 — 2026-07-30

换掉整套视觉模板。前几版改的是间距、圆角、对比度这类细节，观感上等于没变——
这次换的是**设计语言本身**。

### 新模板
- **默认深色**。页面底色 `#0a0a0f`，面板 `#111117`——只比底色高一点点，
  刚好读成一个表面，而不是贴在上面的一张卡片。原来的浅色主题仍在，
  右下角可随时切回，切换后会记住。
- **取消卡片浮层**。原来是「白卡片 + 阴影 + 灰底」，每页都变成一堆盒子。
  现在全站**零阴影**，容器只用 1px 细线界定；表格外层不再套第二层边框。
- **侧栏融入同一张表面**：去掉面板底色与投影，只留一条分隔细线。
  当前页不再是填充色块，而是左侧一根 3px 的强调色条 + 图标变色——
  标记位置，但不把整行重绘成一个按钮。
- **表格成为主角**：行高提到 0.95rem、末行去掉分隔线；
  表头改为 10px、字距 0.11em 的微型大写标签，和数据拉开层级。
- **按钮改为线框**：透明底 + 细边，悬停才上底色。原来是半透明白底，
  叠在表格上会透出底纹。
- 页面标题提到 2rem、字距收紧到 -0.03em；强调色在深色下提亮为 `#8f86ff`。

### 验证
深色与浅色各以重载方式实测（切换主题的即时重算在本环境不可靠）：

| | 正文 | 表头 | 导航 | 弱化文字 |
|---|---|---|---|---|
| 深色 | 15.99 | 8.55 | 9.62 | 5.21 |
| 浅色 | 18.35 | 5.25 | 8.85 | 4.91 |

全部达到 WCAG AA；卡片阴影确认为 `none`；横向溢出 0。
424 passed；`vue-tsc` 与构建通过。

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.47
```
更新后按 Ctrl+F5 强制刷新。**若之前手动切过主题**，浏览器里存着你的选择，
需要在右下角切一次才会看到深色新模板。

---

## 0.4.46 — 2026-07-30

### 修复
- **导航恢复文字**。侧栏改回 240px 的「图标 + 名称」，分组标题（工作台 / 资源 / 系统）
  也恢复为文字。纯图标栏取消——图标是识别，文字才是答案。
- **控件上的 emoji 全部换成 SVG**：租户页的「🔒 默认」徽章、管理页的「🔄 系统更新」
  标题与两步验证列的「✅」。新增 `refresh` 图标。
  正文里的 ⚠ 警示符保留——那是行文中的标点，不是控件图标。

### 功能（全站生效，改的是共用样式）
- **页头有了基线**：每页标题下加一条分隔线，页面不再从半空开始。
- **表头有了下边界**：滚动时数据行不会再从表头文字中间穿过去。
- **空状态成形**：12 个页面里 16 处「暂无数据」从一行灰字，变成有留白、居中的空状态。
- **控件对齐**：按钮 36px / 输入框 37px 统一到同一基线，圆角统一 8px，
  按钮改为不透明（此前是半透明白，叠在表格上会透出底纹）。
- **行悬停反馈**：表格行加上悬停底色，明确「这一行是可点的」。
- **卡片内标题统一节奏**：`h3` / `h4` 的字号与间距不再由各页自己内联控制。

### 验证
桌面 1440 与移动 375 各跑一遍：侧栏 240px、导航图标与文字齐全、
页头分隔线与表头边界到位、锁定徽章为 SVG、横向溢出均为 0；
对比度 导航 7.63 / 表头 7.05 / 单元格 17.41，全部达标。
424 passed；`vue-tsc` 与构建通过。

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.46
```
更新后按 Ctrl+F5 强制刷新。

---

## 0.4.45 — 2026-07-30

### 修复
- **移除导航的收起 / 展开功能**。图标栏现在是桌面端唯一形态，没有可切换的状态、
  也不再往 `localStorage` 记东西。手机端不受影响，抽屉仍是完整文字菜单。
- **补齐缺失的 SVG 图标**。上一版把导航改成图标栏后，几个控件仍在用 emoji 和字符
  （🌙 / ☀️ / ⏻ / ✕ / ☰ / 🔒 / « »）——它们在不同系统里渲染差异很大，
  和描边风格的图标摆在一起也不成套。新增 6 个内联 SVG：
  `moon` / `sun` / `logout` / `lock` / `close` / `menu`，
  主题切换、退出登录、锁定租户、关闭、移动端菜单按钮全部换成 SVG。
  已逐项核对：10 个导航项无一落到回退图标，侧栏内已无 emoji 残留。
- 顺手删掉 `Icon.vue` 顶部一行误留的 Python 文档字符串（`"""..."""`）。

### 功能
- **实例页的批量操作条改为底部悬浮**。原先勾选实例会在表格上方插入一张卡片，
  把整张表往下推——刚点的那一行从鼠标底下跑掉。这和你在租户页提过的是同一个毛病，
  那边修了、这边没修。现在它 `position: fixed` 固定在底部居中（并按图标栏宽度
  做了偏移补偿，使其对准内容区而不是整个窗口），脱离文档流，勾选时表格零位移。

### 关于验证的说明
这轮又一次撞上环境限制：**浏览器面板不合成帧**，所以动态插入的节点拿不到布局，
交互后的重排也量不到。因此：
- 图标栏宽度、图标是否齐全、标签可访问性、移动端抽屉——都是**加载后实测**的
- 悬浮操作条的「零位移」是**由构造保证**（`position: fixed` 本身即脱离文档流），
  并确认规则已进入构建产物，而非靠测量得出

### 维护
- 424 passed；`vue-tsc` 与构建通过

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.45
```
更新后按 Ctrl+F5 强制刷新。

---

## 0.4.44 — 2026-07-30

整体布局改版**第一步**：共用骨架。改这一处，12 个页面一起变。

### 功能
- **左侧导航改为图标栏**，宽度 232px → **68px**，内容区因此宽出 164px。
  - 图标下方悬停显示名称；文字虽然视觉隐藏，但保留在 DOM 中并配 `aria-label`，
    读屏与键盘不受影响
  - 分组标题（工作台 / 资源 / 系统）在收起态化为 1px 分隔线，不再占一整行
  - 顶部新增**固定展开**按钮，展开后恢复 236px 带文字的形态，
    选择记在 `localStorage`——想要图标还是文字，取决于你坐在哪块屏幕前，
    不该每次进来重选
  - 展开时是**撑开栅格**而不是浮层覆盖：盖住你正在看的表格，比表格窄一点更糟
  - 手机端不受影响，抽屉仍是完整文字菜单
- **整体走宽松**：页面留白 `clamp(1.25rem, 3.2vw, 2.75rem)`（原固定 ~18px）、
  卡片内边距 14/16px → 19/21px、卡片间距 12px → 20px、
  表格行高 8/12px → 11/14px、页面标题 18.9px → 24.5px，圆角 6/10 → 8/14。

### 说明：这一版没有做什么
你要的是「所有页面都改」，这里只交付了共用骨架。原因是**这个环境看不到渲染结果**——
截图不可用，而且浏览器面板不合成帧（`requestAnimationFrame` 不触发），
我只能验证加载后的静态状态，无法验证动效与交互后的重排。
在这种条件下一次性重排 12 个页面的内部结构，大概率给你一个又大又难审、
还不一定更好看的改动。**先看这一版的观感，再决定下一步往哪走。**

### 验证
- 收起态：侧栏 68px、内容区 1372px、标签隐藏但 `aria-label` 与悬浮提示齐全、
  分组线 1px、页面留白 38.5px
- 展开态：侧栏 236px、内容区 1204px、标签与分组标题正常
- 移动端 375：顶栏显示、抽屉文字完整、图标栏切换按钮隐藏
- 三种情况横向溢出均为 0；424 passed；`vue-tsc` 与构建通过

### 维护
- 移除了 `grid-template-columns` 上的过渡。最初是依据一次测量加的判断，
  后来发现那次测量因面板不合成帧而不可信；但结论仍然成立——
  对整个内容区的栅格做逐帧过渡在大表格页面上是实打实的重排开销，直接切换更好。
- 侧栏残留的一处 `backdrop-filter` 一并去掉，与 0.4.42 的其余表面保持一致。

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.44
```
更新后按 Ctrl+F5 强制刷新。

---

## 0.4.43 — 2026-07-30

重做登录页。

### 功能
- **登录页改为左右分幅**：左侧是固定深色的身份画布，右侧是表单。
  画布**不随主题切换**——这是进入面板的门槛页面，一个认定了自己长相的界面
  比一个"跟着设置随便变"的界面更像被设计过。表单侧仍跟随浅色 / 暗色。
- **字标用等宽字体**，大号、紧字距。与面板其余部分的排版逻辑一致：
  这里显示的是机器的名字，不是一句广告语。
- **读数区显示真实版本号**：登录前调 `/api/health`（该接口本就无需认证），
  显示你即将登入的那个构建版本。是真数据，不是装饰性文案。
  读取失败就不显示，不会在表单上方弹出一条与登录无关的报错。
- **只有一个动效**：一条细线在精密网格上缓慢扫过。它对应这个面板真正在做的事——
  耐心地等待容量。其余元素一概不动；`prefers-reduced-motion` 下直接隐藏。
- 两步验证码输入框改为等宽 + 字距展开 + 居中，像键盘上的六位数那样读。
- 文案一并收敛：副标题由「Oracle Cloud 多租户实例管理」改为
  「在一处管理跨账号、跨区域的服务器。」，底部提示的「OCI 私钥」改为「API 私钥」，
  与 0.4.42 的 README 改动保持一致。

### 验证
浏览器实测（截图在此环境不可用，故按可测量属性核验）：
- 桌面 1280 分栏 672/608，移动 375 单列堆叠，两种尺寸横向溢出均为 0
- 画布对比度：字标 19.4、说明 6.9、读数数值 14.4、读数标签 5.6
  （标签初版仅 3.73，10px 小字不达标，已按解算值提高到 0.55 透明度）
- 表单侧对比度 4.6 – 14.7
- 触控目标：输入框 40px、提交按钮 42px
- 登录 / 注册切换、两步验证、错误与提示、重定向白名单逻辑均未改动

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.43
```
更新后按 Ctrl+F5 强制刷新。

---

## 0.4.42 — 2026-07-30

前端一轮：一个性能修复 + 一次视觉方向调整。

### 性能
- **打开实例详情页少下载 74 KB（gzip）**。WebSSH 终端组件是静态导入的，
  于是 xterm.js（约 250 KB）被打进详情页主包——哪怕你只是去看一眼 IP 或点开关机，
  也要先把整个终端引擎下载完。改为按需加载后：
  `InstanceDetailView` **337.66 KB → 40.50 KB**（gzip 86.71 → 12.58 KB，减 88%），
  xterm 移入独立分片，只有真正切到 WebSSH 标签页才取。
  已实测：其他页面不再加载该分片，需要时也能正常取回。

### 视觉
原来的设计令牌里注释写着「Feishu-like blue」——直接借用了别家的品牌蓝，
外加一层毛玻璃。重新按这个面板自己的用途定了方向：

- **去掉毛玻璃**（9 处 `backdrop-filter`）。它模糊的正是这个面板最需要读准的东西——
  密集表格里的数字，而且让每次滚动都触发合成层重绘。改为不透明面板 + 1px 描边。
- **强调色改为靛紫** `#4f46c4`。理由不是好看：这个面板通篇在报告状态，
  绿 / 红 / 琥珀已被「运行 / 停止 / 告警」占满，靛紫是唯一不会被误读成状态的色相。
- **机器输出用等宽字体 + 表格数字**。面板显示的几乎都是机器生成的值（ID、地址、
  容量、计数），过去用比例字体渲染，数字在列间会左右跳动，没法竖着比。
  现在表格单元格统一 `tabular-nums`，标识符类值走等宽。
  未引入外部字体（CSP 只允许同源，且刚减下来的体积不该再加回去）。
- **额度条加 25/50/75 刻度**。它们是**有上限的配额**，不是进度条——
  刻度让你不做除法就能读出"用掉约四分之三"，而这正是这条 bar 被问到的问题。
  账号用量页与创建页共用同一套刻度，同一个量在哪都用同样的方式读。
- 圆角 10px → 6px，主按钮从渐变改为纯色（渐变里还残留着旧的品牌蓝硬编码）。

### 修复（本轮顺带发现）
- **卡片阴影全部失效**：`box-shadow` 列表里出现 `none` 是非法值，会让整条声明被丢弃，
  而 `--glass-highlight` 被插值进 10 处 `box-shadow`。改为透明阴影（合法的"不贡献"写法）。
- **对比度不达标**：状态徽章文字仅 2.63:1（「运行中 / 已停止」是这个面板最该读清的东西）、
  弱化文字 3.66:1、表头 3.38:1。逐项按 WCGA AA 解算色值后，浅色与暗色主题下
  全部达标（正文 17.4、表头 7.1、徽章 4.6、主按钮 7.0；暗色下分别为 15.9 / 10.2 / 7.7 / 7.0）。
- **键盘焦点几乎不可见**：全站只有一条 `:focus-visible` 规则（仅 `.copyable`）。
  现在所有可交互元素都有清晰焦点环。
- **新增 `prefers-reduced-motion` 支持**：此前完全没有处理。

### 说明
这个环境里截不了图，所以视觉部分我是按可测量的属性验证的（对比度、字体、
刻度规则、无横向溢出），**观感请你自己看一眼**，哪里不对告诉我调。

### 维护
- 424 passed；`vue-tsc` 与构建通过；改动集中在 `styles.css` 的令牌层，便于整体回退

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.42
```
更新后按 Ctrl+F5 强制刷新，否则浏览器可能仍用旧样式。

---

## 0.4.41 — 2026-07-30

### 修复
- **免费额度校验会对完全合规的配置显示告警**。你已经开了 2 台 AMD（E2.1.Micro）
  各 50G，再建 4 OCPU / 24G / 100G 的 ARM——总量正好卡满免费额度，创建也确实成功，
  面板却显示成有问题。

  原因不在校验算法（它返回的是「放行、零错误」），而在**用量快照的状态判定**：
  只要某项达到上限的 90% 就标成 `critical`，于是 E2.Micro 的 **2/2 = 100%**
  把整体状态拉成了告警。**但把免费额度正好用满，恰恰是免费套餐的正常终态**，
  不是故障，而且这个告警你做任何操作都消不掉。

  新增 `full` 状态区分「正好用满」与「已超额」：用满显示「额度已用满」，
  颜色与正常同系，不再拉高整体状态；只有**真正超出**才是红色的 `over`。
  账号用量页同步。

### 功能
- **创建实例的额度面板改成三段式**，不用再自己心算：每项资源直接列出
  **已用 / 本次占用 / 创建后剩余**，进度条也分成两段（已用 + 本次新增，
  本次那段用斜纹表示"尚未发生"）。超额时直接显示「超出 X」并标红。

- **新增「创建数量」**，一次创建多台相同配置的实例（1–8 台）。
  - 名称自动加后缀 `-1 -2 …`；只建 1 台时名称保持原样不加后缀
  - **额度按总量校验**：4 台 × 1 OCPU 放行，2 台 × 4 OCPU 直接拦下并说明
    「A1 规格 8 OCPU / 48 GB 已超过免费上限」。只校验一台再建 N 台，
    这个守卫对它最该防的场景就形同虚设了
  - 遇到失败**就地停止**，不再把剩余的调用打向同一个已经没容量的 AD——
    那只会白白消耗与抢机竞争的同一份速率配额。返回如
    「已创建 2/4 台，第 3 台起停止：Out of host capacity」，
    且**部分成功不算 ok**，界面会保留失败信息
  - 自动生成的 root 密码**每台一个**（各自写入自己实例的标签，实例列表照常显示），
    一台泄露不会连坐；你自己填的密码则整批复用
  - 容量重试（抢机）保持每次 1 台，选了数量>1 会明确报错而不是默默只建一台

### 维护
- `tests/test_launch_count.py` 新增 14 例（批量额度算术、名称后缀、部分失败、
  密码策略、抢机拒绝、数量边界）
- 424 passed；`vue-tsc` 与构建通过
- 在浏览器里用你的实际场景端到端验证过：1 台 4C/24G/100G 现在 `blocked=false`、
  整体状态 `ok`；2 台 / 4 台则按总量正确拦下

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.41
```
更新后按 Ctrl+F5 强制刷新一次。

---

## 0.4.40 — 2026-07-30

租户页两处体验修复：列表按添加顺序排，操作按钮点了不再让画面往下跳。

### 功能
- **租户列表改为按添加顺序排列**（先来后到，从上往下）。原先按名称字母排序，
  改个名字就会让一行跳到表格的另一个位置。副区仍然嵌在它的主区下面。

### 修复
- **点击操作按钮不再让画面下移**。原因有三处，都已消除：

  1. **顶部的绿色提示条**：等级查询 / 锁定为默认 / 密码到期查询 / 删除 等操作
     都会在表格**上方**插入一条提示，把整张表往下推——刚点的那一行就从鼠标底下
     跑掉了。现在改用面板本来就有的 toast（固定定位在底部，不占布局空间）。
  2. **「🔒 默认」徽章**：过去锁定后才渲染，一出现就把名称列撑宽，名称列一宽
     操作列就变窄，六个按钮多折一行——**整张表每一行都高了 21px**。
     现在徽章始终占位，只切换可见性，锁定变成纯重绘。
  3. **密码到期结果**：过去查完在「状态」格里多出一行，把下面所有行往下推。
     现在是独立的一列，宽度固定，查询前显示等宽的隐藏占位徽章。

  修完实测（对构建产物、真实点击）：表格高度变化 0、四行位移全部 0、
  各列宽度变化在 ±0.05px 的亚像素范围内。

  表单保存失败的错误提示仍保留为常驻的红框——那种消息需要能停在那儿读完，
  而且只在提交表单时出现。

### 维护
- 这轮是靠在浏览器里量 `getBoundingClientRect()` 逐项定位的。
  值得记一笔：我最初为「🔒 默认」加的 `white-space: nowrap` **加重了**问题
  （名称列 99.8px → 170.1px，每行 +21px），是实测把它揪出来的——
  `vue-tsc` 和 `npm run build` 都不会发现这种问题。
- 410 passed；`vue-tsc` 与构建通过

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.40
```
更新后按 Ctrl+F5 强制刷新一次，确保加载的是新前端。

---

## 0.4.39 — 2026-07-30

修复**添加租户返回 500**。根因在 0.4.36，不是那两版安全改动。

### 修复
- **添加租户失败（500 Internal Server Error）**：0.4.36 删掉预算告警和每日出网
  检查时，把 `Tenant` 的 `budget_monthly_usd` / `budget_notified_month` /
  `egress_notified_month` 三个字段一起从模型里删了。这三列是 `nullable=False`，
  而 SQLAlchemy 的 `default=` 是**在 Python 侧**生效的——数据库里的列并没有默认值。
  模型属性一删，INSERT 就不再带这三列，而 `_ensure_schema()` **只加列不删列**，
  所以它们仍然存在于每一个升级上来的数据库中，于是 INSERT 违反非空约束。

  **为什么只有"添加"坏了**：UPDATE 不需要为它没在改的列提供值，只有 INSERT 需要
  补齐全部非空列。所以列表、编辑、删除、创建实例全都正常，唯独新增租户挂掉，
  看起来像是租户功能本身出了问题。

  也解释了为什么测试全绿却没拦住：测试库是用**当前模型**新建的，那三列压根不存在。

  修法沿用这个模型里已有的惯例（`password_changed_at` 等几个列就是这么留的）：
  保留映射并标注为 legacy。**不新建迁移、不动你的数据**，升级完直接就好。

- **数据库拒写不再是一句空白的 500**：租户的两条写入路径过去没有任何错误处理，
  数据库一拒绝就是浏览器里一句没有内容的 `Internal Server Error`，原因只存在于
  容器日志的 traceback 里——这次就是这样，定位它需要去翻服务器。现在会把数据库
  给出的实际原因显示在界面上，并提示连同 `/api/health` 的版本号一起反馈。

### 维护
- 新增 `tests/test_legacy_columns.py`（4 例）：先造一个 0.4.36 之前的表结构，
  再用当前代码 INSERT。另有一条守卫断言直接检查这三列仍被映射，
  下次谁再删掉会立刻在测试里失败并说明原因，而不是在生产上炸。
  已验证这些测试确实有牙齿——去掉修复后 4 例全部以生产同样的报错失败。
- `CLAUDE.md` 记下这条规则：功能可以删，但只要部署中的数据库还有那一列，
  就不能取消映射，除非同时写迁移把列删掉。
- 410 passed

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.39
```

---

## 0.4.38 — 2026-07-30

修复 0.4.37 的回归：**添加租户等所有写操作可能被自己的防 CSRF 校验误拦**。

### 修复
- **Origin 校验没有归一化默认端口（0.4.37 引入的回归）**：上一版的比较是
  `host[:port]` 字面串相等。浏览器的 `Origin` 从不带 scheme 的默认端口
  （只会是 `https://panel.example.com`，绝不会是 `:443`），而反代传过来的
  `Host` 可能带着它——nginx 的 `$http_host` 带、`$host` 不带。两者一旦不一致，
  **每一个 POST/PUT/PATCH/DELETE 都会返回 403**，表现就是"添加租户失效了"。

  已确认会被误拦的三种普通配置：
  - `Origin: https://域名` + `Host: 域名:443`
  - `Origin: http://域名` + `Host: 域名:80`
  - 浏览器用非标端口（`https://域名:8443`）而反代用 `$host` 去掉了端口

  现在按 scheme 归一化默认端口再比对主机名；反代**没给**端口时不拿端口否决
  （`$host` 会去掉端口，拿一个代理压根没传的信息把运维锁在门外是错的），
  主机名匹配才是这个校验的安全价值所在。`OCIBOT_CORS_ORIGINS` 里的条目
  同样走归一化，所以写成 `https://域名` 或 `https://域名:443` 都能匹配。

  真实的端口差异（`:9999` vs `:8443`）、跨域、同域不同子域、
  `Origin: null` 仍然一律拦住 —— 这些都有回归测试钉住。

### 已核对无问题
排查时把租户添加的两条路径都用真实格式的 OCID / fingerprint / PEM 跑通了：
手动表单 `POST /tenants` 返回 201，粘贴导入 `POST /tenants/parse` + `/tenants/import`
返回 200 / 201，带反代形态的 `Origin` 也正常。前端 `openCreate` / `importPaste` /
`saveManual` 逻辑完好。所以后端与前端本身没有缺陷，问题只在上面那处端口比对。

### 维护
- `tests/test_origin_guard.py` 增加 7 例（默认端口归一化、IPv6 字面量不被冒号切错、
  允许列表条目同样归一化），共 22 例
- 406 passed

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.38
```
若更新后写操作仍然 403，请把这行的输出发我，它会直接打印出实际收到的
`Origin` / `Host` / `X-Forwarded-Host`：
```bash
docker logs ocibot-api-1 --tail 80 2>&1 | grep "rejected cross-site"
```

---

## 0.4.37 — 2026-07-30

全功能自检 + 第 10 轮安全审查。修掉 2 个安全问题（1 中危、1 中危可用性），
清理 12 处死代码；确认除抢机外没有任何自发请求 Oracle 的代码路径。

### 修复（安全）
- **跨站写请求可借用你的会话（CSRF，中危）**：所有 POST/PUT/PATCH/DELETE 过去
  只靠会话 Cookie 授权，唯一屏障是 `SameSite=Lax`。这不够——
  - `OCIBOT_COOKIE_SAMESITE=none` 是受支持的配置，一开就完全没有防护；
  - 即使是 `Lax`，Cookie 的作用域是**站点**而不是来源：面板在
    `panel.example.com`，攻击者拿到同域下任意一台 `blog.example.com`，
    从那里发出的跨来源 POST 依然会带上你的登录态。

  WebSSH 的 WebSocket 早在第 4 轮就自己校验了 `Origin`，REST 侧却一直没有。
  现在两边共用同一份策略（`web/backend/origin_guard.py`），不会再各走各的：
  写请求的 `Origin` 必须匹配 `Host` / `X-Forwarded-Host` / `OCIBOT_CORS_ORIGINS`。
  没有 `Origin` 的请求（curl、脚本）照旧放行——它们本来就不带受害者的 Cookie。
  读请求（GET/HEAD）和 CORS 预检（OPTIONS）不受影响。

  新增 `OCIBOT_ORIGIN_CHECK`（默认 `1`）。**这是应急开关，不是常规选项**：
  只有当反代把 `Host` 改写成上游名、又不发 `X-Forwarded-Host` 时才会误拦，
  正确做法是把面板公开地址加进 `OCIBOT_CORS_ORIGINS`。403 的返回文案会直接
  把这两条路写出来——否则整站 POST 全挂，光看现象根本猜不到原因。

- **审计日志可被未认证流量撑爆磁盘（中危，0.4.34 引入）**：登录审计会记录失败
  尝试，包括针对**不存在**用户名的尝试。也就是说未登录的人就能往表里写行，
  写多少由对方决定，而这张表原先没有任何上限。面板暴露在公网上被撞库时，
  `audit_logs` 会一直长到磁盘写满，Postgres 和整个栈一起倒。
  新增 `OCIBOT_AUDIT_RETENTION_DAYS`（默认 180）与 `OCIBOT_AUDIT_MAX_ROWS`
  （默认 50000），两条同时生效：只留时间窗挡不住窗口内的洪峰，只留行数上限
  又会让冷清的实例永久堆着老数据。清理跑在 Worker 心跳里（**只碰数据库、
  绝不碰 Oracle**），所以 `OCIBOT_WORKER_BACKGROUND_OCI=0` 时同样生效；
  每小时一次，避免心跳每几秒就 `COUNT(*)` 一遍。

### 主动请求 Oracle 的排查结果
逐个入口清点完毕，**除抢机外没有自发请求**：
- API 进程启动钩子（`lifespan`）：建表、提升首个管理员、检查弱密钥 —— 无 OCI
- Worker 只有两个阶段：心跳（数据库，本版起附带审计清理）+ 抢机；
  抢机在没有启用任务时 `candidates` 为空，一次调用都不会发出
- 全部 OCI SDK 调用都收口在 `app/oci_client.py`；进程内只有两个会话管理器
  （`oci_bridge`
  给请求用、`worker` 给抢机用）
- API 进程里的其余后台线程都不碰 OCI：WebSSH 的三个协程只走 SSH，
  自更新线程只跑 docker compose
- 唯一的"响应返回后仍在跑"的 OCI 调用：`launch_service.schedule_post_launch_adjustments`
  —— 你点了创建实例、且引导卷性能不是默认 10 VPU 时，后台线程等卷 hydration
  完成再改性能。这是你那次点击的延续，不是自发轮询；代价是 API 重启会让
  正在等待的那次调整无声丢弃（已知，不改：改成同步会让创建请求阻塞十分钟）

### 安全审查覆盖面（本轮无发现的项）
无原始 SQL / `text()`、无 `eval` / `exec` / `pickle` / `yaml.load`、无 `shell=True`；
前端全无 `v-html` / `innerHTML`（审计日志里攻击者可控的 User-Agent 因此只会被
转义显示）；每个 REST 路由都注入了用户依赖，8 个管理端点全部走 `get_admin_user`；
审计查询里非管理员只能看到自己的行；租户输出模型只暴露 `has_private_key`，
私钥不出现在任何响应里；备份恢复用 `zf.open(member)` 逐项读取，不落盘解压，
无 zip slip；SPA 兜底路由拦 `..` 且用 `relative_to` 收敛到 dist 目录；
JWT 算法硬编码 HS256（不可被环境变量降级）、带 `exp`、改密递增 `token_version`；
登录限速在校验口令**之前**执行，配合 6 位动态码使 TOTP 爆破不可行。

### 修复（死代码）
`pyflakes` 全量扫描后清理 12 处：0.4.36 删功能留下的 `models.py` 未用 `Float`、
`jobs.py` 未用 `re`、两个测试文件的未用导入，以及历史遗留的 `app/free_quota.py`
死变量 `disk_rem`、`oci_client.py` 的 `tempfile`、`url_safety.py` 的 `Optional`、
`webssh.py` 的 `select`，和 3 处只读的 `nonlocal` / `global` 声明。
逐个确认过后者不是"本想赋值却漏了"的真 bug——真正的赋值都在别的作用域里。

### 维护
- 新增 `web/backend/origin_guard.py`（入站跨站策略单一来源）
- 新增 `tests/test_origin_guard.py`（15 例）、`tests/test_audit_retention.py`（11 例）
- `tests/test_web_hardening.py` 的 WebSocket 用例改打新接缝
- 399 passed

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.37
```
两项修复都是**默认生效**的，`web/.env` 不用改。若更新后所有写操作都返回
403「跨站请求被拒绝」，说明反代改写了 `Host`：
```bash
echo 'OCIBOT_CORS_ORIGINS=https://你的域名' >> ~/ocibot/web/.env
cd ~/ocibot && docker compose up -d
```

---

## 0.4.36 — 2026-07-29

### 移除（用不上的功能，按使用者要求删除）
- **定时开关机**：整套删除 —— `ScheduleJobRow` / `ScheduleRun` 模型、
  `/jobs/schedules*` 四个接口、Worker 的 `tick_schedules`、任务中心的整块界面与运行历史
- **预算告警**：删除 Worker 的每日 Usage API 检查、`Tenant.budget_monthly_usd`
  与 `budget_notified_month`、租户表单里的预算字段、备份的相关字段。
  **「账号用量」页的费用查询保留**——那是你点了才读，不是后台告警
- **每日自动出网流量检查**：删除 Worker 的每日 Monitoring 查询与
  `Tenant.egress_notified_month`。**面板里 10TB 出网流量的进度条保留**——
  那是你打开页面才读，闲置时零开销
- 推送事件只剩 `capacity` 一项（原为 capacity / schedule / budget）

### 保留
- **抢机（容量重试）**：使用者明确要留。它只在存在任务时才请求 Oracle，
  没建任务就零调用，所以 `OCIBOT_WORKER_BACKGROUND_OCI` 保持 `1` 也不产生任何请求
- `app/scheduler.py` 里的旧 `ScheduleJob` / `JobStore` 未动 ——
  `app/runtime_paths.py` 用 `JobStore` 做数据目录可写性探测，那部分仍在生效

### 结果
Worker 现在只有两个阶段：心跳（只写数据库）+ 抢机（仅在有任务时）。
**没有抢机任务时，面板对 Oracle 的请求 100% 由你的点击触发。**

### 数据库
旧表 `schedule_jobs` / `schedule_runs` 和那三个列不会被自动删除
（自动迁移只加不减），留在库里无害。想彻底清掉可手动 `DROP TABLE`。

### 维护
- 删除 `tests/test_schedule_single_fire.py`，清理其余测试中相关用例
- 373 passed；另外把构建产物实际跑起来确认渲染正常（任务中心的模板做了大面积删除）

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.36
```
既然只剩抢机，可以把开关打开备用（不建任务就不会有请求）：
```bash
sed -i 's/^OCIBOT_WORKER_BACKGROUND_OCI=0/OCIBOT_WORKER_BACKGROUND_OCI=1/' ~/ocibot/web/.env
cd ~/ocibot && docker compose up -d
```

---

## 0.4.35 — 2026-07-29

### 功能
- **新增总开关 `OCIBOT_WORKER_BACKGROUND_OCI`**：设为 `0` 后 Worker
  **完全不再主动请求 Oracle**，面板只在你实际操作时才调 OCI API。
  被关掉的四项：抢机重试、定时开关机、预算告警、出网流量检查
  - Worker 进程照常运行、心跳照常写（**面板不会误报 Worker 离线**），只是不做 OCI 工作
  - **默认仍为 `1`**：改默认会让所有现有部署的抢机任务静默失效
- **关闭状态会明确显示，不静默失效**：抢机和定时开关机本身就是靠后台请求实现的，
  关掉后它们不报错、只是永远不触发。所以：
  - 侧边栏显示「后台 OCI 请求已关闭」，与「Worker 离线」区分开（后者是故障，前者是配置）
  - 任务中心页顶部直接说明「本页任务不会执行」，并给出恢复方法
  - Worker 启动时打一条 WARNING 日志
  - 一个永远停在 idle 的抢机任务，如果没人告诉你为什么，比报错更难查

### 背景
起因是被问到「有没有我不开网页也会请求 OCI 的功能」。核查结果：
抢机 / 定时任务 / 预算告警都需要你先创建对应的东西才会跑，
但 **0.4.19 加的出网流量检查是无条件的**——每个启用租户每天 1 次 Monitoring 查询。
这一项当时没跟使用者确认过，现在连同其余三项一起提供了关闭方式。

### 维护
- 新增 `tests/test_background_oci_switch.py`（4 项）：开时四个阶段全跑、
  关时只剩心跳、默认必须为开、状态接口暴露该标志
- `web/.env.example` 与 README 环境变量表同步

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
```
要关闭后台请求，在 `web/.env` 里加一行再重启：
```bash
echo 'OCIBOT_WORKER_BACKGROUND_OCI=0' >> ~/ocibot/web/.env
cd ~/ocibot && docker compose up -d
```

---

## 0.4.34 — 2026-07-29

### 修复（重要：撞库记录一直看不到）
- **审计页看不到针对不存在用户名的失败登录**。这些事件一直在写，但它们的
  `owner_id` 是 `NULL`（没有账号可归属），而 `/api/audit` 只返回 `owner_id = 当前用户`
  的行 —— 于是面板记录了每一次撞库尝试，却一条都不显示。
  管理员现在同时能看到 `owner_id` 为空的匿名事件；非管理员仍只看自己的

### 功能（登录审计）
- **失败原因写清楚**，不再只有一个笼统的"登录失败"：
  - `用户名不存在` —— 拿泄露名单在乱撒
  - `密码错误` —— 对方知道这个账号真的存在
  - `两步验证码错误（密码是对的）` —— **对方已经拿到密码**，只是被 2FA 挡住
  - `尝试过于频繁，已拦截` —— 之前完全没有记录，而这是自动化脚本最明显的特征
  - `账号被禁用`
- **密码错误若命中真实账号，记录归属到该账号**，这样账号主人不必是管理员也能看到
  针对自己的尝试
- 记录来源 IP（可点击复制）与 User-Agent
- 新增「只看登录」筛选
- 出现可疑迹象时页面顶部才提示（2FA 失败 / 被限流 / 失败次数过多），
  不做成一条永远挂着、久而久之被无视的横幅

### 明确不做：不记录填写的密码
你要的是"看谁在撞库"，这个目标已经由上面的记录满足。但**提交的密码不会被存下来**：
- 失败登录里的密码，绝大多数是**别人泄露的真实凭据**。存下来等于在审计表里
  建一个明文凭据库 —— 而这张表会发到浏览器、存在 Postgres 里、进每一个备份 ZIP
- 其中出现概率最高的一条，是**你自己打错一个字符的管理员密码**
- 而且它对识别撞库**没有任何帮助**：判断靠账号 + IP + 时间 + 失败原因，这些都有了

### 维护
- 新增 `tests/test_login_audit.py`（7 项），其中一项专门断言
  **提交的密码不出现在审计表的任何字段里**

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.34
```
之前记录的失败登录**更新后立刻就能看到**（数据一直都在，只是被过滤掉了）。

---

## 0.4.33 — 2026-07-29

### 功能
- **实例列表显示 root 密码**：密码模式创建的实例，密码在创建时就已写入实例的
  freeform 标签 `ocibot_root_password`（这部分早已存在），但**从来没有读回来过** ——
  所以密码只在创建那一刻显示一次，之后就找不回了。现在列表新增「root 密码」一列
  - **默认打码**，点一下显示明文，再点一下复制。列表是常开着、也常被截图的页面
  - 密钥模式创建的实例没有这个标签，显示 `—`
  - 读取不产生额外的 OCI 调用：标签本来就随实例对象一起返回
  - **未加入 CSV 导出**：导出文件会明文落在下载目录里，比留在面板中更容易泄露

### 需要知道的两件事
- **标签是明文的**。任何对该租户有实例读权限的人（OCI 控制台、CLI、API、
  你授权的任何 IAM 用户）都能看到它，不只是这个面板。单人使用的账号无所谓，
  多人共用的账号要掂量
- **标签记录的是「创建时设置的密码」**。你若在系统内 `passwd` 改过，标签不会跟着变，
  显示的会是旧值

### 维护
- 新增 `tests/test_instance_root_password.py`（4 项）：标签取值、密钥模式为空、
  无标签不报错、非字符串标签值不破坏列表

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.33
```
注意：**已有实例只有当初是密码模式创建的才会有值**，之前用密钥模式创建的不会凭空出现。

---

## 0.4.32 — 2026-07-29

### 变更（租户页操作栏）
- **移除「测试连接」按钮**（添加租户时的「保存后自动测试连接」勾选项保留，那是另一处）
- **「识别等级」改名为「等级查询」**
- **「关闭强制改密」与「查改密状态」合并为「密码到期查询」**：
  点一次先读 `defaultPasswordPolicy` 的天数，若仍设着有效期就自动关闭再读一次。
  按钮提示里写明了会执行关闭 —— 一个叫「查询」的动作会改 Oracle 配置，
  不该藏着不说
- **结果只显示 `defaultPasswordPolicy` 的数值**：`密码到期 未设置` 或 `密码到期 120 天`。
  去掉了之前的结论徽标、摘要文字和其余策略行 —— 判断有没有关掉，看这一个数字就够
- **操作按钮固定单行**：按钮数从 8 个减到 6 个，并禁止换行；
  该列过窄时由表格横向滚动承担，不再把每一行撑成两倍高

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.32
```

---

## 0.4.31 — 2026-07-29

### 修复
- **改密状态可能显示错误的策略**（重要）：一个 Identity Domain 里通常有多条密码策略，
  而**控制台登录实际生效的是 `defaultPasswordPolicy`**。上一版在无法确定用户适用策略时
  取"最严格的一条"，于是可能报出 `StandardPasswordPolicy` 的数字 ——
  那是 Oracle 的内置模板（面板也改不了它，PATCH 会 403），
  和用户在 Oracle 控制台里看到的值对不上。
  选取顺序改为：**用户自身的适用策略 → `defaultPasswordPolicy` → 其余策略中最严格的**，
  内置模板一律排除
  - 直接后果：关闭强制改密成功后，`defaultPasswordPolicy` 已无有效期而模板仍写着 N 天，
    现在正确显示「永不过期」，而不是被模板的值盖掉

### 功能
- **直接显示策略原始值**：查改密状态时列出每条策略的天数，
  如 `defaultPasswordPolicy=未设置`、`StandardPasswordPolicy=120 天（模板）`。
  判断关闭是否生效时，要看的就是这个数字本身，而不只是面板给出的结论。
  生效的那条会高亮，内置模板标注「模板」

### 维护
- `tests/test_password_expiry.py` 增加 5 项：默认策略优先于模板、
  关闭后模板值不再盖住结论、用户自身策略仍最优先、原始值输出、模板不参与兜底

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.31
```

---

## 0.4.30 — 2026-07-29

### 性能（接上一版，按实测网络瀑布图继续优化）
用户提供的浏览器网络面板显示：**60.4 kB 的主包 169ms 到位，而 1.3 kB 的小分片要 380ms**
—— 瓶颈是每个请求的往返延迟，不是传输量。真正花时间的是**串行层数**：
文档 759ms → 主包 169ms → 布局/视图分片 380ms → 接口 700ms，叠起来正好约 2 秒。
所以这一版压的是层数和请求数，不是体积：

- **布局与首页视图改为随主包一起下载**（原本是动态导入）。它们几乎每次访问都要用，
  而动态导入意味着浏览器必须先下载并解析主包才能*发现*要请求它们 ——
  白白多一整个往返。代价是入口包 +7 kB（gzip 61→68 kB），在高延迟链路上远比一次往返便宜
- **CSS 不再按视图切分**：原来每个视图一个 1–3 kB 的样式文件，各自一次请求
- **合并过小的公共分片**（0.7 kB 的 logo、1.7 kB 的 store 之类）：低于这个体积时，
  单独成文件在往返上的开销远大于它省下的重复
- 结果：构建产物 **29 个文件 → 11 个**，首页 HTML 只引用 1 个 JS + 1 个 CSS，
  串行层数从 4 层降到 3 层

### 验证
- 除类型检查与构建外，**实际启动构建产物在浏览器里打开确认渲染正常**——
  静态导入改的是路由启动路径，白屏这类问题类型检查抓不到

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.30
```

---

## 0.4.29 — 2026-07-28

### 性能（页面卡顿）
起因：用户反馈点栏目要等 3 秒以上。实测服务器侧 `/api/health` **9 毫秒**、
4 核 23G 内存只用了 1.5G、容器 CPU 全在 2% 以下 —— 服务器不是瓶颈，
时间花在浏览器到服务器的往返上。两处对症改动：

- **静态资源改为不可变缓存**：`/assets` 下是 Vite 输出的内容哈希文件名，
  之前没有任何 `Cache-Control`，`StaticFiles` 只发 etag/last-modified，
  于是**每次打开页面，十几个 JS/CSS 都要各跑一次「有没有变」的往返**，
  延迟高的时候这就是渲染前的绝大部分等待。现在发
  `max-age=31536000, immutable`，浏览器直接用本地副本，一个来回都不跑
- **`index.html` 明确设为 `no-cache`**：它不带哈希、指向当前这套资源文件名，
  被缓存住会在更新后继续加载旧版本 —— 也就是 README 排查表里那条
  「更新后版本不变 → 浏览器强刷」。配合上一条，这个问题不该再出现
- **空闲时预取各栏目的代码**：页面是按栏目分包懒加载的，所以**第一次**点某个栏目
  要先下载它的 JS 才能渲染。现在进面板后浏览器空闲时按顺序预取，
  点栏目变成即时切换。走 `requestIdleCallback`，不与当前页面抢带宽；
  配合上一条，这个下载每次发版只发生一次，而不是每次访问

### 维护
- `tests/test_web_hardening.py` 增加缓存头断言（index 必须 `no-cache`，
  且该规则不能漏到 `/api/` 上）

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.29
```
更新后**首次**打开仍需下载新版本资源，之后才会明显变快。

---

## 0.4.28 — 2026-07-28

### 功能
- **显示控制台密码的真实到期时间**：租户页新增「查改密状态」，直接从 Oracle 读取，
  用来判断「关闭强制改密」到底有没有生效 —— 那个操作返回成功只代表 PATCH 被接受了，
  之前没有任何办法确认结果
  - 显示 `永不过期` 或 `2026-11-25 到期（还有 100 天）`
  - 日期是**算出来的真实日期**：策略里的 `passwordExpiresAfter` 天数
    + 该用户自己的 `lastSuccessfulSetDate`（密码上次设置时间）。
    只看策略天数是答不出「我的密码哪天过期」的
  - 点「关闭强制改密」成功后会**自动回读一次**，直接看到结果
  - 读不到用户的改密时间时，明确写成「N 天后过期（无法算出具体日期）」，
    而不是显示成永不过期
  - 用户被标记 `cantExpire` 时优先按永不过期处理
  - 无法确定适用哪条策略时，**取最严格的一条**（天数最小）——
    把「其实会过期」说成「永不过期」是会让人丢账号的方向

### 修复（写测试时发现）
- 策略里 `passwordExpiresAfter` 是非数字时，挑选策略的 `min()` 会抛 `ValueError`，
  导致整个状态读取失败。现在按「无有效期」处理，单条异常不影响其余策略

### 维护
- 新增 `tests/test_password_expiry.py`（16 项）：永不过期 / 真实日期计算 / 已过期 /
  缺少改密时间 / 时间格式错误 / 多策略选取 / 脏数据

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.28
```

---

## 0.4.27 — 2026-07-28

### 功能
- **锁定默认租户**：租户页每行新增「锁定为默认」。锁定后，**实例 / 存储 /
  创建实例 / 账号用量** 四个页面进入时都会自动选中它，不用每页再挑一次
  - 侧边栏常驻一个 `🔒 租户名` 标记，点 ✕ 即可取消 —— 一个会在四个页面
    默默生效的默认值，如果没有随处可见的提示和退出口，就会变成一个「为什么总是它」的谜
  - **锁定不会锁死下拉框**：页面内照样能临时切换到别的租户，锁定只决定「进入时选谁」
  - 优先级：URL 里的 `?tenant=` > 锁定的租户 > 列表第一个。深链必须最优先，
    否则从实例点进它的存储页会莫名其妙换成另一个租户
  - 锁定的租户被删除 / 禁用后会自动解除，不会留下一个指向不存在租户的死锁定；
    租户改名后侧边栏标记也会跟着更新

### 说明
- 该偏好存在浏览器本地（localStorage），不占用服务端接口，也不需要数据库改动 ——
  代价是换浏览器 / 换设备需要重新锁一次

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.27
```

---

## 0.4.26 — 2026-07-28

### 文档
- 新增 [docs/REDEPLOY.md](docs/REDEPLOY.md)：换机器重新部署的完整步骤，
  从旧机器导出到新机器可用，全程复制粘贴
  - **迁移走加密备份 zip，而不是搬 `web/.env`**：新机器生成自己的密钥，
    导入时用新密钥重新加密，少一件容易搞错且一旦搞错就全盘解不开的事
  - 明确写出备份**不含**什么（定时任务、通知渠道、抢机任务），并让用户在拆旧机器前
    先截图记下来
  - **Oracle 云放行 80/443 单列一步**：OCI 默认只开 22，且控制台安全列表与实例内
    iptables **两处都要改**，只做一处仍然打不开——这是最容易卡住且报错完全指不到
    原因的地方
  - 完工检查含「公网直连 8000 必须失败」，这是唯一能区分「配了」和「真的隔离了」的检查
  - 收尾提醒备份新机器的 `web/.env`，并把「确认新机器正常后再删旧机器数据卷」写进流程
- README 安装章节链到该文档

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.26
```

---

## 0.4.25 — 2026-07-28

### 功能
- **新增 `scripts/setup-proxy.sh`：一键配置域名 + HTTPS 反代**
  ```bash
  cd ~/ocibot && bash scripts/setup-proxy.sh panel.example.com
  ```
  它会自动完成：按镜像找到 Nginx Proxy Manager（没有就装一个，且把它自己的
  管理后台 81 端口只绑回环）、接入面板的 Docker 网络、按反代场景改好 `web/.env`
  （Cookie 仅 HTTPS、端口只监听回环、限流按真实客户端 IP）、重启并自检，
  最后打印 NPM 网页里要填的字段。用户只需在网页上填几个框
- 自检含一条**「公网还能不能直连 8000」**——这是唯一能证明隔离真的生效的检查
- 改 `web/.env` 前会带时间戳备份，结尾打印还原命令

### 修复（写脚本时测出来的）
- 域名统一转小写后再写入 `OCIBOT_CORS_ORIGINS`。该项是**精确字符串比对**，
  而浏览器发出的 Origin 主机名永远是小写 —— 用户输入 `Panel.Example.com`
  会导致比对不上
- 文档里检测反代容器的命令按名字 grep `nginx-proxy-manager`，
  而官方 compose 的服务名是 `app`，容器实际叫 `npm-app-1` 之类，
  正常安装反而搜不到。改为按镜像匹配

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.25
```

---

## 0.4.24 — 2026-07-28

### 文档
- 新增 [docs/NPM-REVERSE-PROXY.md](docs/NPM-REVERSE-PROXY.md)：用 Nginx Proxy Manager
  挂域名 + HTTPS 的逐步配置，面向「装好就想用、不想研究安全参数」的场景
  - 明确了一个会直接踩坑的点：README 让设 `OCIBOT_BIND=127.0.0.1`，
    但**反代若也是 Docker 容器，它访问不到宿主机的回环地址**，照做会 502。
    正确做法是让反代接入面板的 Docker 网络、按容器名转发，端口则完全不对外
  - 给出完整 `.env` 取值与每个参数「不设会怎样」的说明
  - NPM 三个标签页的逐项填法，含**必须打开 Websockets**（否则网页终端连不上，
    但其余功能正常，极易误判成终端坏了）与 `client_max_body_size 50m;`
    （否则备份导入 / 对象上传会以看不懂的方式失败）
  - 完成后 4 条自检命令，含「公网直连 8000 应该失败」这条验证隔离是否真的生效
- README 安全清单补上同一处提醒并链到该文档

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.24
```

---

## 0.4.23 — 2026-07-28

### 安全（第 9 轮审查：单独审 `self_update.py`）
- **主密钥被写进日志**（重要）：更新时构造的 `docker run` 命令里带着
  `-e OCIBOT_MASTER_KEY=<明文>`（还有 JWT 密钥、数据库密码），而 `_run_cmd`
  会把完整命令 `log.info` 出来。**于是每次更新都把解密所有 OCI 私钥的主密钥
  明文写进了 API 日志**，同时在宿主机 `ps` 里也能看到。
  能读日志（日志采集、`docker logs`、导出的排查包）就等于拿到所有租户私钥。
  现在改为按**变量名**传递（`docker run -e KEY`，值由 docker CLI 从自身环境读取，
  `_run_cmd` 本来就转发了 `os.environ`），日志行另做脱敏
- **`web/.env` 备份残留在 `/tmp`**：`git reset --hard` 前会把含主密钥 / JWT 密钥 /
  数据库密码的 `.env` 复制到 `/tmp/ocibot.env.backup.<pid>`，但**只有成功路径才删除**。
  reset 失败等提前返回的分支会把它永久留在 /tmp。现在改为**只在内存里暂存**，
  不再落盘；若需重建该文件则以 0600 写入
- **管理员可见的更新日志未脱敏**：`log_tail` 会存库并由 `GET /api/admin/update` 返回，
  而它由命令原始输出拼成，docker/compose 报错可能回显插值后的密钥。现在写入时即脱敏

### 明确记录为「接受的风险」
- **更新不校验代码完整性**：`git fetch` + `reset --hard origin/<branch>` 只信任
  TLS 和 GitHub，不验签，也不比对检查时 API 返回的 SHA 与实际检出的 SHA。
  能对配置的仓库提供不同内容的人（仓库账号失陷、宿主机信任了恶意 CA、
  或把 `OCIBOT_UPDATE_REPO` 指向别处）可在下次更新时获得宿主机 root。
  彻底解决需要签名标签 + 固定验证密钥。当前缓解：`OCIBOT_UPDATE_ENABLED` 默认关闭、
  仅管理员可操作且写审计。**不用面板内更新的运维请保持关闭，改用 SSH 更新**

### 维护
- `tests/test_self_update.py` 原有断言 `"OCIBOT_MASTER_KEY=real-key" in joined`
  **正是在固化上述缺陷**，已改写为断言密钥值不出现在命令行；
  另新增日志脱敏、管理员日志脱敏、短值不误伤三项测试
- `web/AUDIT.md` 记录第 9 轮

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.23
```

---

## 0.4.22 — 2026-07-28

### 修复（第 8 轮审查，针对 0.4.16–0.4.21 新增代码）
- **副区租户在主租户轮换密钥后静默失效**（重要）：副区行保存的是主租户凭据的副本，
  改主租户私钥 / 指纹 / OCID 时没有同步，副区会一直用旧密钥认证直到报 401
- **副区里改规格 / 扩容磁盘 / 创建块存储被免费额度守卫误拦**：副区 `account_tier`
  继承自主租户，为空时守卫按"硬拦"处理，于是用户明确付费的操作被
  「超过免费上限」挡下 —— 而副区根本没有免费额度，且用量快照只统计本区域
- **Worker 每日检查失去了节流**：租户间的 1.5 秒间隔只在设了预算时才生效，
  而 0.4.19 的出网流量检查无论有没有预算都会调 Monitoring。没设预算的用户
  会让所有租户的查询连续打出去 —— 正是该间隔要避免的多账号突发
- **开通副区在 PostgreSQL 上可能 500**：副区租户名 `主租户名 · 地区` 拼出来可能超过
  `VARCHAR(128)`。SQLite 静默溢出，PostgreSQL 直接报错 —— 而此时 Oracle 那边
  **不可撤销的区域订阅已经完成**，用户看到的是"操作后报 500"
- **会话缓存在提交前失效**：`update_tenant` / `delete_tenant` 先 `drop_session`
  再 commit，并发请求可能用未提交的旧数据重建并重新缓存，使旧凭据活过本次更新

### 维护
- `web/AUDIT.md` 新增第 8 轮记录，含**本轮未覆盖**的范围说明
- 新增副区存储守卫测试（副区跳过免费上限 / 仅免费模式仍拦截 / 主区不受影响）
  与超长租户名回归测试

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.22
```

---

## 0.4.21 — 2026-07-28

### 回退
- **撤销 0.4.20 的「进入页面自动加载」**，恢复为手动点「刷新 / 加载配置」。
  即使前面挡了一层服务端缓存，把请求预算花在页面导航上仍会和抢机循环抢同一份
  限流额度，而 OCI 的用量条款是运维方自己的责任。一并移除了只为承接自动加载
  而存在的 `web/backend/read_cache.py`（以及各接口的 `force` 参数与失效调用）——
  手动刷新下每次都会 `force`，这层缓存不再有收益，留着只是无谓的失效正确性负担
- CLAUDE.md 已注明这条曾在 0.4.20 尝试并回退，避免以后再被"顺手改掉"

### 保留（与自动加载无关，仍是 0.4.20 修的缺陷）
- 副区租户在主租户轮换密钥后静默失效：改主租户凭据现在会同步到其所有副区行
- 副区里改规格 / 引导卷扩容 / 块存储创建扩容被免费额度守卫误拦：
  这三条路径与创建实例走同一个副区闸门（`quota_guard.secondary_region_gate`）

### 维护
- 新增 `tests/test_user_isolation.py`（7 项）：两个用户**故意填同一个 Oracle
  tenancy OCID**，验证租户列表、读取、改删、抢机/定时任务、加密备份导出
  全部按用户隔离，越权一律 404（而非 403，连存在性也不泄露）

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.21
```

---

## 0.4.20 — 2026-07-28

### 功能
- **页面进入自动加载**（推翻 0.4.13 的「必须手动点刷新」）：实例、存储、账号用量、
  创建实例四个页面进入即加载，切换租户也自动加载
- **新增服务端短缓存 `web/backend/read_cache.py`** 承接由此带来的调用量：
  - 进入页面的读取走 60 秒的按租户缓存，来回切页面不重复打 Oracle
  - 点「刷新」发 `force=1` 绕过缓存强制重新请求
  - **所有会改变结果的写操作都会 `invalidate(tenant_id)`**（开关机、终止、改名、
    改规格、换 IP、创建实例、卷的增删改挂卸、桶与对象操作、租户改配置/删除）
  - 60 秒的 TTL 也是兜底：万一漏了某处失效，最多一分钟自愈
  - 授权先于缓存查询：条目按租户 ID 存，越权读取会是数据泄露，测试固定了这一点

### 修复
- **副区租户在主租户轮换密钥后会静默失效**（重要）：副区行保存的是主租户凭据的
  **副本**，而更新主租户的私钥 / 指纹 / OCID 时没有同步过去，副区会继续用旧密钥
  认证，直到报 401。现在改主租户凭据会一并更新其所有副区行
- **副区里改规格 / 扩容磁盘会被免费额度守卫误拦**：副区租户的 `account_tier` 继承自
  主租户，为空（未识别）时守卫按"硬拦"处理，于是一次用户明确付费的扩容被
  「超过免费上限」拦下 —— 而副区**根本不适用**免费额度，且用量快照只统计本区域。
  改规格、引导卷扩容、块存储创建/扩容现在与创建实例走同一个副区闸门
- **实例页带 `?tenant=` 进入会并发拉取两次**：Vue 的 watcher 在 nextTick 才 flush，
  正好落在初次加载的 await 里，导致同一租户被同时拉两遍。初次加载完成前不交给 watcher
- **`@change="loadBoot"` 把 DOM Event 当成 `force` 参数传入**（vue-tsc 捕获）

### 维护
- 新增 `tests/test_read_cache.py`（9 项）与密钥轮换回归测试
- CLAUDE.md 中「不要改：进入页面不自动请求」一条已更新为当前设计与其约束

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.20
```

---

## 0.4.19 — 2026-07-28

### 功能
- **出网流量（10TB/月）监控**：这是 Always Free 里唯一没被额度守卫盯着的一项，
  也是最现实的一种"意外账单"来源（下载站、代理很容易撞上）。
  - 数据来自 VCN 指标 `VnicToNetworkBytes`（按自然月累计），**不需要实例装 agent**
  - 「账户用量」与「创建实例」的额度面板新增一条进度条
  - Worker 每日检查，达到免费额度 **80%** 时推送一次（每月一次，复用 budget 事件通道）。
    刻意不是超额才提醒 —— 等超了才说就来不及了
- **该数字是估算上限，不是账单**，这一点在接口 note、面板和推送里都写明了：
  - `VnicToNetworkBytes` 包含区域内互通等 Oracle 不收费的流量
  - 免费额度按整个租户计算，而查询是按区域的（有副区时各区分别统计）

### 设计上的两个取舍
- **不拦截创建**：出网流量在创建时根本无从判断，因此它只做展示和告警，
  `validate_launch_against_quota` 完全不读它。桶标记为 `soft`，
  超额也不会把整体状态推成"已超额"，以免让一次无关的创建看起来被拦住
- **不在热路径上查**：`get_free_quota_usage(include_egress=...)` 默认关闭。
  只有只读的 `GET /free-quota`（用户手动刷新）会拉取；
  额度预检和 Worker 抢机每次尝试都要取快照，不该为一个影响不了结论的查询付费。
  未请求时该桶**不存在**而不是显示为 0 —— 显示 0 会被读成"还有 10TB 可用"

### 维护
- 新增 `tests/test_egress_quota.py`（12 项）
- 新增 `Tenant.egress_notified_month` 列（自动迁移，已对无该列的旧库验证）

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.19
```

---

## 0.4.18 — 2026-07-28

### 功能
- **「计算配额（参考）」只列免费套餐**：该表来自 OCI Service Limits，此前把
  E3/E4/E5/standard3 等付费 shape 也一并列出。Oracle 对免费账号同样会返回这些
  付费 shape 的非零配额，所以它们是一堆用不到的数字。现在只保留
  A1 与 E2.1.Micro（`FREE_TIER_LIMIT_TAGS`，与 `FREE_TIER_SHAPES` 对应），
  标题也改为「计算配额（参考 · 仅免费套餐 A1 / E2.1.Micro）」

### 维护
- `tests/test_account_tier.py` 新增该过滤规则的测试

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.18
```

---

## 0.4.17 — 2026-07-28

### 修复
- **开通副区报 `[404] EntityNotFound`**：区域代码被转成了小写再提交。
  `CreateRegionSubscription` 是**按这个 key 定位区域**的，Oracle 返回的 key 是大写
  （`NRT` / `KIX` / `FRA`），小写的 `kix` 不是任何实体，于是报 404 ——
  而 404 看起来又很像权限问题，很容易查错方向。
  现在区域代码原样回传给 Oracle，输入时仍可用区域名或 key、不区分大小写
- **开通失败时看不出是哪一步失败**：开通流程要依次调用「读已开通区域 → 读区域清单 →
  提交开通」三个接口，三者都可能返回 404，但之前的错误消息完全一样。
  现在每种失败都会标明所处步骤
- **404 / 401 补充权限提示**：该接口需要 API 用户具备租户级权限
  （Administrators 组或 `manage tenancies` 策略），且账号必须已升级为 PAYG ——
  纯 Always Free 账号无法订阅新区域。OCI 对无权限也返回 404，因此这条提示直接附在消息里

### 维护
- `tests/test_secondary_region.py` 增加 8 项：区域代码原样提交（回归）、
  区域名 / key 大小写混用均可、已开通时不再重复提交、三个步骤各自的错误归属、
  404 权限提示

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.17
```

---

## 0.4.16 — 2026-07-28

### 功能
- **租户开通副区（其他国家 / 地区）**：租户页新增「副区管理」，可查看该 Oracle 账号
  已开通的区域与主区，并从完整区域清单中开通新的副区
  （`GET /tenants/{id}/regions`、`POST /tenants/{id}/regions/subscribe`）。
  已在 Oracle 控制台开通过的区域也可以一键「添加到面板」，两种情况走同一个接口
- **创建实例时可选择副区**：创建页新增「区域（主区 / 副区）」下拉，
  镜像 / Shape / 网络元数据、额度预检、抢机重试都会按所选区域走
- **副区以「同凭据的子租户」建模**：开通后自动添加一个与主租户同 API 凭据、
  区域不同的租户行（`parent_tenant_id` 指向主租户）。OCI 会话本身只绑定一个区域，
  这样实例列表、存储、WebSSH、定时任务、抢机任务等每个按租户组织的页面
  都能直接管理副区，无需为每个接口再加一个 region 参数。
  删除主租户会一并删除它的副区行；备份 / 恢复会重新映射父子关系（ID 会重新生成）

### 修复 / 防呆
- **副区不适用 Always Free，守卫按区域区分**（重要）：免费额度只存在于主区，
  而用量快照是**按区域**统计的 —— 在一个全新的副区里读到的用量是 0，
  看起来像「还有 4 OCPU / 24 GB 免费额度」，据此放行会直接产生账单。
  现在创建 / 抢机 / 额度预检在副区一律改走区域闸门（`enforce_secondary_region`）：
  - 租户勾选着「仅使用免费额度」→ 直接拒绝，并说明去哪里取消勾选
  - 取消勾选（明确接受计费）→ 放行，并在结果消息里标注「按量计费」
  - 副区租户创建时默认就是「允许计费」，创建页与额度面板也不再显示会被误读为
    「有免费余量」的进度条
- 区域探测**故意保守**：只有当前区域与主区都能解析成合法的 OCI 区域 ID 时才判定为副区，
  读取失败一律按主区处理，避免探测异常把所有创建都卡死

### 维护
- 新增 `tests/test_secondary_region.py`（12 项）与 `tests/test_backup_region_link.py`
- 全接口冒烟测试覆盖新增的两个副区接口

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.16
```

---

## 0.4.15 — 2026-07-27

### 功能
- **创建实例页显示账号已用免费额度**：A1 OCPU、A1 内存、E2.1.Micro 台数、块存储（含引导卷）
  各自的「已用 / 上限 / 剩余」与进度条。选择租户即读取，可手动刷新；用量读取不完整时明确警告
- **提交前拦截超额配置**：新增 `POST /tenants/{id}/launch-quota-check` 对当前表单预演。
  它调用的是服务端真正拦截时用的同一个 `check_launch_quota`，而不是在前端重写额度算法，
  因此预检结论不会与服务端判断漂移。超额时不打开确认框并给出具体原因
  （如「A1 额度不足：需要 3 OCPU / 12 GB，剩余 2 OCPU / 12 GB」）。
  预检请求本身失败时**不阻止**创建，服务端仍会再校验一次

### 修复
- **额度校验对已升级账号形同虚设**（重要）：Oracle 账号只要升级过就会被识别为
  `account_tier="paid"`，而守卫据此把超额从「拦截」降级为「警告」。实测：已用 50GB
  引导卷的账号再创建 A1 4C/24G + **200GB** 引导卷（合计 250GB > 200GB）竟然通过校验。
  改为**按租户显式开关** `free_only_mode`（默认开启），不再从账号等级推测意图：
  - 默认：无论账号是否付费，超出 Always Free 一律拦截
  - 确实要用付费资源时，在「租户」页取消勾选「仅使用免费额度」
  - 升级已有部署时该列默认为开启，即既有租户自动受保护
  - 创建页会显示当前租户处于「仅免费额度」还是「允许超额计费」
- **实例防火墙不显示规则**：`get_instance_firewall` 只读 VNIC 的 `nsg_ids`，但多数实例
  没有 NSG —— OCI 默认网络把规则放在**子网安全列表**里。现在同时读取并以只读方式展示
  （面板的添加规则 / 一键全开放只作用于 NSG），并补上了缺失的空状态提示
- **引导卷扩容 502**：上一轮改动漏了 `quota_guard` 导入，每次请求都失败，
  而宽泛的 `except → 502` 把它伪装成 OCI 报错。新增全接口冒烟测试防止此类问题再隐形
- **块存储永远显示"未挂载"、卸载按钮永不出现**：`list_volume_attachments` 的 SDK 签名
  只接受一个位置参数，代码多传了一个导致 `TypeError` 被吞掉；同时 `delete_block_volume`
  的"仍挂载中"保护也一直静默失效
- **管理员可能把自己永久锁死**：「重置密码」对自己可点，会吊销会话、清除 2FA，
  然后立即跳转登录页销毁唯一一份新密码。现已对自己隐藏，服务端也拒绝
- **实例列表刷新按钮永久失效**：请求在途时切换租户会让 `loading` 永远为 true
- **创建实例可能提交错的镜像**：切换操作系统后镜像被"仅 Ubuntu"的列表覆盖，
  规格回落到 x86 固定型号，可能把 ARM 镜像提交到 x86 shape 上
- 拒绝的开关机 / 重命名 / 终止被显示为成功（终止还会跳转）；取消保留 IP 名称输入仍会创建；
  NSG 端口留空报英文 422；清空月度预算导致整个租户编辑失败；存储页把租户级失败显示成"暂无卷"；
  WebSSH 远端退出后终端看似仍连接；删除控制台连接失败也报成功；
  已达上限的抢机任务点「继续」是空操作；定时任务执行失败既无历史也无告警；
  `24:00` 这类时间可保存但永不触发；备份漏了月度预算；取消全部推送事件反而恢复全选
- 修复 zip 炸弹绕过（伪造 `file_size` 可使 398KB 上传产生 830MB 分配）、
  额度 fail-closed 在 Worker 侧被第二次读取绕过、自更新误报成功等 32 项核查结论

### 维护
- 删除孤儿组件 `views/BootVolumesView.vue`（无路由、无引用）
- 新增 `tests/test_version_bump.py`：`app_version` 必须与 CHANGELOG 顶部版本一致，
  忘记递增会导致测试失败
- 测试总数 153 → 302

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 应为 0.4.15
```

---

## 0.4.14 — 2026-07-26

### 安全（重要）
- **修复登录限流可被绕过**：`run.py` 之前让 uvicorn 无条件信任所有来源的
  `X-Forwarded-For`（`forwarded_allow_ips="*"`），它会直接改写 `request.client.host`，
  而这正是登录限流的计数键 —— 攻击者每次请求伪造一个 IP 即可无限暴力破解密码。
  现在只有 `OCIBOT_TRUST_PROXY=1` 时才信任代理头，且仅信任
  `OCIBOT_FORWARDED_ALLOW_IPS`（默认回环地址）
- **WebSSH 增加 Origin 校验**：WebSocket 不受 CORS 保护，`SameSite=none` 配置下
  任意网站都可借受害者 Cookie 打开终端（CSWSH）；跨站握手现在在 accept 前直接拒绝
- **忽略 `OCIBOT_CORS_ORIGINS=*`**：通配符 + Cookie 凭据会让 Starlette 回显来源域，
  等于任意站点可以以登录用户身份读取 API；启动时会明确告警
- **上传不再整体读入内存**：备份导入 / 对象上传改为分块限量读取，两个接口从
  `async def` 改为同步（线程池）执行，避免阻塞事件循环；新增 32MB 请求体上限
- **补齐 SSRF 地址过滤**：新增 `0.0.0.0/8`、`100.64.0.0/10`、`192.0.0.0/24`、
  `240.0.0.0/4` 等段，并解开 NAT64 `64:ff9b::/96` 与 6to4 `2002::/16` 中内嵌的 IPv4
  （此前 `64:ff9b::a9fe:a9fe` 可绕过限制访问云元数据）
- 新增 HSTS / COOP / CORP 响应头（HSTS 仅在 `OCIBOT_COOKIE_SECURE=1` 时下发）
- **面板内一键更新改为默认关闭**：它会驱动挂载的 `docker.sock` 并可能进入宿主机
  命名空间，管理员失陷≈宿主机 root。`scripts/install.sh` 仍显式置 1，推荐安装方式不受影响
- 弱 / 短 `OCIBOT_MASTER_KEY` 启动时给出具体告警（密钥派生方式未改动，改了会导致
  已存私钥无法解密）
- compose 增加 `no-new-privileges`，新增 `OCIBOT_BIND` 可只监听 127.0.0.1

### 修复
- **容量重试的降级配置会被丢弃**：`POST /jobs/capacity` 校验了 `fallback_configs`
  却没写入任务行，Worker 因此只会尝试主配置；现已持久化并与创建向导共用校验
- **定时任务可能重复执行**：「今天已跑」标记只 `flush()` 未提交，两个 Worker 在同一
  分钟会各发一次开关机；改为提交后再执行（SQLite 上旧代码则会因锁冲突静默跳过）
- 创建实例时额度校验失败会抛未处理的 500，现返回 502 并带原因
- `prepare_launch_network()` 用 `auth_mode` 推断重试模式，导致普通创建被按重试规则校验
- 备份导入遇到超范围 `password_expiry_days` 会 500，现做截断
- 登录后跳转只接受站内路径
- `AdminView.vue` 日志翻译表类型错误（vue-tsc 报错，vite 不做类型检查所以此前未暴露）

### 第二轮（并行审计复查，13 个 agent）
上一轮的修复被独立复查，发现三处真实缺陷并已修正：

- **请求体上限可被 `chunked` 绕过**：只检查 `Content-Length` 是不够的，40MB 会被完整落盘；
  而且 FastAPI 在依赖注入**之前**解析 multipart，所以未认证即可触发，同一手法还能让单个
  请求驻留约 1GB 表单字段。改为按**实际收到的字节**计数的纯 ASGI 中间件
- **`_client_ip` 取 XFF 最左项**：nginx 标准的 `$proxy_add_x_forwarded_for` 是追加而非替换，
  最左项仍由客户端控制，`TRUST_PROXY=1` 时限流照样能绕；现统一使用 `request.client.host`
- **IDNA 编码器不一致导致 SSRF**：`getaddrinfo` 用 IDNA2003、httpx 用 IDNA2008，
  `evilß.example.com` 校验的是 `evilss.example.com` 却连向 `xn--evil-yna.example.com`

同时修复：
- **`POSTGRES_PASSWORD` 一直是默认值**：compose 的 `${VAR}` 插值只读项目根目录的 `.env`，
  不读 `env_file`，所以 `install.sh` 生成的随机密码从未生效。现在会自动建立
  `.env → web/.env` 链接，并在更新时幂等地 `ALTER USER` 对齐（先起 db、改密码、再起 api）
- **`docker-compose.yml` 的 `environment:` 覆盖 `env_file:`**：`OCIBOT_REQUIRE_SECURE_SECRETS` 等
  5 个键重复声明，导致 `web/.env` 里的值被静默丢弃，生产密钥守卫从未生效
- 粘贴 OCI 配置的正则回溯（ReDoS）：108KB 占用 2.3s GIL，现 0.26ms（约 8700 倍）
- `account_tier` 只要不是已知值就**关闭免费额度硬上限**，已反转为仅 `paid` 放开
- 恢复已停止的容量重试任务会绕过"每租户仅一个"限制，导致两个任务竞争 LaunchInstance
- 解密后的 OCI 私钥曾写入系统临时目录且不清理，改为内存内 `key_content`
- 创建实例后 800ms 自动跳转会**销毁一次性 root 密码**，改为需手动确认已保存
- `list_images` 算出 Ubuntu 过滤结果却丢弃、`list_objects` 缺 `fields` 导致用量恒为 0、
  换公网 IP 在解绑超时后误报成功并返回旧地址
- SSH 公钥与启动脚本只挡 `\n`，`\r`/`\x85`/U+2028/U+2029 也是 YAML 换行，可注入 cloud-init
- 限流字典无上界、`LoginRequest.username` 无长度限制
- `install.sh update` 的 `git clean -fd` 会删除未跟踪的运维文件（需 `OCIBOT_CLEAN_UNTRACKED=1` 才执行）
- 自更新用 `docker run -d` 的退出码判断成功（它只代表容器启动了）

### 第三轮（补齐此前搁置的三项）

- **WebSSH / 引导卷扩容现在校验 SSH 主机密钥**（此前 `known_hosts=None`，完全不校验，
  任何能在该地址应答的一方都能冒充实例并拿到你输入的 SSH 凭据）
  - 首次连接记录指纹（TOFU），之后不符即**在发送任何凭据之前**中止
  - 用 `asyncssh.get_server_host_key()` 只做密钥交换，不认证 —— 这是安全性的关键：
    若在 `connect()` 之后才检查，凭据已经交给冒充方了
  - 指纹按**实例 OCID** 记录而非 IP，所以换公网 IP / 停开机不会误报
    （这正是当初跳过校验的理由）
  - 重装系统后指纹会合法变化：实例详情页提供「重置主机密钥」，并有
    `GET/DELETE /api/tenants/{tid}/instances/{iid}/host-key`
- **额度读取不完整时不再 fail-open**：此前每个子查询各自 try/except，限流或报错会得到
  一份"用量为 0"的快照，校验器读成"额度全空"从而放行，可能产生真实费用。
  现在 `get_free_quota_usage` 会标记 `read_incomplete`（含 `list_instances_tree` 的
  分区间部分失败、以及卷列表的 `errors`），API 侧返回 503 拒绝，
  Worker 侧**推迟本次尝试且不消耗次数**（不会因为一次抖动就杀掉长跑的抢机任务）。
  付费账号不受影响 —— 它本就不受硬上限约束
  - 仅根据"是否有 notes"判断是不行的：对象存储的近似统计在**成功时**也会写 notes
- **自更新并发锁不再只在单进程内有效**：`threading.Lock` 挡不住 `OCIBOT_API_WORKERS=2`
  的另一个进程。改为在写入 `running` 的同一事务里用 `SELECT ... FOR UPDATE` 复查状态行
  （SQLite 上是空操作，但那种部署本就是单进程），并把网络请求移出临界区

### 测试
- 新增 `tests/test_web_hardening.py`、`test_upload_limits.py`、
  `test_schedule_single_fire.py`、`test_capacity_job_create.py`、`test_audit_pass2.py`、
  `test_ssh_hostkey.py`、`test_quota_fail_closed.py`，并扩充 `test_url_safety.py`
- 共 258 passed / 1 skipped（原 153）

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 0.4.14
```

---

## 0.4.13 — 2026-07-26

### 行为 · 减少 Oracle API 调用
- **进入面板不再自动打 OCI**：实例 / 创建 / 账号用量 / 存储 / 引导卷等页仅在用户点击「刷新 / 加载」时请求
- 实例列表默认不解析 IP；切换租户或解析 IP 开关不自动重拉
- 租户导入默认关闭「保存后测试连接」
- 禁用 `GET /instances` 全租户聚合接口，强制单租户查询
- Worker 每日预算检查按租户串行并间隔，避免多账号同时打 Usage API
- 实例详情进入时不再预取引导卷 / 默认强拉监控（按当前页签按需加载）

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 0.4.13
```

---

## 0.4.12 — 2026-07-26

### 体验
- 系统更新「更新日志」以中文摘要展示（git/docker/compose 常见输出翻译）
- 新增站点 Logo / Favicon（侧栏、登录页、浏览器标签）

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 0.4.12
```

---

## 0.4.11 — 2026-07-26

### 修复
- 实例列表 / 详情不再展示冗长 OCID（仍可通过路由与复制接口使用）
- **网络监控数据异常**：`NetworksBytesIn/Out` 改为 MQL `.rate()`（B/s），多 VNIC 同秒求和；前端按 B/s·KB/s·MB/s 显示

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 0.4.11
```

---

## 0.4.10 — 2026-07-26

### 修复
- 实例列表首屏不再双重拉取 OCI（bootstrap 门闩）
- 创建实例切换租户时重置/校验 AD，并丢弃过期 launch-meta 响应
- SMTP 发送时再次校验主机（防 DNS 重绑定 SSRF）

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 0.4.10
```

---

## 0.4.9 — 2026-07-26

### 修复
- **密码到期策略真正可写库并即时回显**：编辑租户可改天数（0=关闭）、快捷 90/120；「仅保存密码策略 / 保存全部 / 已改密」写入后列表徽章立刻更新；普通保存路径同步钳制天数
- **实例列表默认不再聚合全部租户**：默认选中第一个租户；下拉去掉「全部租户（聚合）」
- 切换「解析 IP」会重新拉取实例
- 监控曲线对非法采样值做防护，避免 `toFixed` 异常
- 正式版 README

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 0.4.9
```

---

## 0.4.8 — 2026-07-26

### UI · 玻璃质感 + 实例操作 / 监控
- 全局卡片、侧栏、表格、按钮改为毛玻璃（半透明 + blur + 高光边）
- 实例列表操作按钮收进胶囊按钮组，尺寸与风格统一
- 实例监控：鼠标/触控移到曲线上显示该时刻具体数值与时间，右上角实时读数

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 0.4.8
```

---

## 0.4.7 — 2026-07-26

### UI
- 全局修正 checkbox / radio 与文字不在同一水平线的问题（`.choice` 布局）
- 「开放 Guest 防火墙」改为通俗文案：**允许外网直接访问（放宽防火墙）**，并附说明
- 租户密码策略文案：明确 **0=关闭提醒**，可自行取消 120 天；保存写入数据库

### 在线更新
- 面板更新改为：**定位 `OCIBOT_HOST_REPO` → 执行与 SSH 相同的 `bash scripts/install.sh update`**
- `install.sh` 支持 `OCIBOT_SKIP_GIT=1`（代码已由面板拉取时跳过二次 git）
- 若宿主机 nsenter 路径不可用，回退到仓库内 `docker compose build && up -d`

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 0.4.7
```

---

## 0.4.6 — 2026-07-26

### 安全加固
- **Webhook / Bark SSRF**：校验 URL 协议与主机，解析 DNS 后拒绝私网/回环/链路本地/云元数据；禁止跟随重定向与信任环境代理
- **SMTP 主机**：同样拒绝内网目标
- **登录限流**：默认不信任 `X-Forwarded-For`（需 `OCIBOT_TRUST_PROXY=1` 且前置代理覆盖该头）
- **登录时序**：用户名不存在时仍走 bcrypt，降低枚举差异
- **WebSSH**：移除 query-string JWT（防日志/Referer 泄露），仅 cookie / Authorization Bearer
- **备份导入**：限制租户数量与解压后大小，忽略归档内 `owner_id`
- **HTTP 安全头**：`CSP` / `X-Frame-Options` / `nosniff` / `Referrer-Policy`
- **自更新 GitHub 请求**：仓库名/分支名白名单；`trust_env=False`

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 0.4.6
```

---

## 0.4.5 — 2026-07-26

### 修复 · 在线更新后服务挂掉 / 更新未完成
- **根因**：重启 API 时可能注入空的 `POSTGRES_PASSWORD`、缺少 `web/.env`，以及在 API 进程内同步 recreate 时被自己杀掉，栈停在半死状态
- compose 一律带 `--env-file web/.env`；不再传入空密钥环境变量
- **构建完成后**由独立 `docker:cli` 容器跑重启脚本：`worker → api`，失败则 `compose up -d` 全量拉起
- 状态机：中断遗留的 `running` 会在超时后自动标为 error，避免「更新中」卡死无法重试
- 后台重启失败时同步走 `compose up -d` 恢复路径，并给出 SSH 修复提示

### 服务器已挂时（先恢复）
```bash
cd /root/ocibot || cd ~/ocibot
cp -a web/.env /tmp/ocibot.env.bak 2>/dev/null || true
export OCIBOT_HOST_REPO="$(pwd -P)"
# 拉最新修复（若 git 可用）
git fetch origin main && git reset --hard origin/main
cp -a /tmp/ocibot.env.bak web/.env 2>/dev/null || true
bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 期望 0.4.5
```

之后面板「一键更新」应可在重启后自动回血。

---

## 0.4.4 — 2026-07-26

### 修复 · install.sh `cd: /root/ocibot\\n/root/ocibot`
- 根因：`export_build_env` 写成 `cd && pwd -P || cd && pwd`，shell 优先级导致 **pwd 执行两次**，`REPO_DIR` 变成带换行的双路径
- 现改为分步解析 `pwd -P`，失败再回退 `pwd`，并去掉 CR/LF
- `compose()` 增加目录有效性检查

### 服务器（你现在卡住时直接跑）
```bash
cd /root/ocibot
cp -a web/.env /tmp/ocibot.env.bak
# 先手动拉脚本修复（若 update 仍因旧脚本失败）：
curl -fsSL https://raw.githubusercontent.com/674542449/ocibot/main/scripts/install.sh -o scripts/install.sh
chmod +x scripts/install.sh
cp -a /tmp/ocibot.env.bak web/.env
export OCIBOT_HOST_REPO=/root/ocibot
bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 0.4.4
```

---

## 0.4.3 — 2026-07-26

### 修复 · 在线更新 build 失败（加强）
- **正确解析宿主机仓库绝对路径**（`/proc/self/mountinfo` + `OCIBOT_HOST_REPO`），避免 bind 到错误目录
- compose 通过 `docker run docker:27-cli` 执行（自带 compose 插件）
- 构建前检查磁盘空间、预拉 cli 镜像；失败时给出可读中文原因
- 从 `web/.env` 注入 `POSTGRES_PASSWORD` 等变量，避免 compose 插值缺参
- install.sh 导出**绝对路径** `OCIBOT_HOST_REPO`，并预拉 `docker:27-cli`
- compose 默认挂载改为 `${OCIBOT_HOST_REPO:-/root/ocibot}`（不再用相对 `.`）

### 服务器请先命令行升到本版
```bash
cd ~/ocibot
cp -a web/.env /tmp/ocibot.env.bak
git fetch origin main && git reset --hard origin/main
cp -a /tmp/ocibot.env.bak web/.env
# 确认绝对路径
export OCIBOT_HOST_REPO="$(pwd -P)"
bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # 0.4.3
```

若仍失败，把管理页「更新日志」全文发出来。

---

## 0.4.2 — 2026-07-26

### 变更
- **关闭**实例详情「制作镜像」入口
- 后端 `POST .../create-image` 返回 403，提示改用引导卷备份
- 引导卷备份说明文案同步更新

---

## 0.4.1 — 2026-07-26

### 修复 · 在线更新 `docker compose build` 失败
- 根因：API 镜像内的静态 `docker` **没有 compose 插件**，`docker compose build` 必然失败
- 现改为通过 `docker run --rm docker:27-cli` 执行 compose（官方镜像自带插件）
- 构建时注入宿主机路径 `OCIBOT_HOST_REPO`，正确挂载代码目录
- **先重启 worker，再分离重启 api**，避免更新进程把自己杀掉导致中断
- 能力检测增加 `docker_daemon`；失败日志保留更多尾部输出

### 升级（服务器）
```bash
cd ~/ocibot
# 先命令行升到本修复（旧版网页更新仍会失败）
git fetch origin main && git reset --hard origin/main
# 保留密钥
# （若 reset 掉了 .env，从备份拷回 web/.env）
bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # version 0.4.1
```
之后即可在「用户管理 → 系统更新」使用一键更新。

---

## 0.4.0 — 2026-07-26

### UI · 字节系控制台风格
- **默认浅色**主题（飞书 / 火山引擎风格）：灰底 `#f2f3f5`、白卡片、主色 `#3370ff`
- 侧栏分区导航（工作台 / 资源 / 系统）、轻量图标、选中浅蓝底
- 按钮、输入框、表格、徽章、Tab、Toast 统一圆角与阴影
- 登录页品牌区 + 分段切换；暗色模式仍可选，色板同步调整
- 版本号 **0.4.0**

### 升级
```bash
cd ~/ocibot && bash scripts/install.sh update
curl -s http://127.0.0.1:8000/api/health   # version 0.4.0
# 浏览器 Ctrl+F5；若仍是旧暗色可清 localStorage 的 ocibot_theme
```

---

## 0.3.0 — 2026-07-26

### 手机端适配（本次）
- **侧栏抽屉导航**：窄屏顶部栏 + ☰ 菜单，避免侧栏占满整屏
- **页面工具栏**：`.page-head` / `.page-tools` 在手机上纵向堆叠，控件全宽
- **表格**：横向滚动提示，减小强制最小宽度，操作按钮可换行
- **标签页**：实例详情 / 存储等 tab 可左右滑动
- **表单**：手机输入框 16px，减轻 iOS 聚焦放大
- **安全区**：支持刘海屏 `safe-area-inset`
- **WebSSH**：终端高度随视口自适应
- **登录页**：小屏字号与 padding 优化

### 如何验证版本
```bash
curl -s http://127.0.0.1:8000/api/health
# "version": "0.3.0"
```
侧栏底部「构建 xxxxxxx」；管理员页「系统更新」。

---

## 0.2.0 — 2026-07-26

### 功能
- 管理员**在线检查 / 一键更新**（GitHub + docker compose）
- 更新脚本对分叉仓库 **hard reset 到 origin**（保留 `web/.env`）
- 侧栏显示构建 git sha；健康检查返回 `app_version` / `git_sha`
- 系统更新入口置顶，导航改名「用户管理 / 更新」

### 部署
- API 镜像含 `git` + 多架构 `docker` CLI
- compose 挂载宿主机仓库与 `docker.sock`（`OCIBOT_UPDATE_ENABLED=1`）

---

## 0.1.x — 此前累计

- Web 化（Vue3 + Vite + TS + FastAPI），移除桌面 Tk 版
- Always Free 仪表盘、创建/改规格/扩容**额度守卫**
- WebSSH、引导卷 SSH 扩 FS、块/对象存储
- 复制改用 toast + HTTP 剪贴板回退
- Docker Compose + **PostgreSQL** 一键安装/更新脚本
- 公网 IP 单击复制修复

---

升级：

```bash
cd ~/ocibot
bash scripts/install.sh update
# 浏览器 Ctrl+F5
```
